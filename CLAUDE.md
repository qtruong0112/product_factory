# CLAUDE.md — Product Factory 5.1 (bàn giao cho Claude Code)

> File này Claude Code đọc mỗi phiên. Nó KHÔNG thay thế `PROJECT_STATUS.md` và `NEXT_WORK.md` — hai file đó là **nguồn sự thật đầy đủ**. Luôn đọc cả ba trước khi code.

## QUY TẮC VÀNG
- **Code là chuẩn.** MD lệch với code thật → tin code. Trước khi làm gì: quét code trong `product-factory/`.
- **Không hỏi lại những gì MD đã trả lời.** Không đoán tên cột — lấy từ `Product_Factory.sql` (DDL) hoặc canonical JSON.
- **Không tự bịa dữ liệu.** Data hiển thị = data thật từ DB qua API — kể cả Dashboard KPI tổng hợp (Giai đoạn 26: viết lại 100%, không còn ngoại lệ).

## VỊ TRÍ THƯ MỤC (quan trọng)
Gốc dự án nằm ở **`product-factory/`** (thư mục lồng), KHÔNG ở gốc repo:
- `product-factory/backend/`   → Java 21 + Spring Boot 3.3.4 + Maven, **Clean Architecture layer-first** (Giai đoạn 27): package `com.f88.productfactory.{domain.model.<feature>|domain.repository.<feature>|application.service.<feature>|application.common|application.dto.<feature>|infrastructure.config|presentation.controller.<feature>|presentation.common}`. Entity → `domain.model`, Spring Data repo → `domain.repository`, business/join/enrichment logic → `application.service` (Service class), REST endpoint → `presentation.controller` (Controller THIN, chỉ gọi Service). Tài nguyên đọc-only thuần dùng chung `application.common.ReadOnlyService<T,ID>` + `presentation.common.ReadOnlyController<T,ID>`.
- `product-factory/frontend/`  → React 18 + Vite + TS, cấu trúc theo layer (Giai đoạn 28): `src/infrastructure/` (api client, `icons.ts`, `nav.ts`, `tables.ts` — kết nối hạ tầng/config) + `src/presentation/{components,pages}/` (toàn bộ UI). `main.tsx` ở gốc `src/` (entry + router). **`presentation/pages/` (Giai đoạn 31, +34, +35, +36):** 11 cặp list+detail liên quan gộp vào subfolder cùng tên nav key — `businessintent/`, `intent/`, `pattern/`, `template/`, `config/`, `attribute/`, `archetype/`, `lifecycle/`, `domain/`, `block/`, `variant/` (mỗi folder 2 file: `XxxPage.tsx` + `XxxDetailPage.tsx`); 9 page chỉ 1 file (dashboard, matrix, obligation, ontology, sysmap, catalog, release, activity, simulation) giữ phẳng ngay trong `pages/` — không tạo subfolder cho page đơn lẻ.
- `product-factory/docker-compose.yml`
Mọi lệnh build phải `cd product-factory/...` cho đúng cấp (lỗi hay gặp: "no configuration file provided" do đứng sai thư mục).

## FILE NGUỒN (bắt buộc có trong repo — KHÔNG được đoán thay)
- **Prototype UI:** `Product_Factory_5_1.html` — trích markup pixel-perfect cho MỌI màn mới. Nếu không thấy file này trong repo → DỪNG, báo user bổ sung, KHÔNG tự chế markup.
- **DDL:** `backend/src/main/resources/db/migration/V1__schema.sql` (43 bảng) — nguồn tên cột chính xác.
- **Seed:** `backend/src/main/resources/db/migration/V2__seed.sql` — data thật để verify.
- **Canonical (nếu có):** `product_factory_canonical_v3_semantic.json` — cấu trúc logic 43 bảng.
> Cách trích markup từ prototype: mục 8.5 PROJECT_STATUS (tìm mốc `openBuilder`/`BLOCKS(){`/tên view, unescape thủ công, KHÔNG dùng `unicode_escape`).

## LUẬT CỨNG (mục 2 & 7 PROJECT_STATUS)
- **Read-only 90%.** Backend chỉ `GET`. Entity chỉ getter, `@Entity @Table` (nằm ở `domain.model.<feature>`). Read-only thuần → Controller (`presentation.controller.<feature>`) extends `presentation.common.ReadOnlyController<T,ID>`, override `service()` trả `new ReadOnlyService<>(repo)`; cần join làm giàu list → tách riêng `application.service.<feature>.XxxService` (chứa logic) + Controller thin gọi Service, trả `Page<Map<String,Object>>`. Composite PK → `@IdClass` + `Serializable` (đặt cùng `domain.model` với entity).
- **Enum PostgreSQL map bằng `@Column String`** (KHÔNG `@Enumerated`). Resource path kebab số nhiều: `/api/product-patterns`.
- **Frontend pixel-perfect từ prototype** (`Product_Factory_5_1.html`), inline style trích nguyên — không chế lại. Màn list dùng `ListScreen` (signature THẬT: `rows: ReactNode[][]`), `StatusChip`, `Icon` (35 icon, **không có** lock/history/grid). Bảng màu: xanh `#0E8C5A` / đậm `#0B7349` / sidebar `#0B3B2E→#082A20`; chữ `#122019`/`#5E6F66`; viền `#E6ECE8`; nền `#F4F7F5`. Font `Be Vietnam Pro`.
- **Màn builder/biểu đồ KHÔNG rút gọn thành detail thường** (bài học Pattern). Trích đúng markup builder.
- **nav key ≠ resource path**: intent→`/api/product-intents`, businessintent→`/api/business-intents`, pattern→`/api/product-patterns`. Route detail đặt TRƯỚC `/:view` trong `main.tsx`.
- Nút CUD: giữ giao diện, no-op, tooltip "read-only".

## QUY TRÌNH MỖI MÀN
code → `cd product-factory/frontend && npm install && npm run build` (0 lỗi TS) → render kiểm chứng so khớp prototype → mới coi là xong → cập nhật `PROJECT_STATUS.md` + `NEXT_WORK.md`.
**Khác với môi trường web trước:** máy local này **build được backend thật** (`mvn` / `docker compose up --build`) — không còn giới hạn "sandbox chặn Maven 403". Vẫn giữ kỷ luật verify. Không cần đóng zip mỗi lượt nữa — dùng **git commit** từng màn thay cho zip trọn vẹn.

## MÀN KẾ TIẾP
**TẤT CẢ 18 MÀN + Simulation Engine + Attribute Usage ĐÃ HOÀN TẤT.** Vừa xong **Giai đoạn 48 — bổ sung Block "Hạn mức" (BLK_LIMIT) vào Pattern PT-001**: user hỏi tại sao Template TPL-001 có "hạn mức" mà Config/Variant đóng gói từ nó (CFG-0043/VAR-108 "Vay xe máy mùa tựu trường") lại thiếu, muốn thêm cho đủ. Audit xác nhận: Pattern PT-001 (nguồn của TPL-001 → CFG-0021/0040/0043 → VAR-106/103/108) **chưa từng gán Block `BLK_LIMIT` vào `pattern_block` ngay từ đầu** (chỉ có 7 block: Bên tham gia/Tuân thủ/Lãi suất/Phí/Trả nợ/Tài sản ĐB/Phạt trễ hạn) — không phải bug ngẫu nhiên mà là Pattern gốc thiếu sót. Phát hiện thêm: `limit_range` hiển thị trên Variant (list/Simulation) đọc từ 1 cột riêng `product_variant.limit_range` (text tự do, độc lập Pattern/Block) — nên Variant vẫn "có vẻ" đủ hạn mức dù chuỗi Answer Slot hoàn toàn không có, gây cảm giác lệch giữa 2 màn. Đã sửa `V2__seed.sql`: thêm `('PT-001','BLK_LIMIT',8,'active')` vào `pattern_block`; thêm `template_frame` default cho TPL-001 (`3tr – 80tr`) và TPL-002 (`50tr – 2 tỷ`, cùng Pattern PT-001 nhưng chưa có Config nào nên chỉ để đủ, không ảnh hưởng UI); thêm fragment `limit_amount`+`capacity_range` (scope `default`) cho CẢ 3 Config đang dùng TPL-001 (CFG-0021/CFG-0040/CFG-0043) — khớp ĐÚNG giá trị thật đã có sẵn ở cột `product_variant.limit_range` của từng Variant tương ứng (VAR-106="2tr–30tr", VAR-103="3tr–80tr", VAR-108="3tr–40tr", quy đổi sang định dạng VND đầy đủ như `2.000.000đ – 30.000.000đ`), `capacity_range` dùng lại giá trị "Có quản trị" đã có tiền lệ ở CFG-0042. Không đổi code — thuần bổ sung seed data để lấp lỗ hổng Pattern, tái dùng đúng luồng resolve (`buildBody`/`resolved()`) đã có từ Giai đoạn 46. Verify: `GET /api/product-configs/CFG-0043/detail` (builder) và `/resolved` đều lên đúng **17/17 slot bắt buộc = 100%** (trước đó 15/15, giờ +2 do BLK_LIMIT có 2 slot bắt buộc); `GET /api/product-patterns/PT-001/detail` trả đủ 8 block, không lỗi; Playwright `/config/CFG-0043` (builder) hiện đúng nhóm "Hạn mức" trong sidebar, `/variant/VAR-108` tab "Giá trị cấu hình" hiện đúng "Hạn mức cấp: 3.000.000đ – 40.000.000đ". Trước đó **Giai đoạn 47 — Simulation Engine bỏ hardcode, tính từ dữ liệu thật của sản phẩm**: sau Giai đoạn 46, user hỏi Simulation Engine (`SimulationService`/`SimulationEngine`) có đang tính từ dữ liệu thật không hay đang hardcode. Audit `deriveFromVariant()` đối chiếu toàn bộ Answer Slot/Fragment/Template Frame thật, chia 3 nhóm: A) đã thật (amount/months/rate/LTV, không sửa); B) DB có dữ liệu thật per-product nhưng code hardcode bỏ qua — user xác nhận sửa: `appraisalFee` (hardcode 500.000đ mọi sản phẩm dù `template_frame`/`fragment` slot `fee_amount` có giá trị khác nhau thật: TPL-001=300k/TPL-002=800k/TPL-004=400k), `segmentCode` (hardcode `SEG_STANDARD` dù bảng `template_segment` cho biết đúng phân khúc thật mỗi Template, vd TPL-002=`SEG_BUSINESS`), `penaltyFactor` (lấy trần quy định `attribute_constraint` làm giá trị áp dụng thay vì đọc chính sách thật slot `penalty_rate`), và `grace` (số ngày trễ hạn được miễn phạt, slot `BLK_PENALTY.grace` — hoàn toàn chưa đọc ở đâu; đã hỏi user xác nhận đây KHÁC khái niệm `graceMonths`/`graceOn` có sẵn — trục ân hạn gốc đầu kỳ vay, thuộc nhóm C do người dùng tự bật, không có nguồn DB); C) genuinely không có nguồn (`periodicFeePct`, `startDate`, toggle kịch bản penalty/prepay/early/graceOn) — theo yêu cầu user, KHÔNG đụng vào nhóm C. Backend: `SimulationService` inject thêm `TemplateFrameRepository`/`TemplateSegmentRepository`/`AnswerSlotRepository`; thêm `resolveSlotValue(configCode, templateCode, blockId, slotCode)` (3 tầng fallback giống Giai đoạn 46: fragment scope="default" → `template_frame` → `answer_slot.default_value`) + parser `parseVndAmount`/`parseFirstInt`. `deriveFromVariant()` giờ resolve `appraisalFee` (slot `fee_amount`, BLK_FEE) và `segmentCode` (`template_segment` theo Template nguồn) thật, chỉ fallback hardcode khi cả 3 tầng đều thiếu. `applyRegulatoryCaps()` (đổi tên ý nghĩa, vẫn giữ tên) giờ resolve thêm `penaltyFactor` thật (slot `penalty_rate`) và field MỚI `graceDays` (slot `grace`, khác `graceMonths`) theo `req.getConfigCode()` — áp dụng cho CẢ `getDefault()` và `run()` vì đây là chính sách sản phẩm cố định (khác `appraisalFee`/`segmentCode` vẫn là input người dùng chỉnh tay trên form, chỉ sửa initial value). `SimulationRequest` thêm field `graceDays`. `SimulationEngine.run()`: công thức phạt đổi từ `pmt*(penaltyDays/30)*r*penaltyFactor` thành trừ ân hạn trước: `billableDays = max(0, penaltyDays - graceDays)`; kết quả trả thêm `graceDaysApplied` để minh bạch. Frontend: `SimulationPage.tsx` thêm `graceDaysApplied` vào `RunResult`, card "TỔNG PHẠT TRỄ HẠN" hiện thêm dòng "miễn N ngày đầu (chính sách sản phẩm)" khi `graceDaysApplied>0`. Verify: VAR-101 (CFG-0042←TPL-003, không có fee_amount/penalty_rate riêng ở cả 3 tầng) đúng fallback 500.000đ (không phải bug, do thật sự không có dữ liệu nào); VAR-103 (CFG-0040←TPL-001, có fee_amount=300.000đ thật) trả đúng 300.000đ + segmentCode=SEG_INDIVIDUAL (không còn SEG_STANDARD hardcode); test phạt trễ hạn 10 ngày với configCode có graceDays=5 ra tổng phạt đúng bằng 1/2 so với khi không có configCode (graceDays=0) — xác nhận công thức trừ ân hạn hoạt động đúng; Playwright xác nhận UI hiện đúng dòng "Kỳ 3 · trễ 10 ngày · miễn 5 ngày đầu (chính sách sản phẩm)". Trước đó **Giai đoạn 46 — hiện ĐỦ mọi Answer Slot ở tab "Giá trị cấu hình" của Variant**: user mô tả lại đúng bản chất resolve giá trị (Pattern định nghĩa Block → Template `template_frame`=default → Config `fragment`=ghi đè → Variant đóng gói), muốn tab này hiện đủ mọi Answer Slot của Pattern, slot nào không có fragment thì lấy default Template. Audit phát hiện: tab này tái dùng thẳng `GET /api/product-configs/{code}/detail` (API màn **builder** Config), và dòng `ProductConfigService.java` (`activeBlocks = fragBlocks.isEmpty() ? frameBlocks : fragBlocks`) **cố ý** (từ Giai đoạn 21, để khớp pixel-perfect 1 ảnh chụp CFG-0042) loại bỏ toàn bộ block CHƯA từng có fragment ngay khi config có fragment ở block khác — kiểm chứng thật CFG-0042 (pattern 9 block/24 slot) chỉ trả 6 block/17 slot, mất hẳn `BLK_BILLING`/`BLK_ELIGIBILITY`/`BLK_REGULATORY` dù có default Template. Cũng phát hiện completeness% (`totalReq`/`reqFilled`) bị tính THIẾU do cùng nguyên nhân. Quyết định kiến trúc (hỏi user qua AskUserQuestion, chọn phương án tách): **không sửa** `detail()` (giữ nguyên màn builder Config, tránh regression) — refactor phần vòng lặp block/slot thành private method dùng chung `buildBody(cfg, tpl, patternCode, frameByKey, fragsByKey, blockFilter)` (`blockFilter=null` = không lọc), thêm method mới `resolved(code)` gọi `blockFilter=null` + endpoint `GET /api/product-configs/{code}/resolved`. Tiện thể bổ sung field `slotDefaultValue` (= `answer_slot.default_value`, cột có sẵn nhưng trước đây không dùng ở đâu trong luồng resolve) làm tầng fallback thứ 3, áp dụng cho CẢ 2 method. Frontend: `ConfigValuesTab` (trong `ProductVariantDetailPage.tsx`) đổi fetch sang `getDetail(..., 'resolved')`, thêm nhánh fallback "Mặc định Answer Slot: X" khi không có fragment lẫn `inheritedFrameValue`. Không đổi gì `ProductConfigDetailPage.tsx` (builder). Verify: curl `/detail` CFG-0042 vẫn y hệt cũ (6 block/17 slot, không regression); `/resolved` CFG-0042 trả đủ 9 block/24 slot, completeness đổi từ 86% (14 slot) → 67% (18 slot, đúng thật); Playwright `/variant/VAR-101` tab Giá trị cấu hình hiện đủ 9 block (kể cả "Sao kê & Hóa đơn" trước đây ẩn) với đúng giá trị Kế thừa Template; `/config/CFG-0042` (builder) không đổi, vẫn 6 block. Trước đó **Giai đoạn 45 — màn detail cho Nhật ký hoạt động**: user yêu cầu thêm detail cho từng dòng activity log — prototype gốc không có (đã xác nhận từ Giai đoạn 29, activity chỉ list), nên đây là UI mới thiết kế theo đúng ngôn ngữ thiết kế đã lập ở các detail khác (banner gradient/StatusChip-style badge/fadeUp). Backend: `ActivityLogService.detail(id)` (mới, trả thẳng `toRow()` — không bọc thêm) + `GET /api/activity-logs/{id}/detail`; `toRow()` bổ sung field `id` (trước đây thiếu, cần cho row-click). Frontend: chuyển `ActivityPage.tsx` vào `pages/activity/` (theo convention Giai đoạn 31 — 1 khi có detail thì gộp subfolder), thêm `onRowClick` điều hướng `/activity/{id}`; `ActivityDetailPage.tsx` (mới): banner badge màu theo hành động (tái dùng thẳng `STATUS_COLORS` qua alias create~draft/submit_review~review/approve~approved/publish~published/retire~retired — không bịa màu mới), 4 info card (Actor/Thời gian/Kênh/Đối tượng liên quan kèm nút "Xem chi tiết" điều hướng đúng route entity nguồn nếu có — BusinessIntent/ProductIntent quy đổi code→id, Pattern/Template/Config/Variant dùng thẳng code; AnswerSlot/ConstraintMatrix không có detail nên không hiện nút), và khối "Hoạt động liên quan của đối tượng này" tái dùng THẲNG component `ApprovalHistory` có sẵn (Giai đoạn 42) — chỉ thêm 1 prop `title` tùy chọn (mặc định "Lịch sử duyệt") để đổi nhãn cho phù hợp ngữ cảnh, không tạo API/component mới. Route `/activity/:id` đặt trước `/:view`. Verify: curl `GET /api/activity-logs/{id}/detail` đúng dữ liệu + 404 khi id không tồn tại; Playwright bấm dòng đầu list → detail đúng badge/info/timeline liên quan, bấm "Xem chi tiết" nhảy đúng sang `/variant/VAR-108`; test 2 case không có detail (AnswerSlot, ConstraintMatrix) xác nhận không hiện nút "Xem chi tiết" và không crash. Trước đó **Giai đoạn 44 — "Lịch sử duyệt" cho Business Intent & Product Intent + filter Hành động ở Nhật ký hoạt động**: user yêu cầu 2 việc liên tiếp. (1) Mở rộng khối "Lịch sử duyệt" (Giai đoạn 42) lên 2 tầng đầu Pipeline chưa có — Business Intent và Product Intent — cùng cách làm với Giai đoạn 43: audit 13 entity (BI×7/PI×6), 8/13 thiếu 1-4 bước activity_log so với status thật, thêm 32 dòng activity_log mới (tổng 133, từ 101) + viết lại UPDATE `created_user`/`updated_user` cho cả 2 bảng (hết NULL). Thêm `<ApprovalHistory entityType="BusinessIntent"/>` và `entityType="ProductIntent"` vào cuối `BusinessIntentDetailPage.tsx`/`ProductIntentDetailPage.tsx` — tái dùng nguyên component có sẵn, không API mới. (2) Filter "Loại" ở màn Nhật ký hoạt động (`ActivityPage.tsx`, `filters={['Actor','Loại','Kênh']}`) hoá ra **không khớp cột nào** — `ListScreen.guessColumnIndex()` so khớp label filter với label cột thật (Thời gian/Actor/Hành động/Đối tượng/Kênh), "Loại" không match cột nào nên dropdown luôn rỗng ("Không có giá trị để lọc") — filter chết từ trước tới giờ. Đổi thành `'Hành động'` khớp đúng cột "HÀNH ĐỘNG" có sẵn, tự động ra đủ 8 giá trị thật (Tạo/Gửi duyệt/Phê duyệt/Xuất bản/Thu hồi/Gán/Đồng bộ/Cập nhật) — không cần sửa gì khác vì `ListScreen` tự suy filter option từ dữ liệu cột. Verify: SQL đối chiếu status↔activity_log muộn nhất cho 13 entity khớp 100%; Playwright xác nhận BI-01/PI-005 hiện đủ "Lịch sử duyệt", filter "Hành động" ra đúng 8 option lọc được. Trước đó **Giai đoạn 43 — vá lỗ hổng "Lịch sử duyệt" trống trơn trên toàn Pipeline (Pattern/Template/Config/Variant)**: user phát hiện qua UI Template `TPL-001` ("Vay cầm cố trả góp · KH cá nhân") status "Đã xuất bản" nhưng khối "Lịch sử duyệt" (thêm ở Giai đoạn 42) trống trơn, hỏi đây là lỗi data hay lỗi code. Audit toàn diện xác nhận **lỗi data, không phải lỗi code** — component `ApprovalHistory.tsx`/endpoint `GET /api/activity-logs/entity` hoạt động đúng, chỉ là DB thật sự không có dòng `activity_log` nào cho các entity đó (0-1 dòng lẻ tẻ thay vì đủ chuỗi tạo→gửi duyệt→phê duyệt→xuất bản). Rà soát **toàn bộ 28 entity Pattern(6)+Template(6)+Config(8)+Variant(8)** (không chỉ TPL-001) bằng cách đối chiếu status thật ↔ activity_log ↔ version_entry, phát hiện: (1) 22/28 entity thiếu 1-4 bước activity_log so với status thật; (2) 1 bug status khác cùng loại: `product_template` TPL-003 có `status='review'` nhưng `version_entry` head thật (v1.2, is_head=true) đã ở `published` từ 2026-06-30 — và cả 2 Config đóng gói từ nó (CFG-0042/0041) đều đã published — Template nguồn không thể đứng sau Config trong lifecycle (bug giống hệt Giai đoạn 40 nhưng ở cấp Template↔version_entry thay vì Config↔Variant); đã sửa `status` → `published`. Thêm 61 dòng `activity_log` mới (tổng 101, từ 40) khớp đúng chuỗi create→submit_review→approve→publish/retire theo ĐÚNG status hiện có của từng entity, tái dùng actor/mốc thời gian từ `version_entry` khi entity đó đã có sẵn (PT-001/003/005/006, CFG-0021/037/039/040/041, TPL-003) để 2 nguồn dữ liệu khớp nhau; viết lại toàn bộ khối UPDATE `created_user`/`updated_user` (Giai đoạn 42) cho khớp chuỗi mới — không còn dòng nào NULL trong 4 loại entity này. Verify bằng SQL đối chiếu `status` thật ↔ hành động activity_log MUỘN NHẤT cho cả 28 entity — khớp 100%; Playwright xác nhận `TPL-001`/`TPL-003` hiện đủ 4 bước đúng actor/thời gian, không còn "Lịch sử duyệt" trống ở đâu trong Pipeline. Ngoài phạm vi (không sửa vì không có UI nào đọc): Business Intent/Product Intent vẫn thiếu activity_log ở vài dòng (không có khối "Lịch sử duyệt" trên 2 màn này) và 5/6 Template (trừ TPL-003) vẫn chưa có `version_entry` riêng (nút "Phiên bản" sẽ trống cho TPL-001/002/004/005/006) — đã báo cho user, chưa sửa vì không phải bug được báo cáo trong đợt này. Trước đó **Giai đoạn 42 — Nhật ký hoạt động theo audit thật + Menu theo Role người dùng**: user hỏi "ai duyệt, ai làm gì cho từng sản phẩm" rồi muốn xử lý nhật ký hoạt động theo cột audit (`created_user`/`updated_user`, thêm ở Giai đoạn 40 nhưng seed chưa từng điền — toàn NULL), đồng ý thêm bảng user thật nếu cần; kế đó bổ sung yêu cầu menu sidebar phải lọc theo role người dùng, có 1 role Admin thấy hết. Đã EnterPlanMode vì phạm vi lớn (DB + backend + frontend). Quyết định kiến trúc quan trọng: bộ chọn "đổi vai trò" là **demo kiểu chuyển đổi user** (dropdown, không mật khẩu/session/JWT), lọc menu THUẦN phía frontend — không phải hệ thống đăng nhập/bảo mật thật (đã nói rõ với user). `activity_log.actor`/`version_entry.author` giữ nguyên `varchar` (snapshot tên, không FK hoá — audit-trail phổ biến, tránh viết lại ~70 dòng có sẵn). Backend: enum mới `user_role_enum` (Product Owner/Product Designer/Checker · Approver/Operations/Admin, tách khỏi `release_role_enum` cũ) + bảng `app_user` (6 dòng — 5 tên đã dùng thật trong activity_log/version_entry, chỉ 1 "Quản trị viên" là mới) + `AppUserController` (`GET /api/users`, thuần đọc). Populate `created_user`/`updated_user` cho 6 loại entity (business_intent/product_intent/pattern/template/config/variant) suy **trực tiếp** từ activity_log thật đã có (actor của hành động `create` → created_user, actor hành động muộn nhất → updated_user) — chỉ update những dòng có ít nhất 1 activity_log thật, không suy đoán cho dòng chưa từng xuất hiện (kể cả `product_catalog` — dù nằm trong "6 entity chính" theo dự tính ban đầu, audit phát hiện KHÔNG có activity_log nào nhắc tới catalog nên bỏ qua, giữ NULL đúng thật). Tổng quát hoá `ActivityLogService.forEntity()` (đã có sẵn từ Giai đoạn 36 nhưng chỉ dùng nội bộ cho Variant) thành endpoint `GET /api/activity-logs/entity?type=&code=` dùng chung. Frontend: `UserContext.tsx` (React Context, fetch `/api/users`, lưu lựa chọn `localStorage`, mặc định lần đầu = Admin để không ai bị ẩn menu khi mới vào); `nav.ts` thêm field `roles?: UserRole[]` mỗi item (undefined = mọi role thấy, dùng cho dashboard/activity); `Layout.tsx` thay khối user hardcode ("PD"/"Phạm Designer"/"Product Owner" — phát hiện qua audit là literal string vô nghĩa, không lấy từ đâu cả) bằng dropdown thật chọn `app_user`, lọc `NAV` theo `currentUser.role` (Admin bỏ qua lọc). Khối "Lịch sử duyệt" mới (`ApprovalHistory.tsx`, tái dùng đúng style timeline chấm tròn của "Hoạt động gần đây" ở Variant detail) thêm vào Config/Pattern/Template detail — gọi endpoint chung, không API riêng từng trang. Verify: curl `/api/users` đủ 6 user đúng role, `/api/activity-logs/entity?type=ProductConfig&code=CFG-0043` đúng 4 bước; Playwright xác nhận Admin thấy đủ 20 mục, Product Owner chỉ còn 4 (Business Intent/Product Intent/Dashboard/Activity), Checker/Approver đúng 7 mục, role chọn giữ nguyên sau F5 (localStorage), khối Lịch sử duyệt ở CFG-0043 hiện đúng Phạm An tạo → Trần Lan gửi duyệt → Lê Minh phê duyệt → Hệ thống xuất bản. Trước đó **Giai đoạn 41 — sample data "Vay xe máy mùa tựu trường"**: user yêu cầu 1 sản phẩm vay hoàn chỉnh đầy đủ các bước, đã duyệt qua kiểm duyệt. Tái dùng khuôn `PT-001`/`TPL-001` published có sẵn (không tạo trùng BI/PI/Pattern mới) — thêm `CFG-0043`(17 fragment, completeness 100%, 2 fragment ưu đãi thời vụ) + `VAR-108` (published, niêm yết App+Web) + `version_entry` CFG-0043 đủ 5 bước + 8 dòng `activity_log` (create/submit_review/approve/publish cho cả 2). Trước đó **Giai đoạn 40 — sửa lệch lifecycle Config↔Variant + thêm cột audit/CDC vào 43 bảng**: user phát hiện qua UI Config "chờ duyệt" nhưng Variant đóng gói từ nó lại "đã xuất bản" — verify bằng SQL join thật xác nhận là lỗi seed data (không phải hiển thị), sửa 3 Config (CFG-0042/0041/0038) nâng status khớp Variant nguồn, kèm version_entry+activity_log đầy đủ cho từng bước (không đổi trơ 1 cột). Cùng đợt: theo file `Product_Factory.sql` mới user cung cấp, thêm 9 cột audit (created_user/updated_user/deleted_user/is_deleted/cdc_version/cdc_timestamp...) cho TẤT CẢ 43 bảng + 4 cột governance (data_owner/data_steward/sensitivity/retention_policy) cho 8 bảng nghiệp vụ chính — đã tự sửa lỗi double-encode UTF-8 trong file gốc trước khi áp dụng (không copy nguyên bản mojibake). Trước đó **Giai đoạn 39 — làm thật nút "Xem trước" ở builder Product Pattern**: nút này trước đây no-op (`title=READONLY`) như mọi nút CUD khác, nhưng user hỏi ý tưởng rồi chốt làm thật vì bản chất là hành động XEM (không phải CUD) — giống tiền lệ nút "Phiên bản" đã làm thật trước đó. Đã hỏi user 2 quyết định UI qua AskUserQuestion: overlay toàn màn hình (không phải drawer) và KHÔNG thêm nút xuất PDF (tránh tạo nút no-op gây hiểu lầm). **Không cần API mới** — toàn bộ dữ liệu Preview cần (blocks/slots đầy đủ, assignedOTs, coverage) đã có sẵn trong state của builder từ `GET /api/product-patterns/{code}/detail` đang dùng, Preview chỉ là 1 cách trình bày khác (component `PatternPreviewModal.tsx` mới, thuần render prop, không fetch gì). Nội dung: header gradient (tên/mã/status/Product Intent nguồn), 3 stat card (Block/Slot/Obligation Type), khối Nghĩa vụ tài chính, khối Độ phủ ma trận (badge theo verdict đã tính sẵn ở `coverage.rows`), và cấu trúc đầy đủ theo thứ tự `position` — mỗi Block liệt kê toàn bộ Answer Slot (tên/kiểu/bắt buộc/mặc định). Verify: Playwright bấm "Xem trước" ở PT-002 ra đúng 9 Block/24 Slot/2 Obligation Type, đúng badge độ phủ, đúng cấu trúc. Trước đó **Giai đoạn 38 — hoàn thiện thanh tìm kiếm toàn cục ở topbar**: user yêu cầu "trang nào có thanh tìm kiếm thì hoàn thiện nó". Rà soát phát hiện 2 loại search khác nhau: (1) search trong từng màn list (`ListScreen`) — **đã thật từ Giai đoạn 20**, chạy client-side, không cần sửa; (2) thanh search ở **topbar** (`Layout.tsx`, input "Tìm mã, sản phẩm, obligation…" + chip ⌘K) — **hoàn toàn trang trí, không có `onChange`/logic gì**, tồn tại y nguyên từ lúc trích prototype tới giờ. Đây mới là phần cần hoàn thiện. Backend (mới hoàn toàn): `GlobalSearchService` (package `application.service.search`) quét trực tiếp 12 repo lõi đã có sẵn (BusinessIntent/ProductIntent/ProductPattern/ProductTemplate/ProductConfig/ProductVariant/ObligationType/FinancialObligationArchetype/Block/Attribute/Domain/Lifecycle) — so khớp substring không phân biệt hoa thường trên tên+mã, tối đa 5 kết quả/loại (tránh 1 loại át hết), trả `{type,code,name,status,path}` với `path` trỏ thẳng route chi tiết đã có (Obligation Type chưa có detail riêng → trỏ qua Archetype cha, đúng thật vì `archetype_code` NOT NULL). `GlobalSearchController`: `GET /api/search?q=`. Frontend: `GlobalSearch.tsx` (component mới) thay thế input tĩnh — debounce 250ms, dropdown kết quả có icon/type/mã/StatusChip, bấm ra điều hướng đúng route, phím `⌘K`/`Ctrl+K` focus ô search, `Escape` đóng, click-outside đóng. Verify: gõ "xe máy" ra 6 kết quả đúng thật đa loại (Business Intent BI-02, Product Intent PI-005, 2 Config, 2 Variant); bấm kết quả điều hướng đúng sang `/businessintent/2` hiện đúng dữ liệu. Lưu ý kỹ thuật: test qua `curl` trong Git Bash Windows bị lỗi encoding tiếng Việt (không phải bug — xác nhận bằng percent-encode thủ công ra đúng kết quả), nên verify cuối cùng bằng Playwright gõ trực tiếp (UTF-8 đúng). Trước đó **Giai đoạn 37 — chia detail Product Variant thành 3 tab con**: user phân vân giữa Catalog/Variant nên xem chi tiết ở đâu cho nhu cầu "toàn bộ dữ liệu liên quan — FOA, block, attribute, giá trị". Đề xuất (user đồng ý): giữ Catalog→Release (đúng ý nghĩa "tiến độ phát hành", đã chốt Giai đoạn 32), còn nhu cầu "xem toàn bộ" thì đào sâu ngay ở `ProductVariantDetailPage` (Giai đoạn 36) — chia 3 tab thay vì 1 trang cuộn dài: **Tổng quan** (nội dung Giai đoạn 36 giữ nguyên), **Giá trị cấu hình** (tái dùng THẲNG `GET /api/product-configs/{code}/detail` đã có sẵn từ Giai đoạn 21 — completeness bar + từng block/slot kèm giá trị fragment đã resolve theo scope, badge cảnh báo vàng cho `is_warning`, không viết thêm backend), **Nghĩa vụ tài chính** (tái dùng THẲNG `GET /api/product-patterns/{code}/detail` đã có sẵn từ Giai đoạn 13 — danh sách Obligation Type gán cho Pattern nguồn kèm archetype + vai trò Chính/Phụ). **Không thêm backend nào mới** — cả 2 tab đều gọi API sẵn có theo `configCode`/`patternCode` lấy từ lineage đã build ở Giai đoạn 36, load lười (lazy) khi bấm tab. Sửa 1 lỗi nhỏ lúc verify: field `role` thật trong DB là `Primary`/`Support` (viết hoa, không phải `primary`/`secondary` như đoán ban đầu) — sửa `ROLE_LABEL` map khớp đúng giá trị thật. Verify: Playwright cả 3 tab với VAR-101 xác nhận đúng dữ liệu thật (Giá trị cấu hình: 12/14 slot bắt buộc, Bên tham gia/Hạn mức/Lãi suất... đúng giá trị theo block; Nghĩa vụ tài chính: 2 Obligation Type đúng archetype/vai trò). Trước đó **Giai đoạn 36 — detail "thật chi tiết và đẹp và mượt và đầy đủ thông tin" cho Product Variant**: user yêu cầu rõ ràng mức độ đầu tư cao hơn hẳn Giai đoạn 34-35. Backend (khác Block — cần code mới vì chưa có sẵn): `ProductVariantService.detail(code)` join **toàn bộ lineage ngược Pattern←Template←Config←Variant** (dùng `ProductTemplateRepository`/`ProductPatternRepository` mới inject) + **niêm yết Catalog** (`CatalogListingRepository.findByVariantCode` có sẵn, join `ProductCatalog` lấy tên/kênh/ngày niêm yết/trạng thái) + **nhật ký hoạt động riêng của variant đó** (mới `ActivityLogRepository.findByEntityTypeAndEntityCodeOrderByOccurredAtDesc` + `ActivityLogService.forEntity()` tách từ `list()` để tái dùng logic dịch nhãn/suy kênh có sẵn, không viết trùng). `ProductVariantController` thêm `GET /api/product-variants/{code}/detail`. Frontend: chuyển `ProductVariantPage.tsx` vào `pages/variant/`; `ProductVariantDetailPage.tsx` (mới, giàu nhất từ trước đến nay) gồm: banner + chip family/status, khối "Nguồn gốc sản phẩm" (4 ô Pattern→Template→Config→Variant nối mũi tên, click 3 ô đầu điều hướng sang trang chi tiết tương ứng đã có sẵn, ô Variant hiện tại highlight viền xanh), khối "Thông tin sản phẩm" (hạn mức/lãi suất/nội dung marketing nếu có), khối "Quy trình phát hành" (CTA điều hướng `/release/{code}` — TÁI DÙNG luôn màn Giai đoạn 32, không lặp logic 8 bước), 2 cột "Niêm yết Catalog" (card theo từng catalog, badge trạng thái, ngày niêm yết hoặc "Chưa có ngày niêm yết") và "Hoạt động gần đây" (timeline chấm tròn, actor/hành động/kênh/thời gian). `fadeUp` stagger toàn bộ card con. Route mới `/variant/:code`. Verify: curl `VAR-101` (published, đủ 3 catalog, 1 activity) và `VAR-107` (draft, 2 catalog chưa niêm yết ngày, 1 activity "Tạo") + 404 case; Playwright cả 2 trạng thái xác nhận giao diện đẹp, mượt, đầy đủ, không bịa gì. Trước đó **Giai đoạn 35 — thêm detail cho Block & Answer Slot**: user hỏi tiếp sau Giai đoạn 34, chỉ ra màn Block & Answer Slot cũng chưa có detail. Điểm khác biệt: backend **đã có sẵn từ Giai đoạn 6** — `BlockService.detail(id)`/`BlockController GET /api/blocks/{id}/detail` được xây trước (dự phòng cho việc gỡ fix cứng builder Pattern) đã trả đúng `{block, slots:[{code,name,type,required,def,rule,attrCode,attrName}]}` join answer_slot+attribute+data_type — **không cần sửa backend gì cả**, chỉ thiếu frontend. Frontend: chuyển `BlockPage.tsx` vào subfolder riêng (`pages/block/`) theo convention Giai đoạn 31; thêm `BlockDetailPage.tsx` (banner icon "block" + chip nhóm nghiệp vụ + StatusChip, stat card số Answer Slot + "chi phối bởi", danh sách Answer Slot dạng card có badge type/required + mặc định/ràng buộc, `fadeUp` stagger + hover elevate — cùng style với `LifecycleDetailPage`/`DomainDetailPage` Giai đoạn 34). Route mới `/block/:id`. Verify: curl `GET /api/blocks/BLK_ELIGIBILITY/detail` (3 slot: age/min_income/occupation đúng type Range/Money/Enum) + Playwright xác nhận UI đẹp, đúng dữ liệu. Trước đó **Giai đoạn 34 — thêm detail cho Lifecycle & State và Domain (UI mới ngoài prototype, user yêu cầu "giao diện phải mượt mà và đẹp mắt")**: sau Giai đoạn 29 đã xác nhận prototype gốc KHÔNG có detail cho 2 màn này (chỉ list), nhưng user hỏi ý tưởng rồi chốt làm cả 2. Backend: `LifecycleStateRepository.findByLifecycleCodeOrderBySortOrder` + `LifecycleService.detail(code)` (entity + states đúng thứ tự `sort_order`) + `LifecycleController` thêm `GET /api/lifecycles/{code}/detail`; `AttributeGroupRepository.findByDomainCodeOrderByName` + `DomainService` mới (join Attribute Group theo `domain_code` + đếm attribute từng group qua `AttributeRepository.countByGroupCode` có sẵn) + `DomainController` thêm `GET /api/domains/{code}/detail` (giữ nguyên list/byId read-only thuần cũ). Frontend: chuyển `LifecyclePage.tsx`/`DomainPage.tsx` vào subfolder riêng (`pages/lifecycle/`, `pages/domain/`) theo đúng convention Giai đoạn 31; thêm `LifecycleDetailPage.tsx` (chuỗi state machine dạng vòng tròn nối mũi tên, tô gradient xanh nhạt→đậm theo thứ tự — chỉ để phân biệt trực quan, KHÔNG mang nghĩa done/undone vì đây là định nghĩa state machine chứ không phải tiến độ 1 thực thể) và `DomainDetailPage.tsx` (stat card + danh sách Attribute Group dạng progress-bar theo số attribute); cả 2 dùng chung style banner gradient/back-button/StatusChip đã có ở `ArchetypeDetailPage`/`ReleasePage`, thêm hiệu ứng `fadeUp` stagger theo index + hover scale/elevate cho mượt. Route mới `/lifecycle/:code`, `/domain/:code` đặt trước `/:view`. Verify: curl `GET /api/lifecycles/{code}/detail` (7 state Term Loan: Draft→...→Closed) và `GET /api/domains/{code}/detail` (Pricing: 2 group/5 attribute) đúng thật + 404 khi mã không tồn tại; Playwright xác nhận UI mượt, đúng dữ liệu. Trước đó **Giai đoạn 33 — bổ sung seed `activity_log` cho đủ dày**: user hỏi màn Nhật ký hoạt động có đang fix cứng không. Audit kỹ (frontend/backend/DB) xác nhận **không có gì fix cứng** — mọi field đều lấy từ DB thật (channel suy ra bằng regex trên cột `detail` thật, nhãn hành động dịch từ code thật có fallback, COUNT thật không còn số bịa "1.284" từ Giai đoạn 18). Vấn đề thật sự chỉ là **quy mô**: seed gốc chỉ có 8 dòng. User chọn bổ sung thêm — đã thêm 20 dòng `activity_log` mới (tổng 28), mỗi dòng là sự kiện tạo/gửi duyệt/phê duyệt/xuất bản/thu hồi khớp ĐÚNG trạng thái thật hiện có của entity đó (business_intent/product_intent/product_pattern/product_template/product_config/product_variant) — không bịa entity hay trạng thái mới, không đổi code (chỉ sửa `V2__seed.sql`). Cần `docker compose down -v && up --build` để Flyway chạy lại seed mới. Verify: `GET /api/activity-logs` trả `totalElements:28`, Playwright xác nhận list hiển thị đa dạng actor/hành động/đối tượng/kênh, đều là dữ liệu thật. Trước đó **Giai đoạn 32 — liên kết Catalog ↔ Quy trình phát hành theo trạng thái sản phẩm**: user yêu cầu bấm 1 sản phẩm trong Catalog phải chuyển sang màn Quy trình phát hành và hiển thị đúng tiến độ 8 bước khớp trạng thái sản phẩm đó (draft/review/approved/published); mặc định vào từ sidebar phải hiển thị done 8 bước. Phát hiện bug thật: DB chỉ có 1 dòng `maker_checker_process` (cho VAR-101) với `done_count=4` — lệch với trạng thái thật của VAR-101 (`published`, đáng lẽ done 8/8). Quyết định (đã hỏi user): **không** seed thêm dòng `maker_checker_process` cho từng variant (tránh lặp lại đúng lỗi lệch dữ liệu) — thay vào đó dùng đúng 1 dòng hiện có làm **process template** (8 bước title/role/input/output/checklist chuẩn, không đổi theo sản phẩm) và **tính runtime** done/current/upcoming từ `product_variant.status` thật (draft→5/8, review→6/8, approved→7/8, published/retired→8/8; bước 7="Phê duyệt", bước 8="Đóng gói & Phát hành"). Backend: `ReleaseProcessService.detailByVariant(variantCode)` thay `detail(id)`, `MakerCheckerProcessRepository.findFirstByOrderById()`, controller đổi `/{id}/detail`→`/{variantCode}/detail`, xóa `list()` không dùng nữa. Frontend: `ProductCatalogPage` card thêm `onClick` điều hướng `/release/{variantCode}`; route mới `/release/:variantCode`; `ReleasePage` bỏ bước lấy danh sách rồi lấy dòng đầu, gọi thẳng theo variantCode (mặc định `VAR-101` khi vào từ sidebar). Verify bằng curl 4 trạng thái + Playwright click từ Catalog. Trước đó **Giai đoạn 30 — popup xem nhanh cho tab Attribute Group/Data Type**: sau Giai đoạn 29, user hỏi tiếp về 2 tab còn lại trong màn Attribute. Đối chiếu prototype gốc phát hiện cả 2 tab ĐỀU có `onClick` (`openCreate('attribute')`) — nhưng đó là mở modal CUD "Tạo Attribute mới" dùng chung với nút góc phải (không truyền id riêng theo dòng), và modal CUD này chưa từng được cài đặt cho bất kỳ màn nào trong dự án (đúng luật "Nút CUD: no-op"). Hỏi lại user: muốn dựng modal tạo/sửa thật hay chỉ xem thông tin — user chọn **chỉ xem, không có tác động gì**. Kết quả: thêm `InfoModal` (component popup dùng chung, không phải CUD) trong `AttributePage.tsx` — bấm dòng Attribute Group hoặc Data Type mở popup hiển thị thông tin nhóm/kiểu dữ liệu + danh sách attribute liên quan (lọc từ chính danh sách Attribute đã fetch sẵn, KHÔNG gọi thêm API mới, không bịa). Đây là UI mới không có trong prototype gốc (bản gốc chỉ có modal CUD) nhưng đúng yêu cầu user, hoàn toàn dữ liệu thật. Trước đó **Giai đoạn 29 — hoàn thiện click-through list→detail + màn Attribute Usage**: user yêu cầu rà mọi màn list xem bấm vào có sang detail không, so với prototype gốc để biết thiếu gì. Khảo sát (đọc state `view`/`isList`/các setter mang id như `openArchDetail`/`openAttrUsage` trong `docs/Product Factory 5.1.html`) xác nhận: prototype gốc CHỈ có đúng 2 khu vực có detail theo bản ghi — `archetype→archetypeDetail` (đã xong từ trước) và `attribute→attrUsage` (lineage, còn thiếu — chính là nợ 5.4 cũ). `pattern`/`template` có "builder" và `config` có "configForm" nhưng dùng chung 1 view cho mọi dòng (không truyền id qua state) nên KHÔNG tính là detail thật; 13 nav còn lại (variant/catalog/obligation/block/matrix/lifecycle/domain/activity/dashboard/ontology/sysmap/release/simulation) không có khái niệm detail trong bản gốc — giữ nguyên list-only, không tự vẽ thêm (đúng tinh thần pixel-perfect, không bịa). User chọn scope: chỉ làm Attribute Usage. Đã bổ sung backend (`AttributeEnumValue` entity mới, `TemplateFrameRepository`/`FragmentRepository` thêm method `findByBlockIdAndSlotCode`, `ProductVariantRepository.findByFromConfigCodeIn`, `AttributeUsageService`+`AttributeUsageController` mới — `GET /api/attributes/{code}/usage`) và frontend (`AttributeUsageDetailPage.tsx` trích đúng markup `attrUsageModel()`/`ATTR_USAGE()` từ prototype — rail chọn attribute, 5-stage lineage, constraint, Selector Scope values, where-used; route `/attribute/:code`; `AttributePage.tsx` tab Attribute thêm `onRowClick`). Verify bằng curl thật (`base_rate`: 2 constraint, 6 template, 7 config đa scope có 1 case warning, 7 variant; `occupation`: enum 4 giá trị, 0 fragment/variant — trả mảng rỗng gọn, không lỗi) + Playwright screenshot so khớp bố cục prototype. Trước đó (Giai đoạn 22) đã audit toàn diện sample data theo FK thật phát hiện 5 lỗ hổng dữ liệu; trước nữa (Giai đoạn 21) viết lại Product Config theo đúng markup builder gốc sau khi user đối chiếu phát hiện bản rút gọn sai — bài học: **luôn trích đúng markup gốc, không tự rút gọn, và khi so sánh list-vs-detail với prototype phải đọc đúng state/setter mang id, đừng suy diễn qua tên view.** Chỉ còn: **5.3 loading/error states nhất quán + Docker hoàn thiện** (nợ polish cuối, chưa có yêu cầu mới nào khác).

## NỢ — LÀM CUỐI, KHÔNG XEN GIỮA (← ĐANG Ở ĐÂY)
5.3: loading/error states nhất quán; Docker hoàn thiện.

## ĐÃ XONG
dashboard, businessintent(list + detail/KPI, route `/businessintent/:id`), intent(list+detail), pattern(builder, đã WIRE về DB thật + **"Xem trước" thật, Giai đoạn 39** — overlay toàn màn hình `PatternPreviewModal.tsx`, không API mới), block(list + **detail route `/block/:id`, Giai đoạn 35** — Answer Slot thuộc block, backend `BlockService.detail` có sẵn từ Giai đoạn 6), matrix(4-tab + backend `governance`), attribute(list 3-tab + backend `Domain`/`AttributeGroup`/`AttributeConstraint` + **detail "Attribute Usage" route `/attribute/:code`, Giai đoạn 29** — lineage Answer Slot→Template→Config→Variant, backend `AttributeUsageService`/`AttributeEnumValue` + **popup xem nhanh tab Group/Data Type, Giai đoạn 30** — `InfoModal` client-side, không API mới), obligation(list 3-tab, join làm giàu ontology có sẵn), archetype(card grid + detail, route `/archetype/:code`), domain(list + **detail route `/domain/:code`, Giai đoạn 34** — Attribute Group thuộc domain kèm progress bar, backend `DomainService`), lifecycle(list, join stateCount + **detail route `/lifecycle/:code`, Giai đoạn 34** — chuỗi state machine theo `sort_order`, backend `LifecycleService.detail`), ontology(ER-chain + decomposition + vocab, không backend mới — tổng hợp client-side từ API đã có), sysmap(pipeline + foundations + bảng quan hệ, không backend mới), template(list + detail `/template/:code`, backend `pipeline.ProductTemplate`/`CustomerSegment`/`TemplateSegment`/`TemplateFrame`), config(builder Fragment+Resolution pixel-perfect `/config/:code`, backend `pipeline.ProductConfig`/`SelectorScope`/`Fragment` — VIẾT LẠI Giai đoạn 21), variant(list + **detail route `/variant/:code`, Giai đoạn 36** — lineage Pattern→Template→Config→Variant, niêm yết Catalog, hoạt động gần đây, CTA sang Quy trình phát hành, backend `pipeline.ProductVariant`/`ProductCatalog`/`CatalogListing` + `ProductVariantService.detail`), catalog(card grid, backend `pipeline.ProductCatalogController`), release(stepper 8 bước + swimlane, route `/release/:variantCode` — Giai đoạn 32, backend `release.MakerCheckerProcess`/`ProcessStep`/`ProcessStepChecklist` dùng làm template, tiến độ tính runtime từ `product_variant.status`), activity(list + **detail route `/activity/:id`, Giai đoạn 45** — banner theo hành động, liên kết sang entity nguồn, khối hoạt động liên quan tái dùng `ApprovalHistory`, backend `activity.ActivityLog` + `ActivityLogService.detail`), simulation(form + annuity engine thật, backend `simulation.SimulationEngine`, `POST /api/simulation/run` — nút DUY NHẤT gọi tính toán thật, không no-op). **`ListScreen` (dùng chung mọi màn list) nay có search + filter dropdown THẬT chạy client-side (`components/ListScreen.tsx`, không cần sửa page nào khác).** **Thanh tìm kiếm topbar (`components/GlobalSearch.tsx`, Giai đoạn 38) nay THẬT — quét 12 loại thực thể qua `GET /api/search`, không còn là input trang trí.** **Menu sidebar nay lọc theo role người dùng thật (Giai đoạn 42)** — bảng `app_user` + `UserContext.tsx` + dropdown "đổi vai trò" (demo, không đăng nhập thật) thay khối user hardcode cũ; **khối "Lịch sử duyệt" (`components/ApprovalHistory.tsx`)** ở Business Intent/Product Intent/Config/Pattern/Template detail (Giai đoạn 42-44) hiển thị đúng ai tạo/gửi duyệt/phê duyệt/xuất bản từ `activity_log` thật qua `GET /api/activity-logs/entity`, tái dùng lại (đổi nhãn qua prop `title`) làm khối "Hoạt động liên quan" ở detail Nhật ký hoạt động (Giai đoạn 45). TẤT CẢ 18 MÀN + Simulation Engine + Attribute Usage ĐÃ HOÀN TẤT. Chỉ còn 5.3 (loading/error, Docker).
