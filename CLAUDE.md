# CLAUDE.md — Product Factory 5.1 (bàn giao cho Claude Code)

> File này Claude Code đọc mỗi phiên. Nó KHÔNG thay thế `PROJECT_STATUS.md` và `NEXT_WORK.md` — hai file đó là **nguồn sự thật đầy đủ**. Luôn đọc cả ba trước khi code.

## QUY TẮC VÀNG
- **Code là chuẩn.** MD lệch với code thật → tin code. Trước khi làm gì: quét code trong `product-factory/`.
- **Không hỏi lại những gì MD đã trả lời.** Không đoán tên cột — lấy từ `Product_Factory.sql` (DDL) hoặc canonical JSON.
- **Không tự bịa dữ liệu.** Data hiển thị = data thật từ DB qua API — kể cả Dashboard KPI tổng hợp (Giai đoạn 26: viết lại 100%, không còn ngoại lệ).

## VỊ TRÍ THƯ MỤC (quan trọng)
Gốc dự án nằm ở **`product-factory/`** (thư mục lồng), KHÔNG ở gốc repo:
- `product-factory/backend/`   → Java 21 + Spring Boot 3.3.4 + Maven, **Clean Architecture layer-first** (Giai đoạn 27): package `com.f88.productfactory.{domain.model.<feature>|domain.repository.<feature>|application.service.<feature>|application.common|application.dto.<feature>|infrastructure.config|presentation.controller.<feature>|presentation.common}`. Entity → `domain.model`, Spring Data repo → `domain.repository`, business/join/enrichment logic → `application.service` (Service class), REST endpoint → `presentation.controller` (Controller THIN, chỉ gọi Service). Tài nguyên đọc-only thuần dùng chung `application.common.ReadOnlyService<T,ID>` + `presentation.common.ReadOnlyController<T,ID>`.
- `product-factory/frontend/`  → React 18 + Vite + TS, cấu trúc theo layer (Giai đoạn 28): `src/infrastructure/` (api client, `icons.ts`, `nav.ts`, `tables.ts` — kết nối hạ tầng/config) + `src/presentation/{components,pages}/` (toàn bộ UI). `main.tsx` ở gốc `src/` (entry + router). **`presentation/pages/` (Giai đoạn 31, +34, +35, +36):** 11 cặp list+detail liên quan gộp vào subfolder cùng tên nav key — `businessintent/`, `intent/`, `pattern/`, `template/`, `config/`, `attribute/`, `archetype/`, `lifecycle/`, `domain/`, `block/`, `variant/` (mỗi folder 2 file: `XxxPage.tsx` + `XxxDetailPage.tsx`); 9 page chỉ 1 file (dashboard, matrix, obligation, ontology, sysmap, catalog, release, activity, simulation) giữ phẳng ngay trong `pages/` — không tạo subfolder cho page đơn lẻ.
- `product-factory/docker-compose.yml`
Mọi lệnh build phải `cd product-factory/...` cho đúng cấp (lỗi hay gặp: "no configuration file provided" do đứng sai thư mục).

## FILE NGUỒN (bắt buộc có trong repo — KHÔNG được đoán thay)
- **Prototype UI:** `Product_Factory_5_1.html` — trích markup pixel-perfect cho MỌI màn mới. Nếu không thấy file này trong repo → DỪNG, báo user bổ sung, KHÔNG tự chế markup.
- **DDL:** `backend/src/main/resources/db/migration/V1__schema.sql` (43 bảng) — nguồn tên cột chính xác.
- **Seed:** `backend/src/main/resources/db/migration/V2__seed.sql` — data thật để verify.
- **Canonical (nếu có):** `product_factory_canonical_v3_semantic.json` — cấu trúc logic 43 bảng.
> Cách trích markup từ prototype: mục 8.5 PROJECT_STATUS (tìm mốc `openBuilder`/`BLOCKS(){`/tên view, unescape thủ công, KHÔNG dùng `unicode_escape`).

## LUẬT CỨNG (mục 2 & 7 PROJECT_STATUS)
- **Read-only 90%.** Backend chỉ `GET`. Entity chỉ getter, `@Entity @Table` (nằm ở `domain.model.<feature>`). Read-only thuần → Controller (`presentation.controller.<feature>`) extends `presentation.common.ReadOnlyController<T,ID>`, override `service()` trả `new ReadOnlyService<>(repo)`; cần join làm giàu list → tách riêng `application.service.<feature>.XxxService` (chứa logic) + Controller thin gọi Service, trả `Page<Map<String,Object>>`. Composite PK → `@IdClass` + `Serializable` (đặt cùng `domain.model` với entity).
- **Enum PostgreSQL map bằng `@Column String`** (KHÔNG `@Enumerated`). Resource path kebab số nhiều: `/api/product-patterns`.
- **Frontend pixel-perfect từ prototype** (`Product_Factory_5_1.html`), inline style trích nguyên — không chế lại. Màn list dùng `ListScreen` (signature THẬT: `rows: ReactNode[][]`), `StatusChip`, `Icon` (35 icon, **không có** lock/history/grid). Bảng màu: xanh `#0E8C5A` / đậm `#0B7349` / sidebar `#0B3B2E→#082A20`; chữ `#122019`/`#5E6F66`; viền `#E6ECE8`; nền `#F4F7F5`. Font `Be Vietnam Pro`.
- **Màn builder/biểu đồ KHÔNG rút gọn thành detail thường** (bài học Pattern). Trích đúng markup builder.
- **nav key ≠ resource path**: intent→`/api/product-intents`, businessintent→`/api/business-intents`, pattern→`/api/product-patterns`. Route detail đặt TRƯỚC `/:view` trong `main.tsx`.
- Nút CUD: giữ giao diện, no-op, tooltip "read-only".

## QUY TRÌNH MỖI MÀN
code → `cd product-factory/frontend && npm install && npm run build` (0 lỗi TS) → render kiểm chứng so khớp prototype → mới coi là xong → cập nhật `PROJECT_STATUS.md` + `NEXT_WORK.md`.
**Khác với môi trường web trước:** máy local này **build được backend thật** (`mvn` / `docker compose up --build`) — không còn giới hạn "sandbox chặn Maven 403". Vẫn giữ kỷ luật verify. Không cần đóng zip mỗi lượt nữa — dùng **git commit** từng màn thay cho zip trọn vẹn.

## MÀN KẾ TIẾP
**TẤT CẢ 18 MÀN + Simulation Engine + Attribute Usage ĐÃ HOÀN TẤT.** Vừa xong **Giai đoạn 59 — sửa 3 lỗi phát hiện ngay sau Giai đoạn 58 + bổ sung dữ liệu FOA×OE theo tài liệu**: user hard-refresh sau Giai đoạn 58, phát hiện 2 lỗi UI: (1) badge sidebar "Ma trận ràng buộc" hiện "0" dù có 2 tab thật — do `Layout.tsx` vẫn gọi `fetch('/api/constraint-matrices')` (endpoint `list()` đã xoá ở Giai đoạn 58), lỗi 500 "No static resource" khiến `Array.isArray(arr)` false → fallback 0; (2) ma trận OE×Block trông "rỗng" (12 hàng nhưng phần lớn toàn "Không"). Sửa: xoá fetch chết trong `Layout.tsx`, `nav.ts` đổi `count: null` (dynamic) → `count: '2'` (cố định, vì không còn resource phân trang thật để đếm); `oeBlockMatrix()` đổi rows từ "toàn bộ 12 Block" xuống "chỉ 5 Block có `governed_by_element_code`" — 7 Block còn lại (BLK_LIMIT/INTEREST/FEE theo FOA, BLK_ELIGIBILITY/COUNTERPARTY/REGULATORY/PENALTY theo khái niệm tự do ở `governed_by_aspect`, không phải OE) luôn "na" ở mọi cột nên đưa vào chỉ gây loãng bảng, ma trận giờ 5×5 thay vì 12×5. Tiện thể xoá 2 card "Obligation Nature"/"Value Structure" ở Archetype detail theo yêu cầu riêng, dọn 4 field không dùng khỏi interface. Sau đó user hỏi thẳng "các dữ liệu trong phần matrix có đang hardcode không, sao tôi thấy có rất nhiều OE trong database mà lên matrix lại ít như thế" — xác nhận qua `docker exec pf-db psql` (query thẳng DB, bỏ qua app/cache) **KHÔNG hardcode**, cả 2 ma trận đều derived thật. Nhưng tìm ra 2 nguyên nhân khác nhau: (a) FOA×OE — đối chiếu tài liệu "Ví dụ xuyên suốt: Ma Trận FOA × OE..." xác nhận phần lớn 24/37 OE không lên ma trận là ĐÚNG THIẾT KẾ (6 mã Party — tài liệu ghi rõ "không áp dụng cho Party"; ~8 mã thuộc OT Giải ngân/Bàn giao TS — tài liệu ghi rõ các OT này "khá cố định... không cần ma trận phân xử"; còn lại thuộc OT phụ trợ ngoài phạm vi tài liệu), NHƯNG có đúng 2 mã bị thiếu thật (`OE_VAL_PRINCIPAL_SINGLE_DECREASE` "bullet", `OE_REC_UNSECURED` "Tín chấp") và 9 ô bị lệch giá trị BB/CP/KO so với tài liệu — phát hiện còn tồn đọng từ đợt audit tài liệu trước đó, user xác nhận "có" sửa ngay; (b) OE×Block — đọc lại nguyên văn tài liệu "Lớp vỏ thương mại" xác nhận đây CHỈ là văn bản khái niệm (giải thích Block là gì), KHÔNG hề có bảng ánh xạ đầy đủ 12 Block→OE nào — không có căn cứ tài liệu để bổ sung thêm quan hệ cho 7 Block còn lại, tự thêm sẽ là bịa dữ liệu nên KHÔNG sửa. Sửa `foa_element`: thêm 9 ô còn thiếu cho 5 OE đã có sẵn hàng (mỗi OE thật ra có nhiều mức yêu cầu theo từng FOA chứ không chỉ 1 cột) + thêm mới 2 OE hoàn toàn vắng mặt — tổng 15 dòng cũ + 14 dòng mới = 29 dòng, phủ 15 OE (từ 13). Verify: seed đổi nên `docker compose down -v && up --build`; curl `foa-oe-matrix` giờ 15 dòng/45 ô (req=12/pos=17/na=16 — mật độ "Được phép" tăng từ 4→17 ô, khớp sát tài liệu); curl `oe-block-matrix` đúng 5×5; regression `obligation-type-compositions` vẫn 246, `product-patterns` list 200 OK, `product-patterns/PT-001/detail` coverage vẫn đúng 4 dòng (không đổi vì dùng nguồn khác — `obligation_type_composition`, không phải `foa_element`); `npm run build` 0 lỗi TS. Trước đó **Giai đoạn 58 — màn Ma trận chỉ còn đúng 2 ma trận: FOA × Obligation Element và Obligation Element × Block**: user yêu cầu thẳng "ở màn ma trận giờ sẽ chỉ có OE x block và FOA x OE" — rút màn Ma trận (trước có 4 tab: FOA×OE/OET×OET/OTF×Block/Pattern×Block-độ-phủ) xuống còn đúng 2. Đã hỏi qua AskUserQuestion 2 quyết định: (1) số phận 2 ma trận cũ "OET × OET" và "Pattern × Block (độ phủ)" — user chọn "Bỏ hẳn cả 2, chỉ còn đúng 2 tab" (chấp nhận hệ quả: banner độ phủ ở Pattern builder phải đổi nguồn); (2) nguồn dữ liệu ma trận "OE × Block" mới — user chọn "Dựa vào `governed_by_element_code` có sẵn" (không tạo bảng/dữ liệu req/pos/na mới). Phân tích trước khi sửa xác nhận: "OET × OET" và "OTF × Block" là 2 ma trận DUY NHẤT còn lưu thật ở `constraint_matrix`/`matrix_cell`; banner "Đủ Block bắt buộc" ở Pattern builder (`ProductPatternService.detail()`) và tab "Pattern × Block (độ phủ)" (`ConstraintMatrixService.patternCoverage()`) là 2 chỗ độc lập cùng đọc ma trận OTF×Block — cả 2 cần đổi nguồn. Sửa: `ConstraintMatrixService` viết lại gọn còn đúng 2 method derived (`foaOeMatrix()` giữ nguyên + `oeBlockMatrix()` mới — rows=toàn bộ Block(12), cols=các OE thực sự chi phối ít nhất 1 Block(5 mã, suy từ `block.governed_by_element_code`), cell req/na nhị phân không có mức tuỳ chọn) — bỏ hẳn `list()`/`detail(id)`/`patternCoverage()`/`legendOf`/`rowHeadOf`/`rowLabel`/`colLabel` (dead code). `ConstraintMatrixController` bỏ 3 endpoint chết, thêm `GET /oe-block-matrix`. `ProductPatternService.detail()` field `coverage` viết lại hoàn toàn: gom mọi OE dùng trong composition (`ObligationTypeCompositionRepository.findByObligationTypeCode()`) của các OTF đã gán pattern, lọc Block nào được 1 trong các OE đó chi phối → verdict="req" (coverage giờ độ dài THAY ĐỔI theo từng pattern, không còn cố định 6 Block "cover" cũ) — bỏ `COVER_BLOCKS`/`matrixRepo`/`matrixCellRepo`/`rank`/`unrank`. `V2__seed.sql` xoá sạch seed `constraint_matrix`(id=2,3)/`matrix_cell` liên quan — giữ nguyên bảng trong schema (không DROP, có thể tái dùng sau), chỉ seed để trống. Tiện thể phát hiện và sửa tiếp `ReleaseProcessService.TIP[2]` (vừa sửa ở Giai đoạn 57 thành "Ma trận Obligation Type Family (OTF) × Block") sang "Ma trận Obligation Element × Block" — tránh lặp lại lỗi tham chiếu ma trận vừa bị xoá. Frontend: `MatrixPage.tsx` viết lại gọn — `TAB_LABELS` còn `['FOA × Obligation Element', 'Obligation Element × Block']`, bỏ cơ chế gọi list()+loop id cũ, gọi thẳng 2 endpoint cố định; `LegKind` thu gọn còn `'rpn'`. **Hệ quả đáng chú ý (đã báo trước cho user):** độ phủ Pattern builder đổi hẳn ý nghĩa vì chuyển từ ma trận OTF×Block (fabricated, curated tay — 6 Block cố định) sang OE→Block thật (5 Block thực sự có OE chi phối) — vd PT-001 giờ hiện "Thiếu 2 Block bắt buộc" (BLK_VALUEBASE/BLK_DISBURSEMENT) thay vì "✓ Đủ" như cũ, vì 2 Block này chưa từng được kiểm tra ở ma trận cũ. Verify: seed đổi nên `docker compose down -v && up --build`; backend start 0 lỗi; curl `oe-block-matrix` đúng 12 row/5 col, đúng 5 cặp req khớp 5 quan hệ governed-by thật từ Giai đoạn 53b; curl `product-patterns/PT-001/detail` → coverage đúng 4 Block (2 covered/2 missing), `PT-002/detail` → 5 Block (thêm BLK_BILLING); regression `obligation-type-compositions` vẫn 246 dòng, `product-patterns` list vẫn 200 OK; 3 endpoint cũ không còn route; `npm run build` 0 lỗi TS. Trước đó **Giai đoạn 57 — cập nhật checklist Bước 2 "Tạo Product Intent" ở Quy trình phát hành theo mô hình OTF mới**: user rà màn Quy trình phát hành (`/release/VAR-101`), phát hiện checklist Bước 2/8 vẫn hiện văn bản cũ từ trước Giai đoạn 51 ("Gán Obligation Nature định danh", "Khai báo Obligation Element nền", "Đối chiếu Ma trận FOA × Element") — nhắc khái niệm "Obligation Nature" đã bị xoá hẳn từ Giai đoạn 52 (gộp vào Archetype). User mô tả bằng văn nói 3 việc cần làm thật theo quy trình OTF hiện tại, yêu cầu viết lại cho chuẩn: điền OE cho 3 OT lõi cốt lõi, bật/tắt OT lõi phụ trợ, điền OE cho OT lõi phụ trợ đã bật để hoàn thành OTF. Sửa `V2__seed.sql` (`process_step_checklist`, process_id=1/step_no=2) thành "Điền OE cho 3 OT lõi Cốt lõi (Giải ngân / Hoàn trả gốc / Trả lãi)", "Bật/tắt OT lõi Phụ trợ theo bảng kích hoạt", "Điền OE cho OT lõi Phụ trợ đã bật để hoàn thành OTF" — khớp đúng thuật ngữ "Cốt lõi"/"Phụ trợ" đã dùng ở tab "Obligation Type (lõi)". Rà thêm phát hiện 2 chỗ cùng lỗi thời trong `ReleaseProcessService.java` (hằng số `DESC`/`TIP` tĩnh, văn bản không đổi theo instance): `DESC[1]` ("gắn Obligation Nature...") và `TIP[1]` ("suy diễn Family hợp lệ...") cùng nhắc khái niệm đã xoá — sửa lại theo đúng flow FOA→OE→OTF hiện tại; tiện thể sửa `TIP[2]` (Bước 3 "Dựng Product Pattern") từ "Ma trận Obligation Type × Block" sang "Ma trận Obligation Type Family (OTF) × Block" cho khớp tên ma trận đã đổi ở Giai đoạn 54/56. Verify: seed đổi nên chạy `docker compose down -v && up --build`; curl `GET /api/release-processes/VAR-101/detail` xác nhận `steps[1].checklist`/`.desc`/`.tip` đúng văn bản mới; regression `obligation-type-compositions` vẫn đúng 246 dòng. Trước đó **Giai đoạn 56 — spell-out "OTF" thành "Obligation Type Family (OTF)" ở mọi nhãn/tiêu đề còn viết tắt trần**: sau Giai đoạn 55, user rà UI qua nhiều vòng gửi ảnh chụp thực tế và chỉ ra tiếp: (1) 2 chỗ chữ hoa cứng ở màn Pattern bị sót ("GÁN OBLIGATION TYPE VÀO KHUÔN", "OBLIGATION TYPE ĐÃ GÁN") — do viết hoa toàn bộ nên grep phân biệt hoa/thường trước đó không bắt được; (2) tab "Nghĩa vụ tài chính" ở Variant detail (tái dùng data từ Pattern nguồn) cũng cần đổi theo cùng lý do "Pattern-sourced content" đã áp dụng từ Giai đoạn 54; (3) stat card "OTF" ở màn Sơ đồ Ontology nên ghi đầy đủ khớp kiểu "Obligation Element Type (OET)" bên cạnh; (4) tab "OTF" ở Obligation Library cũng vậy. Đã hỏi qua AskUserQuestion phạm vi rộng hơn (chỉ 2 chỗ đã chỉ hay sửa hết mọi nơi còn viết tắt) — user chọn "Sửa hết mọi chỗ". Rà toàn bộ frontend+backend, đổi mọi nhãn/tiêu đề/tab/stat-card đang ghi "OTF" trần sang "Obligation Type Family (OTF)": `tables.ts` (2 title), `DashboardPage.tsx` (KPI), `ArchetypePage.tsx`/`ArchetypeDetailPage.tsx` (stat + "thuộc Archetype này"), `SysmapPage.tsx` (3 node bảng quan hệ), `ObligationPage.tsx` (tên tab), `ObligationTypeDetailPage.tsx` (2 tiêu đề khối), `MatrixPage.tsx` (tên tab "× Block"), `GlobalSearch.tsx` (key map icon + badge loại kết quả — đổi đồng bộ với backend vì đây là chuỗi `type` trả thẳng từ API), `ProductPatternDetailPage.tsx` (2 chỗ chữ hoa cứng bị sót), `ProductVariantDetailPage.tsx` (tiêu đề tab). Backend: `ConstraintMatrixService.rowHeadOf()` (case default), `GlobalSearchService` (chuỗi `type`). Giữ ngắn theo ngoại lệ đã thống nhất: cột bảng dữ liệu (`label: 'OTF'`) và câu văn xuôi tiếng Việt dùng "OTF" như từ viết tắt thông thường (vd "1 OTF = tổ hợp...", "cho từng OTF con"). **Bài học kỹ thuật quan trọng phát hiện giữa chừng**: `frontend` chạy trong Docker qua nginx serve `dist/` build sẵn (không phải `npm run dev`) — `npm run build` cục bộ không đủ để thấy thay đổi trên `localhost:5173`, phải `docker compose up -d --build frontend` (và `backend` nếu đổi Java) mỗi vòng sửa mới áp dụng. Verify: build backend (Maven qua Docker)+frontend (tsc) đều 0 lỗi; curl xác nhận `constraint-matrices/3/detail` → `rowHead:"Obligation Type Family (OTF)"`, `GET /api/search?q=vay` → `type:"Obligation Type Family (OTF)"`. Trước đó **Giai đoạn 55 — màn Product Pattern: đổi nhãn "Obligation Type" → "Obligation Type Family"**: ngay sau Giai đoạn 54 (đổi hết nhãn "Obligation Type"→"OTF" NHƯNG cố tình chừa riêng màn Pattern giữ nguyên "Obligation Type"/"OT"), user quay lại yêu cầu tiếp "ở màn Product Pattern có Obligation Type thì đổi thành Obligation Type Family" — tức màn Pattern KHÔNG dùng viết tắt "OTF" như phần còn lại của app, mà dùng dạng đầy đủ "Obligation Type Family" (khác hẳn quyết định "rút gọn còn OTF" đã áp cho mọi màn khác ở Giai đoạn 54). Grep xác nhận đúng 5 chỗ còn "Obligation Type" trần trong 3 file Pattern: `ProductPatternPage.tsx` (cột list "Obligation Type" + filter dropdown cùng tên — sửa ĐỒNG THỜI cả 2 vì `ListScreen.guessColumnIndex()` so khớp label filter với label cột theo chuỗi, đổi lệch 1 bên sẽ tái diễn đúng bug filter chết đã gặp ở Nhật ký hoạt động Giai đoạn 44), `ProductPatternDetailPage.tsx` (banner cảnh báo độ phủ "Chưa gán Obligation Type…"/"Đủ Block bắt buộc theo ma trận Obligation Type × Block" + tên tab palette bên trái "Obligation Type"), `PatternPreviewModal.tsx` (stat card + tiêu đề "Độ phủ theo ma trận Obligation Type × Block"). Không đổi gì khác trong 3 file này (biến nội bộ `paletteTab === 'ot'`, hằng số code vẫn giữ nguyên — chỉ đổi label hiển thị). Verify: `npm run build` 0 lỗi TS. Trước đó **Giai đoạn 54 — nhất quán hoá nhãn "Obligation Type" → "OTF" (trừ màn Pattern)**: user rà lại UI 2 lượt liên tiếp — (1) "đổi tên thành OTF đi ở pattern vẫn để là ot" (đổi hết nhãn "Obligation Type" còn sót sang OTF, NHƯNG màn Pattern giữ nguyên "OT"); (2) "Obligation Type đổi tên thành OTF đi Obligation Type Family" (rút gọn luôn dạng đầy đủ "Obligation Type Family (OTF)" xuống còn "OTF", không cần spell-out nữa). Đã hỏi qua AskUserQuestion xác nhận phạm vi trước khi sửa (liệt kê đủ các chỗ tìm thấy còn sót), user chọn "Đổi hết, trừ Pattern". Rà soát tìm thấy: Archetype (`ArchetypePage.tsx`/`ArchetypeDetailPage.tsx` — StatCard "Obligation Type"→"OTF", "Obligation Type thuộc Archetype này"→"OTF thuộc Archetype này"), Matrix (`MatrixPage.tsx` tab "Obligation Type × Block"→"OTF × Block", và phát hiện thêm sót từ Giai đoạn 52b: `constraint_matrix.title`/`.description` trong `V2__seed.sql` cho ma trận 2/3 chưa từng được sửa dù backend `rowHeadOf()`/frontend `TAB_LABELS` đã đổi — chỉ sửa phần chrome, còn data thật hiển thị (`d.matrix.title`/`d.matrix.description`) vẫn nguyên "Element Type × Element Type"/"Obligation Type"), Dashboard (KPI "Obligation Types"→"OTF"), GlobalSearch (`GlobalSearchService.java` type "Obligation Type"→"OTF" — nhân tiện phát hiện và sửa luôn `path` đang trỏ vòng qua Archetype cha vì lúc viết Giai đoạn 38 "chưa có detail riêng"; giờ đã có từ Giai đoạn 53 nên đổi trỏ thẳng `/obligation-type/{code}`), `tables.ts` (title 'Obligation Type Family (OTF)'/'Obligation Type Composition'→'OTF'/'OTF Composition'), `ObligationPage.tsx` (tab 0 + cột), `OntologyPage.tsx` (card + câu mô tả), `SysmapPage.tsx` (bảng quan hệ tĩnh). **Ngoại lệ có chủ ý, không áp máy móc theo grep**: "Quy tắc kích hoạt Obligation Type phụ trợ" ở Archetype detail — đối chiếu code xác nhận `activationRules` thực chất nói về kích hoạt **OT lõi phụ trợ** (`obligation_type_core`/`ot_activation_rule`, Giai đoạn 51), KHÔNG phải OTF — sửa đúng thành "Quy tắc kích hoạt OT lõi phụ trợ" thay vì đổi nhầm thành OTF. KHÔNG đụng `pages/pattern/*` (`ProductPatternPage.tsx`/`ProductPatternDetailPage.tsx`) và `PatternPreviewModal.tsx` — theo đúng yêu cầu, giữ nguyên "Obligation Type"/"OT" ở màn Pattern (đã grep xác nhận sau khi sửa). Verify: `docker compose down -v && up --build` 0 lỗi (seed đổi nên cần `down -v` — gặp lỗi Flyway checksum mismatch 1 lần khi thử `up --build` thường không xóa volume, đã xử lý đúng bằng down -v); curl xác nhận `GET /api/constraint-matrices` trả đúng title "Ma trận 2: OET × OET (tương thích)"/"Ma trận 3: OTF × Block"; `GET /api/constraint-matrices/3/detail` → `rowHead:"OTF"`; `GET /api/search?q=vay` trả đúng `type:"OTF"` + `path:"/obligation-type/OT_..."` cho cả 5 OTF khớp từ khóa. Trước đó **Giai đoạn 53b — liên kết OE→Block + layout dạng cột cho detail OTF**: ngay sau Giai đoạn 53, user hỏi "đọc trong các tài liệu thì OE sẽ có block ở trong đó đúng k". Đã giải mã thêm file `docs/Lớp+vỏ+thương+mại.doc` (chưa đọc kỹ ở các đợt trước — 3 file MHTML kia không hề nhắc "Block") để xác nhận: KHÔNG phải "OE chứa Block" — tài liệu mô tả rõ OE chỉ định tính ("gốc giảm dần" — như 1 con trỏ hàm, chưa có tham số để chạy), Block là **tầng kế tiếp** (Lớp Tham số Định lượng) nhóm các Attribute định lượng hóa OE đó — đúng khớp quan hệ đã có sẵn trong schema (`block.governed_by_element_code`, Block "chi phối bởi" 1 OE). Xác nhận qua code: field này CHỈ dùng hiển thị (`Block.java`/`BlockService.java`, không có logic ghép nối nào dùng). Sửa: `BlockRepository` thêm `findByGovernedByElementCode()`; `ObligationTypeService.detail()` — mỗi element trong `otCores[].elements[]` thêm `blockId`/`blockName` (null nếu không có Block nào chi phối bởi OE đó — chỉ đúng 5/37 OE thật sự governs 1 Block: `OE_VAL_PRINCIPAL_MULTI_DECREASE`→BLK_VALUEBASE, `OE_ACT_ON_DISBURSEMENT`→BLK_DISBURSEMENT, `OE_FUL_INSTALLMENT`→BLK_REPAYMENT, `OE_REC_ASSET_PLEDGE`→BLK_COLLATERAL, `OE_TIME_CYCLE_STATEMENT`→BLK_BILLING). Frontend `ObligationTypeDetailPage.tsx`: mỗi thẻ OE có Block hiện chip tím bấm được (điều hướng `/block/:id`). User phản hồi layout ban đầu (lưới thẻ) "hơi rối", yêu cầu chia cột — đổi khối "Cấu trúc OTF" sang bảng 3 cột rõ ràng (OET / Obligation Element / Block) cho từng nhóm OT lõi, ô Block hiện "—" khi không có thay vì ẩn hẳn. Verify: `docker compose up --build` 0 lỗi; curl `OT_PLEDGE_INSTALLMENT/detail` xác nhận đúng 5/30 dòng có `blockName` thật (2 dòng "Cơ sở giá trị"/"Trả nợ"/"Tài sản đảm bảo" xuất hiện lặp lại ở cả nhóm Hoàn trả gốc VÀ Trả lãi vì 2 OT lõi này dùng chung vài OE — đúng dữ liệu thật, không phải lỗi trùng), còn lại đúng `null`. Trước đó **Giai đoạn 53 — detail cho Obligation Type Family (OTF)**: user yêu cầu thêm chức năng click detail cho tab OTF trong Obligation Library (trước đây tab này chỉ list thường, không click được). Backend: `ObligationTypeCompositionRepository` thêm `findByObligationTypeCode()`; `ObligationTypeService.detail(code)` mới trả `{otf, archetypeName, patterns:[{patternCode,patternName,role}], otCores:[{otCoreCode,otCoreName,groupKind,leg,elements:[{elementTypeCode,elementTypeName,elementCode,elementName}]}]}` — `patterns` tái dùng thẳng `PatternObligationTypeRepository.findByObligationTypeCode()` đã có sẵn (vốn dùng cho Archetype detail đếm sản phẩm), `otCores` nhóm các dòng `obligation_type_composition` theo `(ot_core_code, leg)` — chính xác cùng logic mà `OntologyPage.tsx` đang tự tính phía client (memo `decomposition`) nhưng giờ tính server-side và chỉ trả đúng 1 OTF thay vì kéo cả 246 dòng composition về frontend rồi lọc lại. `ObligationTypeController` thêm `GET /{code}/detail`. Frontend: chuyển `ObligationPage.tsx` vào subfolder `pages/obligation/` (đúng convention Giai đoạn 31 khi 1 màn có thêm detail sibling); tab 0 (OTF) của `ListScreen` thêm `onRowClick` điều hướng `/obligation-type/{code}` (3 tab còn lại — OT lõi/OE/OET — vẫn giữ nguyên list thường, không có detail vì đó là các catalog cố định/tập đóng, không phải entity nghiệp vụ có lineage riêng). `ObligationTypeDetailPage.tsx` mới (banner gradient tím — khác tông xanh của Archetype để phân biệt trực quan 2 tầng OTF/FOA): 3 stat card (Obligation Type lõi/Element/Product Pattern), khối "Cấu trúc OTF" hiện từng nhóm OT lõi+leg (badge "nhận"/"trả" cho Bàn giao TS) kèm lưới 6 thẻ OET nhỏ, khối "Product Pattern dùng OTF này" (bấm điều hướng sang `/pattern/:code` có sẵn từ trước, không tạo route mới). Route `/obligation-type/:code` đặt trước `/:view`. Verify: `docker compose down -v && up --build` 0 lỗi; curl `OT_PLEDGE_INSTALLMENT/detail` đúng 5 nhóm OT lõi (Giải ngân/Hoàn trả gốc/Trả lãi/Bàn giao TS×2 leg receive+release), mỗi nhóm đủ 6 element, đúng 2 pattern (PT-001 vai trò Primary/PT-002 Support); `OT_UNSECURED/detail` đúng chỉ 3 nhóm (không có Bàn giao TS — đúng đặc điểm tín chấp không tài sản đảm bảo); 404 khi mã không tồn tại. Trước đó **Giai đoạn 52b — đổi nhãn "Element Type" → "OET"**: ngay sau Giai đoạn 52, user hỏi lại "sao trong màn Obligation Library vẫn còn tab tên là element type" — hỏi rõ qua AskUserQuestion thì user xác nhận đây KHÔNG phải còn dữ liệu thừa (đã đúng 6 dòng từ Giai đoạn 52) mà là vấn đề **tên nhãn**: "ta có dùng gì tên là element type nữa đâu, cái này phải tên là OET chứ nhỉ" — yêu cầu đọc lại kỹ Data Dictionary và tài liệu Tư duy thiết kế. Đối chiếu xác nhận đúng: cả 2 tài liệu luôn dùng "Obligation Element Type" (đầy đủ) hoặc viết tắt "OET", không nơi nào dùng tên chung "Element Type"; trong code đã có vài chỗ dùng đúng (`OntologyPage.tsx` card, `tables.ts` title, `SysmapPage.tsx`) nhưng nhiều chỗ khác (tab Obligation Library, 2 cột trong `ObligationPage.tsx`, cột `tables.ts`, 3 câu mô tả `OntologyPage.tsx`, nhãn tab `MatrixPage.tsx`, và `rowHead` trả về từ backend `ConstraintMatrixService.rowHeadOf()` cho Matrix 2) vẫn dùng bare "Element Type" — không nhất quán. Sửa thuần đổi nhãn hiển thị (không đổi dữ liệu/logic): backend `rowHeadOf()` trả `"OET"`; frontend đổi toàn bộ các chỗ trên sang "OET"/"Obligation Element Type (OET)". Verify: curl `constraint-matrices/2/detail` trả đúng `rowHead:"OET"`, `docker compose up --build` 0 lỗi. Trước đó **Giai đoạn 52 — bỏ "Element Type" thừa (gỡ OET_NATURE, OET_LIFECYCLE)**: user gửi thêm tài liệu BA v1.0 mới (`docs/Tu_duy_Loi_Nghia_vu_Tai_chinh_v1.0.docx`, .docx thật — khác 4 file MHTML trước, đã giải mã bằng unzip+strip XML), đối chiếu với Obligation Library và chỉ ra thẳng "cái element type đang bị thừa thãi". Xác nhận đúng: tab "Element Type" có 8 dòng thay vì đúng 6 chuẩn (Party/Value/Activation/Time/Fulfillment/Recovery). Lỗi 1 — `OET_NATURE` không phải 1 trong 6 OET: tài liệu mới (mục OI-4) nói thẳng đây là "va chạm thuật ngữ", đề xuất "gỡ OET_NATURE, chuyển Nature về thuộc tính của FOA"; kiểm chứng DB xác nhận trùng lặp dữ liệu thật ở 5 chỗ — `product_intent` có CẢ `archetype_code` VÀ `nature_element_code` cùng mã hoá 1 fact (UI lộ rõ: `ProductIntentPage.tsx` cột "Archetype" ngay cạnh cột "Obligation nature", `ProductIntentDetailPage.tsx` 2 InfoCard cạnh nhau); `foa_element` có 3 dòng tautology (vd FOA_TERM_LOAN→TERM_LOAN_OBLIGATION required) làm nhiễu ma trận FOA×OE (`GET /api/constraint-matrices/foa-oe-matrix` trả 16 hàng thay vì đúng 13, đã curl xác nhận); `product_intent_element` dư 5 dòng nature lẫn với mã OE thật; `block.governed_by_element_code` của BLK_LIMIT/BLK_INTEREST/BLK_FEE trỏ vào nature giả (2 block cuối trùng y hệt 1 mã `TERM_LOAN_OBLIGATION` — dấu hiệu gán qua loa, đã xác nhận field này CHỈ dùng hiển thị qua đọc `Block.java`/`BlockService.java`, không dùng cho logic ghép nối nào); `obligation_element` có 3 dòng thuộc OET_NATURE. Lỗi 2 — `OET_LIFECYCLE` còn thừa hơn: 0 dòng `obligation_element` nào thuộc chiều này, comment trong seed thừa nhận thẳng nó chỉ tồn tại để "tab có thêm dòng", không gắn dữ liệu nghĩa vụ thật nào. Đã EnterPlanMode (đổi schema/entity/frontend, không chỉ seed), hỏi user qua AskUserQuestion cách xử lý 3 Block governed-by — chọn phương án khuyến nghị: chuyển `governed_by_element_code=NULL`, `governed_by_aspect`=mã FOA thật tương ứng (`FOA_REVOLVING` cho BLK_LIMIT; `FOA_TERM_LOAN` cho BLK_INTEREST/BLK_FEE) — giữ đúng ý nghĩa gốc nhưng trỏ FOA thật thay vì nature giả, không bịa liên kết Block↔OE mới. Sửa: `V2__seed.sql`/`V1__schema.sql` xóa 2 dòng `obligation_element_type` (NATURE/LIFECYCLE) + 3 dòng `obligation_element` thuộc NATURE + 3 dòng tautology `foa_element` + cột `nature_element_code` khỏi `product_intent` (schema+seed+FK+comment) + 5 dòng nature trong `product_intent_element` + sửa governed_by 3 block + co Matrix 2 (`matrix_cell` id=2) từ 6×6 (36 ô) còn 5×5 (25 ô, bỏ hàng/cột OET_NATURE). Backend: `ProductIntent.java` bỏ field+getter `natureElementCode` (controller chỉ serialize entity trực tiếp, không có logic riêng dùng field này). Frontend: `ProductIntentPage.tsx`/`ProductIntentDetailPage.tsx` bỏ cột/card "Obligation nature" (không mất thông tin — "Archetype" đã hiển thị đúng fact này). Ngoài phạm vi (để đợt sau nếu user muốn): tài liệu mới còn nêu OI-4 điểm 2 — đổi tên `OT_*` (tổ hợp OTF, vd `OT_PLEDGE_INSTALLMENT`) → `OTF_*` cho đúng lớp khái niệm, việc này lớn hơn (đổi PK dùng ở nhiều bảng), không thuộc yêu cầu "Element Type thừa" lần này. Verify: `docker compose down -v && up --build` 0 lỗi; curl xác nhận `obligation-element-types` đúng 6 dòng, `obligation-elements` đúng 37 dòng (40−3), `foa-oe-matrix` đúng 13 hàng (16−3), `product-intents/{1..6}/detail` không còn `natureElementCode` và `elements` sạch mã nature, `blocks/BLK_LIMIT|BLK_INTEREST|BLK_FEE/detail` đúng `gov` mới (`FOA_REVOLVING`/`FOA_TERM_LOAN`/`FOA_TERM_LOAN`); regression `obligation-type-compositions` vẫn đúng 246 dòng, `product-patterns/PT-001/detail` và `archetypes/FOA_TERM_LOAN/detail` vẫn 200 OK. Trước đó **Giai đoạn 51b — sửa catalog Obligation Element cho đúng Data Dictionary (36 mã đóng)**: ngay sau Giai đoạn 51, user báo "Obligation Library vẫn sai", yêu cầu đọc lại thật kỹ Data Dictionary rồi đối chiếu đúng/sai. Đã giải mã lại trực tiếp file MHTML gốc (không dựa trí nhớ) lấy chính xác Mục 4 (catalog 36 mã OE theo 6 OET: Party 6/Value 8/Activation 7/Time 5/Fulfillment 5/Recovery 5). Phát hiện lỗi thật: ở Giai đoạn 51 chỉ OET_PARTY được thêm đúng 6 mã canonical mới, còn 5 OET khác vẫn GIỮ NGUYÊN 13 mã kiểu cũ (`PRINCIPAL_NO_INCREASE_MULTI_DECREASE`, `EVENT_LENDER_DISBURSEMENT`, `CALENDAR_HAS_CYCLE_HAS_DEADLINE`...) thay vì đổi đúng tên Data Dictionary (`OE_VAL_PRINCIPAL_MULTI_DECREASE`, `OE_ACT_ON_DISBURSEMENT`, `OE_TIME_CYCLE_DEADLINE`...) — quyết định "tránh rename rủi ro vỡ FK" lúc đó khiến tab "Obligation Element" không khớp catalog 36 mã đóng thật của tài liệu; đồng thời thiếu hẳn 10 mã catalog (`OE_VAL_PRINCIPAL_SINGLE_DECREASE`/`OE_VAL_ACCRUAL_ON_OVERDUE`/`OE_VAL_PERCENT_OF_BASE`/`OE_ACT_DRAWDOWN_REQUEST`/`OE_ACT_BREACH_EVENT`/`OE_ACT_CHARGEABLE_EVENT`/`OE_TIME_UNTIL_EVENT`/`OE_FUL_PER_REQUEST`/`OE_REC_THIRD_PARTY_GUARANTEE`/`OE_REC_RECEIVABLES`) và phát hiện `PAYMENT_BULLET` (mã cũ) trùng nghĩa với `OE_FUL_LUMP_SUM` (đã thêm sẵn ở Giai đoạn 51) — dư 1 mã. Đã hỏi user qua AskUserQuestion về `OE_ACT_SETTLEMENT_TRIGGER` (mã tôi tự thêm ở Giai đoạn 51 cho "Bàn giao TS (trả)" nhưng không mã nào trong 7 mã đóng OET_ACTIVATION khớp đúng "trigger tất toán" — lỗ hổng của chính tài liệu gốc, không phải tôi tự bịa) — user xác nhận Obligation Library chỉ gồm đúng 4 khái niệm OET/OT/OE/OTF (khớp đúng 4-tab đã dựng ở Giai đoạn 51, không đổi cấu trúc tab), giữ mã này làm phần mở rộng có ghi chú rõ. Đã grep xác nhận KHÔNG Java/TSX nào hardcode các mã sắp đổi tên (chỉ xuất hiện trong `V1__schema.sql`/`V2__seed.sql`) — sửa THUẦN `V2__seed.sql`: đổi tên 13 mã sang đúng canonical, gộp `PAYMENT_BULLET`→`OE_FUL_LUMP_SUM`, thêm 10 mã catalog còn thiếu, áp dụng rename nhất quán ở 6 chỗ dùng element_code (`obligation_element`, `foa_element`, `obligation_type_composition` 246 dòng, `block.governed_by_element_code`, `product_intent_element` 2 block insert, `ot_activation_rule`). Verify: `docker compose down -v && up --build` 0 lỗi; curl `obligation-elements` đúng 40 dòng (36 canonical đúng 6+8+7+5+5+5 + 3 mã `OET_NATURE` lịch sử giữ cho `product_intent` + 1 mã mở rộng có ghi chú), `obligation-type-compositions` vẫn đúng 246 dòng không còn dòng nào dùng mã kiểu cũ; regression `blocks/BLK_LIMIT/detail`, `product-intents/1/detail`, `archetypes/FOA_TERM_LOAN/detail` (activationRules), `product-patterns/PT-001/detail`, `constraint-matrices/foa-oe-matrix` đều đúng dữ liệu (chỉ đổi `code` ẩn bên dưới, `name` hiển thị không đổi). Trước đó **Giai đoạn 51 — tái cấu trúc Lõi Nghĩa Vụ Tài Chính theo tài liệu mới FOA/OET/OE/OT/OTF**: user bổ sung 4 file thiết kế mới vào `docs/` (`Lõi Nghĩa Vụ Tài Chính Của Sản Phẩm Vay`, `Lớp vỏ thương mại`, `Ví dụ xuyên suốt: Ma Trận FOA × OE...`, `Data dictionary: FOA, OET, OE, OT, OTF` — thực chất là file MHTML/Confluence export, không phải `.doc` nhị phân thật, đã tự viết script Node giải mã quoted-printable + trích HTML→text để đọc), báo "sẽ thay đổi tư duy thiết kế matrix, FOA", yêu cầu đọc rồi giải thích quan hệ FOA/OET/OE/OT/OTF, sau đó hỏi tác động nếu sửa theo tư duy mới. Tóm tắt mô hình mới: 6 OET (Party/Value/Activation/Time/Fulfillment/Recovery) cố định; OE = câu trả lời cụ thể cho 1 OET; OT = tổ hợp đủ 6 OE (7 mã cố định: 3 cốt lõi Giải ngân/Hoàn trả gốc/Trả lãi + 4 phụ trợ Phí/Phạt/Bảo hiểm/Bàn giao TS, TÊN OT bất biến qua mọi FOA); FOA (Term Loan/Revolving/Conditional, phân loại theo "ai kiểm soát kích hoạt") chỉ gác OE nào được phép cho từng OET của 3 OT cốt lõi (ma trận FOA×OE) — KHÔNG quyết định OT phụ trợ nào tồn tại (việc đó do 1 bảng tra ĐỘC LẬP: OE cụ thể → bật/tắt OT phụ trợ); OTF = OT+OE cụ thể, sẵn sàng tái dùng. Đối chiếu kỹ với code/schema thật (nguyên tắc "code là chuẩn") qua nhiều vòng grep/đọc trực tiếp — phát hiện khung sườn đã có sẵn ~70% dưới tên gọi khác: `financial_obligation_archetype`≈FOA, `foa_element`≈ma trận FOA×OE, `obligation_element`/`obligation_element_type`≈OE/OET (đã có sẵn, không phải chỉ chuỗi tự do như đoán ban đầu), `obligation_type`≈OTF (không phải OT thật — đang bị gọi nhầm tên), `obligation_type_composition`≈bảng OT×OET→OE nhưng bị THIẾU 1 chiều cấu trúc quan trọng: coi 1 OTF chỉ có 1 dòng OET phẳng duy nhất thay vì tổ hợp NHIỀU OT lõi (Giải ngân/Hoàn trả gốc/Trả lãi/Bàn giao TS...) mỗi cái đủ 6 OET riêng. Cũng phát hiện 2 chỗ trùng lặp dữ liệu: `obligation_family` trùng 1:1 với FOA (`FAM_LOAN`↔`FOA_TERM_LOAN`...), và `foa_element` trùng với `constraint_matrix`(kind=`ARCHETYPE_X_ELEMENT`) — cả 2 đều lưu cùng loại dữ liệu FOA×OE requirement. Đã xác nhận rõ: **Pattern↔Block (`pattern_block`) và toàn bộ Block/Attribute/Answer Slot/Constraint/Selector Scope/Fragment/Template/Config/Variant/Catalog/Release KHÔNG bị ảnh hưởng** (2 join độc lập trong `ProductPatternService`, không tham chiếu chéo); vì mã `obligation_type.code` (OT_PLEDGE_INSTALLMENT...) **giữ nguyên không đổi tên**, `pattern_obligation_type`/`ProductPatternService`/`PatternPreviewModal.tsx`/tab "Nghĩa vụ tài chính" ở Variant detail **không cần sửa code**. Đã EnterPlanMode (đổi cấu trúc lõi + nhiều bảng/tầng), hỏi user 3 quyết định qua AskUserQuestion — **cả 3 đều chọn phương án khuyến nghị**: (1) gộp bỏ `obligation_family`, dùng thẳng `archetype_code`; (2) làm ĐỦ theo tài liệu — OTF = tổ hợp nhiều OT lõi, thêm OET_PARTY; (3) gộp `foa_element`+`constraint_matrix`(ARCHETYPE_X_ELEMENT) về 1 nguồn duy nhất (`foa_element`). Backend: **schema** (`V1__schema.sql`) — xóa bảng `obligation_family` + cột `obligation_type.family_code`; bảng mới `obligation_type_core` (7 dòng cố định OT lõi) + `ot_activation_rule` (OE→OT phụ trợ, độc lập FOA); `obligation_type_composition` thêm cột `ot_core_code`+`leg` (PK 4 phần — `leg` phân biệt 2 chiều cùng 1 OT lõi, vd Bàn giao TS nhận/trả). **Seed** (`V2__seed.sql`) — thêm `OET_PARTY` + 6 `OE_PARTY_*` (data dictionary Mục 4.1) + 7 OE mới cần cho OT Giải ngân/Bàn giao TS/Trả lãi (`OE_VAL_FIXED_ONE_OFF`, `OE_VAL_ACCRUAL_ON_BALANCE`, `OE_ACT_CONTRACT_EFFECTIVE`, `OE_ACT_SETTLEMENT_TRIGGER` — mã bổ sung ngoài catalog 36 gốc do ví dụ xuyên suốt cần nhưng data dictionary thiếu sót, `OE_TIME_POINT`, `OE_FUL_LUMP_SUM`, `OE_REC_NOT_APPLICABLE`); backfill lại TOÀN BỘ 246 dòng composition cho 9 OTF hiện có (mỗi OTF tách OT_DISBURSEMENT cố định mọi FOA + OT_PRINCIPAL_REPAYMENT/OT_INTEREST tái dùng đúng giá trị cũ + thêm Party + OT_ASSET_HANDOVER×2 chiều cho 8/9 OTF có Recovery=ASSET_PLEDGE; FOA_CONDITIONAL (OT_GUARANTEE/OT_COMMITMENT) KHÔNG có OT_INTEREST đúng đặc điểm tài liệu nêu). Entity/Repo/Controller mới (đúng pattern `ReadOnlyController`/`@IdClass` có sẵn): `ObligationTypeCore`, `OtActivationRule`(+`Id`) — `/api/obligation-type-cores`, `/api/ot-activation-rules`; xóa hẳn `ObligationFamily`/`Repository`/`Controller`. `ObligationTypeService` bỏ join family. `FinancialObligationArchetypeService.detail()` thêm `activationRules`. `ConstraintMatrixService` bỏ `ARCHETYPE_X_ELEMENT` khỏi switch generic, thêm `foaOeMatrix()` (derived trực tiếp từ `foa_element`, cùng cơ chế `patternCoverage()` sẵn có) + endpoint `GET /api/constraint-matrices/foa-oe-matrix`. Frontend: `ObligationPage.tsx` (Obligation Library) đổi 3→4 tab (thêm "Obligation Type (lõi)", bỏ cột Family); `OntologyPage.tsx` group theo Archetype thay Family, decomposition nhóm theo OT lõi/leg; `ArchetypeDetailPage.tsx` thêm khối "Quy tắc kích hoạt OT phụ trợ"; `MatrixPage.tsx` tab 1 đổi nguồn sang `foa-oe-matrix` (giữ nguyên 4 tab, cùng renderer); `SysmapPage.tsx` sửa bảng quan hệ (Family→OT lõi/OTF); `DashboardPage.tsx` (KPI "phân bố theo Family" không còn nguồn — đổi sang phân bố theo Financial Obligation Archetype, dùng `typeCount` đã tính sẵn ở archetype list, không cần join tay nữa); `tables.ts` (generic fallback DataTable) cập nhật/thêm entry cho 2 bảng mới, bỏ entry `obligation-families` (tránh vỡ nếu ai điều hướng thẳng route đó). Verify: `docker compose down -v && up --build` — Maven+Flyway+tsc đều 0 lỗi; curl xác nhận `obligation-type-cores` đúng 7 dòng, `ot-activation-rules` đúng 1 dòng (`ASSET_PLEDGE→OT_ASSET_HANDOVER`), `obligation-type-compositions` tổng đúng 246 dòng (khớp tính tay), `OT_UNSECURED` đúng 18 dòng không có Bàn giao TS, `foa-oe-matrix` khớp y hệt dữ liệu `constraint_matrix` id=1 cũ trước khi xóa; regression `product-patterns/PT-001/detail` và `product-intents/1/detail` không đổi output; `archetypes` list đúng `typeCount` 5/2/2 (tổng 9). **Chưa verify bằng Playwright/trình duyệt thật** — môi trường phiên này không có công cụ browser automation, chỉ xác nhận qua API + tsc build 0 lỗi, CHƯA render trực tiếp kiểm chứng UI (khác quy trình chuẩn mọi Giai đoạn trước — cần verify bổ sung ở phiên sau nếu cần chắc chắn tuyệt đối). Đã dọn `docs_extract_tmp/` (thư mục tạm giải mã 4 file .doc). Trước đó **Giai đoạn 50 — sửa bug EMI trôi dần khi có phí quản lý + đổi sang tính lãi/phí theo ngày thật (actual/365)**: user gửi file Excel tham chiếu (`docs/Simulation Engine Tính Lịch.xlsx`, mô tả đúng cách công ty tính lịch trả nợ thật), hỏi lịch thanh toán hiện tại có đang tính sai không. Kiểm chứng xác nhận **bug thật**: khi bật `periodicFeePct > 0`, số tiền trả mỗi kỳ không cố định (trôi dần 3.001.299đ→2.924.802đ cho cùng 1 khoản vay) dù đây phải là "trả góp đều" — nguyên nhân do PMT (`annuity(balance, r, months)`) chỉ tính trên lãi suất, rồi cộng phí quản lý riêng lẻ (giảm dần theo dư nợ) khiến tổng trôi thay vì cố định. File Excel làm đúng: gộp Lãi+Phí thành 1 "CPV" (Chi phí vay) theo rate tổng, tính EMI cố định trên CPV này, mỗi kỳ tách lại theo đúng tỷ lệ cấu thành; đồng thời CPV tính theo SỐ NGÀY THẬT trong tháng lịch (28-31 ngày)/365 chứ không cố định "1 kỳ=1 tháng chẵn", và kỳ cuối có 1 khoản "plug" bù sai số dồn để dư nợ về đúng 0. Đã EnterPlanMode (thay đổi lõi công thức, ảnh hưởng cả grace/prepay/penalty), hỏi user 2 quyết định qua AskUserQuestion — cả 2 chọn "Có, sửa/đổi": (1) PMT tính trên rate gộp lãi+phí; (2) đổi hẳn sang tính theo ngày thật/365. Sửa `SimulationEngine.java`: thêm `rTotal = r + feeMonth`, `dailyRateTotal = rTotal×12/365`; `pmt = annuity(balance, rTotal, months-grace)` (thay vì chỉ `r`); mỗi kỳ tính `daysInPeriod` thật (`ChronoUnit.DAYS.between` 2 mốc `periodStart`/`periodEnd` đã có sẵn), `cpv = opening×dailyRateTotal×daysInPeriod`, tách `interest = cpv×(r/rTotal)`, `fee = cpv×(feeMonth/rTotal)`; thêm plug kỳ cuối (`isLastScheduled` hoặc dư nợ gần 0 → `principal = opening` thay vì đúng PMT, `payment` kỳ đó tự nhiên khác các kỳ trước); 2 chỗ tái tính `pmt` giữa vòng lặp (grace kết thúc, có prepay) đổi từ `r` sang `rTotal`. Không đổi `SimulationRequest`/API contract, không đổi công thức `penalty` (không liên quan bug này), không đổi frontend. Verify: dựng lại đúng input Excel (30tr/12th/lãi 1,6%/phí 0,4%) qua `/api/simulation/run` — EMI giờ CỐ ĐỊNH 2.836.788đ (gần khớp Excel 2.832.000đ, lệch ~0,17% do khác giả định làm tròn EMI ban đầu — đã báo trước với user, không cần khớp bit-for-bit), kỳ cuối "plug" đúng đưa dư nợ về 0; VAR-108 (case gây bug ban đầu, phí 0,4%) giờ payment cố định 2.964.511đ mọi kỳ (trước đây trôi 3.001.299→2.924.802đ); regression test penalty/graceDays (Giai đoạn 47) vẫn đúng tỷ lệ 2x; regression test VIP/Thân thiết (Giai đoạn 49) vẫn đúng thứ tự ưu đãi; Playwright xác nhận UI "Lịch trả nợ" hiện đúng "Kỳ trả" cố định mọi kỳ. Trước đó **Giai đoạn 49 — sửa lệch ưu đãi lãi suất VIP < Thân thiết trong Simulation Engine**: user phát hiện qua UI so sánh 2 phân khúc: tổng phải trả của VIP lại LỚN HƠN Thân thiết dù trực giác VIP phải được ưu đãi bằng hoặc hơn — hỏi đây là lỗi logic hay gì. Kiểm chứng bằng test cô lập (giữ nguyên mọi tham số, chỉ đổi `segmentCode`) xác nhận **logic tính hoàn toàn đúng** theo đúng số đang khai báo — không phải bug code: bảng `customer_segment` seed gốc lỡ đặt Thân thiết ưu đãi `−0,5%/tháng` CAO HƠN VIP `−0,3%/tháng` (ngược trực giác thông thường VIP ≥ Thân thiết), khiến VIP chịu lãi suất cao hơn → tổng phải trả cao hơn — đúng như tính toán, chỉ là dữ liệu ưu đãi đặt ngược. Đã hỏi user qua AskUserQuestion, chọn **đổi lại đúng thứ tự thông thường**. Sửa: `V2__seed.sql` đổi `customer_segment.name` (SEG_LOYALTY → `−0,3%/tháng`, SEG_VIP → `−0,5%/tháng`); `SimulationEngine.segmentAdjustment()` đổi `case "loyalty" -> -0.3` / `case "vip" -> -0.5` (trước đó ngược lại); `SimulationPage.tsx` đổi nhãn `SEGMENTS` khớp theo. Verify: test cô lập VAR-108 xác nhận VIP giờ đúng 0,8%/th (thấp hơn Thân thiết 1,0%/th) → tổng phải trả VIP (23.249.242đ) thấp hơn Thân thiết (23.451.715đ) — đúng thứ tự mong muốn; Playwright xác nhận UI hiện đúng nhãn "Ưu đãi −0,3%/tháng" (Thân thiết) và "−0,5%/tháng" (VIP). Trước đó **Giai đoạn 48 — bổ sung Block "Hạn mức" (BLK_LIMIT) vào Pattern PT-001**: user hỏi tại sao Template TPL-001 có "hạn mức" mà Config/Variant đóng gói từ nó (CFG-0043/VAR-108 "Vay xe máy mùa tựu trường") lại thiếu, muốn thêm cho đủ. Audit xác nhận: Pattern PT-001 (nguồn của TPL-001 → CFG-0021/0040/0043 → VAR-106/103/108) **chưa từng gán Block `BLK_LIMIT` vào `pattern_block` ngay từ đầu** (chỉ có 7 block: Bên tham gia/Tuân thủ/Lãi suất/Phí/Trả nợ/Tài sản ĐB/Phạt trễ hạn) — không phải bug ngẫu nhiên mà là Pattern gốc thiếu sót. Phát hiện thêm: `limit_range` hiển thị trên Variant (list/Simulation) đọc từ 1 cột riêng `product_variant.limit_range` (text tự do, độc lập Pattern/Block) — nên Variant vẫn "có vẻ" đủ hạn mức dù chuỗi Answer Slot hoàn toàn không có, gây cảm giác lệch giữa 2 màn. Đã sửa `V2__seed.sql`: thêm `('PT-001','BLK_LIMIT',8,'active')` vào `pattern_block`; thêm `template_frame` default cho TPL-001 (`3tr – 80tr`) và TPL-002 (`50tr – 2 tỷ`, cùng Pattern PT-001 nhưng chưa có Config nào nên chỉ để đủ, không ảnh hưởng UI); thêm fragment `limit_amount`+`capacity_range` (scope `default`) cho CẢ 3 Config đang dùng TPL-001 (CFG-0021/CFG-0040/CFG-0043) — khớp ĐÚNG giá trị thật đã có sẵn ở cột `product_variant.limit_range` của từng Variant tương ứng (VAR-106="2tr–30tr", VAR-103="3tr–80tr", VAR-108="3tr–40tr", quy đổi sang định dạng VND đầy đủ như `2.000.000đ – 30.000.000đ`), `capacity_range` dùng lại giá trị "Có quản trị" đã có tiền lệ ở CFG-0042. Không đổi code — thuần bổ sung seed data để lấp lỗ hổng Pattern, tái dùng đúng luồng resolve (`buildBody`/`resolved()`) đã có từ Giai đoạn 46. Verify: `GET /api/product-configs/CFG-0043/detail` (builder) và `/resolved` đều lên đúng **17/17 slot bắt buộc = 100%** (trước đó 15/15, giờ +2 do BLK_LIMIT có 2 slot bắt buộc); `GET /api/product-patterns/PT-001/detail` trả đủ 8 block, không lỗi; Playwright `/config/CFG-0043` (builder) hiện đúng nhóm "Hạn mức" trong sidebar, `/variant/VAR-108` tab "Giá trị cấu hình" hiện đúng "Hạn mức cấp: 3.000.000đ – 40.000.000đ". Trước đó **Giai đoạn 47 — Simulation Engine bỏ hardcode, tính từ dữ liệu thật của sản phẩm**: sau Giai đoạn 46, user hỏi Simulation Engine (`SimulationService`/`SimulationEngine`) có đang tính từ dữ liệu thật không hay đang hardcode. Audit `deriveFromVariant()` đối chiếu toàn bộ Answer Slot/Fragment/Template Frame thật, chia 3 nhóm: A) đã thật (amount/months/rate/LTV, không sửa); B) DB có dữ liệu thật per-product nhưng code hardcode bỏ qua — user xác nhận sửa: `appraisalFee` (hardcode 500.000đ mọi sản phẩm dù `template_frame`/`fragment` slot `fee_amount` có giá trị khác nhau thật: TPL-001=300k/TPL-002=800k/TPL-004=400k), `segmentCode` (hardcode `SEG_STANDARD` dù bảng `template_segment` cho biết đúng phân khúc thật mỗi Template, vd TPL-002=`SEG_BUSINESS`), `penaltyFactor` (lấy trần quy định `attribute_constraint` làm giá trị áp dụng thay vì đọc chính sách thật slot `penalty_rate`), và `grace` (số ngày trễ hạn được miễn phạt, slot `BLK_PENALTY.grace` — hoàn toàn chưa đọc ở đâu; đã hỏi user xác nhận đây KHÁC khái niệm `graceMonths`/`graceOn` có sẵn — trục ân hạn gốc đầu kỳ vay, thuộc nhóm C do người dùng tự bật, không có nguồn DB); C) genuinely không có nguồn (`periodicFeePct`, `startDate`, toggle kịch bản penalty/prepay/early/graceOn) — theo yêu cầu user, KHÔNG đụng vào nhóm C. Backend: `SimulationService` inject thêm `TemplateFrameRepository`/`TemplateSegmentRepository`/`AnswerSlotRepository`; thêm `resolveSlotValue(configCode, templateCode, blockId, slotCode)` (3 tầng fallback giống Giai đoạn 46: fragment scope="default" → `template_frame` → `answer_slot.default_value`) + parser `parseVndAmount`/`parseFirstInt`. `deriveFromVariant()` giờ resolve `appraisalFee` (slot `fee_amount`, BLK_FEE) và `segmentCode` (`template_segment` theo Template nguồn) thật, chỉ fallback hardcode khi cả 3 tầng đều thiếu. `applyRegulatoryCaps()` (đổi tên ý nghĩa, vẫn giữ tên) giờ resolve thêm `penaltyFactor` thật (slot `penalty_rate`) và field MỚI `graceDays` (slot `grace`, khác `graceMonths`) theo `req.getConfigCode()` — áp dụng cho CẢ `getDefault()` và `run()` vì đây là chính sách sản phẩm cố định (khác `appraisalFee`/`segmentCode` vẫn là input người dùng chỉnh tay trên form, chỉ sửa initial value). `SimulationRequest` thêm field `graceDays`. `SimulationEngine.run()`: công thức phạt đổi từ `pmt*(penaltyDays/30)*r*penaltyFactor` thành trừ ân hạn trước: `billableDays = max(0, penaltyDays - graceDays)`; kết quả trả thêm `graceDaysApplied` để minh bạch. Frontend: `SimulationPage.tsx` thêm `graceDaysApplied` vào `RunResult`, card "TỔNG PHẠT TRỄ HẠN" hiện thêm dòng "miễn N ngày đầu (chính sách sản phẩm)" khi `graceDaysApplied>0`. Verify: VAR-101 (CFG-0042←TPL-003, không có fee_amount/penalty_rate riêng ở cả 3 tầng) đúng fallback 500.000đ (không phải bug, do thật sự không có dữ liệu nào); VAR-103 (CFG-0040←TPL-001, có fee_amount=300.000đ thật) trả đúng 300.000đ + segmentCode=SEG_INDIVIDUAL (không còn SEG_STANDARD hardcode); test phạt trễ hạn 10 ngày với configCode có graceDays=5 ra tổng phạt đúng bằng 1/2 so với khi không có configCode (graceDays=0) — xác nhận công thức trừ ân hạn hoạt động đúng; Playwright xác nhận UI hiện đúng dòng "Kỳ 3 · trễ 10 ngày · miễn 5 ngày đầu (chính sách sản phẩm)". Trước đó **Giai đoạn 46 — hiện ĐỦ mọi Answer Slot ở tab "Giá trị cấu hình" của Variant**: user mô tả lại đúng bản chất resolve giá trị (Pattern định nghĩa Block → Template `template_frame`=default → Config `fragment`=ghi đè → Variant đóng gói), muốn tab này hiện đủ mọi Answer Slot của Pattern, slot nào không có fragment thì lấy default Template. Audit phát hiện: tab này tái dùng thẳng `GET /api/product-configs/{code}/detail` (API màn **builder** Config), và dòng `ProductConfigService.java` (`activeBlocks = fragBlocks.isEmpty() ? frameBlocks : fragBlocks`) **cố ý** (từ Giai đoạn 21, để khớp pixel-perfect 1 ảnh chụp CFG-0042) loại bỏ toàn bộ block CHƯA từng có fragment ngay khi config có fragment ở block khác — kiểm chứng thật CFG-0042 (pattern 9 block/24 slot) chỉ trả 6 block/17 slot, mất hẳn `BLK_BILLING`/`BLK_ELIGIBILITY`/`BLK_REGULATORY` dù có default Template. Cũng phát hiện completeness% (`totalReq`/`reqFilled`) bị tính THIẾU do cùng nguyên nhân. Quyết định kiến trúc (hỏi user qua AskUserQuestion, chọn phương án tách): **không sửa** `detail()` (giữ nguyên màn builder Config, tránh regression) — refactor phần vòng lặp block/slot thành private method dùng chung `buildBody(cfg, tpl, patternCode, frameByKey, fragsByKey, blockFilter)` (`blockFilter=null` = không lọc), thêm method mới `resolved(code)` gọi `blockFilter=null` + endpoint `GET /api/product-configs/{code}/resolved`. Tiện thể bổ sung field `slotDefaultValue` (= `answer_slot.default_value`, cột có sẵn nhưng trước đây không dùng ở đâu trong luồng resolve) làm tầng fallback thứ 3, áp dụng cho CẢ 2 method. Frontend: `ConfigValuesTab` (trong `ProductVariantDetailPage.tsx`) đổi fetch sang `getDetail(..., 'resolved')`, thêm nhánh fallback "Mặc định Answer Slot: X" khi không có fragment lẫn `inheritedFrameValue`. Không đổi gì `ProductConfigDetailPage.tsx` (builder). Verify: curl `/detail` CFG-0042 vẫn y hệt cũ (6 block/17 slot, không regression); `/resolved` CFG-0042 trả đủ 9 block/24 slot, completeness đổi từ 86% (14 slot) → 67% (18 slot, đúng thật); Playwright `/variant/VAR-101` tab Giá trị cấu hình hiện đủ 9 block (kể cả "Sao kê & Hóa đơn" trước đây ẩn) với đúng giá trị Kế thừa Template; `/config/CFG-0042` (builder) không đổi, vẫn 6 block. Trước đó **Giai đoạn 45 — màn detail cho Nhật ký hoạt động**: user yêu cầu thêm detail cho từng dòng activity log — prototype gốc không có (đã xác nhận từ Giai đoạn 29, activity chỉ list), nên đây là UI mới thiết kế theo đúng ngôn ngữ thiết kế đã lập ở các detail khác (banner gradient/StatusChip-style badge/fadeUp). Backend: `ActivityLogService.detail(id)` (mới, trả thẳng `toRow()` — không bọc thêm) + `GET /api/activity-logs/{id}/detail`; `toRow()` bổ sung field `id` (trước đây thiếu, cần cho row-click). Frontend: chuyển `ActivityPage.tsx` vào `pages/activity/` (theo convention Giai đoạn 31 — 1 khi có detail thì gộp subfolder), thêm `onRowClick` điều hướng `/activity/{id}`; `ActivityDetailPage.tsx` (mới): banner badge màu theo hành động (tái dùng thẳng `STATUS_COLORS` qua alias create~draft/submit_review~review/approve~approved/publish~published/retire~retired — không bịa màu mới), 4 info card (Actor/Thời gian/Kênh/Đối tượng liên quan kèm nút "Xem chi tiết" điều hướng đúng route entity nguồn nếu có — BusinessIntent/ProductIntent quy đổi code→id, Pattern/Template/Config/Variant dùng thẳng code; AnswerSlot/ConstraintMatrix không có detail nên không hiện nút), và khối "Hoạt động liên quan của đối tượng này" tái dùng THẲNG component `ApprovalHistory` có sẵn (Giai đoạn 42) — chỉ thêm 1 prop `title` tùy chọn (mặc định "Lịch sử duyệt") để đổi nhãn cho phù hợp ngữ cảnh, không tạo API/component mới. Route `/activity/:id` đặt trước `/:view`. Verify: curl `GET /api/activity-logs/{id}/detail` đúng dữ liệu + 404 khi id không tồn tại; Playwright bấm dòng đầu list → detail đúng badge/info/timeline liên quan, bấm "Xem chi tiết" nhảy đúng sang `/variant/VAR-108`; test 2 case không có detail (AnswerSlot, ConstraintMatrix) xác nhận không hiện nút "Xem chi tiết" và không crash. Trước đó **Giai đoạn 44 — "Lịch sử duyệt" cho Business Intent & Product Intent + filter Hành động ở Nhật ký hoạt động**: user yêu cầu 2 việc liên tiếp. (1) Mở rộng khối "Lịch sử duyệt" (Giai đoạn 42) lên 2 tầng đầu Pipeline chưa có — Business Intent và Product Intent — cùng cách làm với Giai đoạn 43: audit 13 entity (BI×7/PI×6), 8/13 thiếu 1-4 bước activity_log so với status thật, thêm 32 dòng activity_log mới (tổng 133, từ 101) + viết lại UPDATE `created_user`/`updated_user` cho cả 2 bảng (hết NULL). Thêm `<ApprovalHistory entityType="BusinessIntent"/>` và `entityType="ProductIntent"` vào cuối `BusinessIntentDetailPage.tsx`/`ProductIntentDetailPage.tsx` — tái dùng nguyên component có sẵn, không API mới. (2) Filter "Loại" ở màn Nhật ký hoạt động (`ActivityPage.tsx`, `filters={['Actor','Loại','Kênh']}`) hoá ra **không khớp cột nào** — `ListScreen.guessColumnIndex()` so khớp label filter với label cột thật (Thời gian/Actor/Hành động/Đối tượng/Kênh), "Loại" không match cột nào nên dropdown luôn rỗng ("Không có giá trị để lọc") — filter chết từ trước tới giờ. Đổi thành `'Hành động'` khớp đúng cột "HÀNH ĐỘNG" có sẵn, tự động ra đủ 8 giá trị thật (Tạo/Gửi duyệt/Phê duyệt/Xuất bản/Thu hồi/Gán/Đồng bộ/Cập nhật) — không cần sửa gì khác vì `ListScreen` tự suy filter option từ dữ liệu cột. Verify: SQL đối chiếu status↔activity_log muộn nhất cho 13 entity khớp 100%; Playwright xác nhận BI-01/PI-005 hiện đủ "Lịch sử duyệt", filter "Hành động" ra đúng 8 option lọc được. Trước đó **Giai đoạn 43 — vá lỗ hổng "Lịch sử duyệt" trống trơn trên toàn Pipeline (Pattern/Template/Config/Variant)**: user phát hiện qua UI Template `TPL-001` ("Vay cầm cố trả góp · KH cá nhân") status "Đã xuất bản" nhưng khối "Lịch sử duyệt" (thêm ở Giai đoạn 42) trống trơn, hỏi đây là lỗi data hay lỗi code. Audit toàn diện xác nhận **lỗi data, không phải lỗi code** — component `ApprovalHistory.tsx`/endpoint `GET /api/activity-logs/entity` hoạt động đúng, chỉ là DB thật sự không có dòng `activity_log` nào cho các entity đó (0-1 dòng lẻ tẻ thay vì đủ chuỗi tạo→gửi duyệt→phê duyệt→xuất bản). Rà soát **toàn bộ 28 entity Pattern(6)+Template(6)+Config(8)+Variant(8)** (không chỉ TPL-001) bằng cách đối chiếu status thật ↔ activity_log ↔ version_entry, phát hiện: (1) 22/28 entity thiếu 1-4 bước activity_log so với status thật; (2) 1 bug status khác cùng loại: `product_template` TPL-003 có `status='review'` nhưng `version_entry` head thật (v1.2, is_head=true) đã ở `published` từ 2026-06-30 — và cả 2 Config đóng gói từ nó (CFG-0042/0041) đều đã published — Template nguồn không thể đứng sau Config trong lifecycle (bug giống hệt Giai đoạn 40 nhưng ở cấp Template↔version_entry thay vì Config↔Variant); đã sửa `status` → `published`. Thêm 61 dòng `activity_log` mới (tổng 101, từ 40) khớp đúng chuỗi create→submit_review→approve→publish/retire theo ĐÚNG status hiện có của từng entity, tái dùng actor/mốc thời gian từ `version_entry` khi entity đó đã có sẵn (PT-001/003/005/006, CFG-0021/037/039/040/041, TPL-003) để 2 nguồn dữ liệu khớp nhau; viết lại toàn bộ khối UPDATE `created_user`/`updated_user` (Giai đoạn 42) cho khớp chuỗi mới — không còn dòng nào NULL trong 4 loại entity này. Verify bằng SQL đối chiếu `status` thật ↔ hành động activity_log MUỘN NHẤT cho cả 28 entity — khớp 100%; Playwright xác nhận `TPL-001`/`TPL-003` hiện đủ 4 bước đúng actor/thời gian, không còn "Lịch sử duyệt" trống ở đâu trong Pipeline. Ngoài phạm vi (không sửa vì không có UI nào đọc): Business Intent/Product Intent vẫn thiếu activity_log ở vài dòng (không có khối "Lịch sử duyệt" trên 2 màn này) và 5/6 Template (trừ TPL-003) vẫn chưa có `version_entry` riêng (nút "Phiên bản" sẽ trống cho TPL-001/002/004/005/006) — đã báo cho user, chưa sửa vì không phải bug được báo cáo trong đợt này. Trước đó **Giai đoạn 42 — Nhật ký hoạt động theo audit thật + Menu theo Role người dùng**: user hỏi "ai duyệt, ai làm gì cho từng sản phẩm" rồi muốn xử lý nhật ký hoạt động theo cột audit (`created_user`/`updated_user`, thêm ở Giai đoạn 40 nhưng seed chưa từng điền — toàn NULL), đồng ý thêm bảng user thật nếu cần; kế đó bổ sung yêu cầu menu sidebar phải lọc theo role người dùng, có 1 role Admin thấy hết. Đã EnterPlanMode vì phạm vi lớn (DB + backend + frontend). Quyết định kiến trúc quan trọng: bộ chọn "đổi vai trò" là **demo kiểu chuyển đổi user** (dropdown, không mật khẩu/session/JWT), lọc menu THUẦN phía frontend — không phải hệ thống đăng nhập/bảo mật thật (đã nói rõ với user). `activity_log.actor`/`version_entry.author` giữ nguyên `varchar` (snapshot tên, không FK hoá — audit-trail phổ biến, tránh viết lại ~70 dòng có sẵn). Backend: enum mới `user_role_enum` (Product Owner/Product Designer/Checker · Approver/Operations/Admin, tách khỏi `release_role_enum` cũ) + bảng `app_user` (6 dòng — 5 tên đã dùng thật trong activity_log/version_entry, chỉ 1 "Quản trị viên" là mới) + `AppUserController` (`GET /api/users`, thuần đọc). Populate `created_user`/`updated_user` cho 6 loại entity (business_intent/product_intent/pattern/template/config/variant) suy **trực tiếp** từ activity_log thật đã có (actor của hành động `create` → created_user, actor hành động muộn nhất → updated_user) — chỉ update những dòng có ít nhất 1 activity_log thật, không suy đoán cho dòng chưa từng xuất hiện (kể cả `product_catalog` — dù nằm trong "6 entity chính" theo dự tính ban đầu, audit phát hiện KHÔNG có activity_log nào nhắc tới catalog nên bỏ qua, giữ NULL đúng thật). Tổng quát hoá `ActivityLogService.forEntity()` (đã có sẵn từ Giai đoạn 36 nhưng chỉ dùng nội bộ cho Variant) thành endpoint `GET /api/activity-logs/entity?type=&code=` dùng chung. Frontend: `UserContext.tsx` (React Context, fetch `/api/users`, lưu lựa chọn `localStorage`, mặc định lần đầu = Admin để không ai bị ẩn menu khi mới vào); `nav.ts` thêm field `roles?: UserRole[]` mỗi item (undefined = mọi role thấy, dùng cho dashboard/activity); `Layout.tsx` thay khối user hardcode ("PD"/"Phạm Designer"/"Product Owner" — phát hiện qua audit là literal string vô nghĩa, không lấy từ đâu cả) bằng dropdown thật chọn `app_user`, lọc `NAV` theo `currentUser.role` (Admin bỏ qua lọc). Khối "Lịch sử duyệt" mới (`ApprovalHistory.tsx`, tái dùng đúng style timeline chấm tròn của "Hoạt động gần đây" ở Variant detail) thêm vào Config/Pattern/Template detail — gọi endpoint chung, không API riêng từng trang. Verify: curl `/api/users` đủ 6 user đúng role, `/api/activity-logs/entity?type=ProductConfig&code=CFG-0043` đúng 4 bước; Playwright xác nhận Admin thấy đủ 20 mục, Product Owner chỉ còn 4 (Business Intent/Product Intent/Dashboard/Activity), Checker/Approver đúng 7 mục, role chọn giữ nguyên sau F5 (localStorage), khối Lịch sử duyệt ở CFG-0043 hiện đúng Phạm An tạo → Trần Lan gửi duyệt → Lê Minh phê duyệt → Hệ thống xuất bản. Trước đó **Giai đoạn 41 — sample data "Vay xe máy mùa tựu trường"**: user yêu cầu 1 sản phẩm vay hoàn chỉnh đầy đủ các bước, đã duyệt qua kiểm duyệt. Tái dùng khuôn `PT-001`/`TPL-001` published có sẵn (không tạo trùng BI/PI/Pattern mới) — thêm `CFG-0043`(17 fragment, completeness 100%, 2 fragment ưu đãi thời vụ) + `VAR-108` (published, niêm yết App+Web) + `version_entry` CFG-0043 đủ 5 bước + 8 dòng `activity_log` (create/submit_review/approve/publish cho cả 2). Trước đó **Giai đoạn 40 — sửa lệch lifecycle Config↔Variant + thêm cột audit/CDC vào 43 bảng**: user phát hiện qua UI Config "chờ duyệt" nhưng Variant đóng gói từ nó lại "đã xuất bản" — verify bằng SQL join thật xác nhận là lỗi seed data (không phải hiển thị), sửa 3 Config (CFG-0042/0041/0038) nâng status khớp Variant nguồn, kèm version_entry+activity_log đầy đủ cho từng bước (không đổi trơ 1 cột). Cùng đợt: theo file `Product_Factory.sql` mới user cung cấp, thêm 9 cột audit (created_user/updated_user/deleted_user/is_deleted/cdc_version/cdc_timestamp...) cho TẤT CẢ 43 bảng + 4 cột governance (data_owner/data_steward/sensitivity/retention_policy) cho 8 bảng nghiệp vụ chính — đã tự sửa lỗi double-encode UTF-8 trong file gốc trước khi áp dụng (không copy nguyên bản mojibake). Trước đó **Giai đoạn 39 — làm thật nút "Xem trước" ở builder Product Pattern**: nút này trước đây no-op (`title=READONLY`) như mọi nút CUD khác, nhưng user hỏi ý tưởng rồi chốt làm thật vì bản chất là hành động XEM (không phải CUD) — giống tiền lệ nút "Phiên bản" đã làm thật trước đó. Đã hỏi user 2 quyết định UI qua AskUserQuestion: overlay toàn màn hình (không phải drawer) và KHÔNG thêm nút xuất PDF (tránh tạo nút no-op gây hiểu lầm). **Không cần API mới** — toàn bộ dữ liệu Preview cần (blocks/slots đầy đủ, assignedOTs, coverage) đã có sẵn trong state của builder từ `GET /api/product-patterns/{code}/detail` đang dùng, Preview chỉ là 1 cách trình bày khác (component `PatternPreviewModal.tsx` mới, thuần render prop, không fetch gì). Nội dung: header gradient (tên/mã/status/Product Intent nguồn), 3 stat card (Block/Slot/Obligation Type), khối Nghĩa vụ tài chính, khối Độ phủ ma trận (badge theo verdict đã tính sẵn ở `coverage.rows`), và cấu trúc đầy đủ theo thứ tự `position` — mỗi Block liệt kê toàn bộ Answer Slot (tên/kiểu/bắt buộc/mặc định). Verify: Playwright bấm "Xem trước" ở PT-002 ra đúng 9 Block/24 Slot/2 Obligation Type, đúng badge độ phủ, đúng cấu trúc. Trước đó **Giai đoạn 38 — hoàn thiện thanh tìm kiếm toàn cục ở topbar**: user yêu cầu "trang nào có thanh tìm kiếm thì hoàn thiện nó". Rà soát phát hiện 2 loại search khác nhau: (1) search trong từng màn list (`ListScreen`) — **đã thật từ Giai đoạn 20**, chạy client-side, không cần sửa; (2) thanh search ở **topbar** (`Layout.tsx`, input "Tìm mã, sản phẩm, obligation…" + chip ⌘K) — **hoàn toàn trang trí, không có `onChange`/logic gì**, tồn tại y nguyên từ lúc trích prototype tới giờ. Đây mới là phần cần hoàn thiện. Backend (mới hoàn toàn): `GlobalSearchService` (package `application.service.search`) quét trực tiếp 12 repo lõi đã có sẵn (BusinessIntent/ProductIntent/ProductPattern/ProductTemplate/ProductConfig/ProductVariant/ObligationType/FinancialObligationArchetype/Block/Attribute/Domain/Lifecycle) — so khớp substring không phân biệt hoa thường trên tên+mã, tối đa 5 kết quả/loại (tránh 1 loại át hết), trả `{type,code,name,status,path}` với `path` trỏ thẳng route chi tiết đã có (Obligation Type chưa có detail riêng → trỏ qua Archetype cha, đúng thật vì `archetype_code` NOT NULL). `GlobalSearchController`: `GET /api/search?q=`. Frontend: `GlobalSearch.tsx` (component mới) thay thế input tĩnh — debounce 250ms, dropdown kết quả có icon/type/mã/StatusChip, bấm ra điều hướng đúng route, phím `⌘K`/`Ctrl+K` focus ô search, `Escape` đóng, click-outside đóng. Verify: gõ "xe máy" ra 6 kết quả đúng thật đa loại (Business Intent BI-02, Product Intent PI-005, 2 Config, 2 Variant); bấm kết quả điều hướng đúng sang `/businessintent/2` hiện đúng dữ liệu. Lưu ý kỹ thuật: test qua `curl` trong Git Bash Windows bị lỗi encoding tiếng Việt (không phải bug — xác nhận bằng percent-encode thủ công ra đúng kết quả), nên verify cuối cùng bằng Playwright gõ trực tiếp (UTF-8 đúng). Trước đó **Giai đoạn 37 — chia detail Product Variant thành 3 tab con**: user phân vân giữa Catalog/Variant nên xem chi tiết ở đâu cho nhu cầu "toàn bộ dữ liệu liên quan — FOA, block, attribute, giá trị". Đề xuất (user đồng ý): giữ Catalog→Release (đúng ý nghĩa "tiến độ phát hành", đã chốt Giai đoạn 32), còn nhu cầu "xem toàn bộ" thì đào sâu ngay ở `ProductVariantDetailPage` (Giai đoạn 36) — chia 3 tab thay vì 1 trang cuộn dài: **Tổng quan** (nội dung Giai đoạn 36 giữ nguyên), **Giá trị cấu hình** (tái dùng THẲNG `GET /api/product-configs/{code}/detail` đã có sẵn từ Giai đoạn 21 — completeness bar + từng block/slot kèm giá trị fragment đã resolve theo scope, badge cảnh báo vàng cho `is_warning`, không viết thêm backend), **Nghĩa vụ tài chính** (tái dùng THẲNG `GET /api/product-patterns/{code}/detail` đã có sẵn từ Giai đoạn 13 — danh sách Obligation Type gán cho Pattern nguồn kèm archetype + vai trò Chính/Phụ). **Không thêm backend nào mới** — cả 2 tab đều gọi API sẵn có theo `configCode`/`patternCode` lấy từ lineage đã build ở Giai đoạn 36, load lười (lazy) khi bấm tab. Sửa 1 lỗi nhỏ lúc verify: field `role` thật trong DB là `Primary`/`Support` (viết hoa, không phải `primary`/`secondary` như đoán ban đầu) — sửa `ROLE_LABEL` map khớp đúng giá trị thật. Verify: Playwright cả 3 tab với VAR-101 xác nhận đúng dữ liệu thật (Giá trị cấu hình: 12/14 slot bắt buộc, Bên tham gia/Hạn mức/Lãi suất... đúng giá trị theo block; Nghĩa vụ tài chính: 2 Obligation Type đúng archetype/vai trò). Trước đó **Giai đoạn 36 — detail "thật chi tiết và đẹp và mượt và đầy đủ thông tin" cho Product Variant**: user yêu cầu rõ ràng mức độ đầu tư cao hơn hẳn Giai đoạn 34-35. Backend (khác Block — cần code mới vì chưa có sẵn): `ProductVariantService.detail(code)` join **toàn bộ lineage ngược Pattern←Template←Config←Variant** (dùng `ProductTemplateRepository`/`ProductPatternRepository` mới inject) + **niêm yết Catalog** (`CatalogListingRepository.findByVariantCode` có sẵn, join `ProductCatalog` lấy tên/kênh/ngày niêm yết/trạng thái) + **nhật ký hoạt động riêng của variant đó** (mới `ActivityLogRepository.findByEntityTypeAndEntityCodeOrderByOccurredAtDesc` + `ActivityLogService.forEntity()` tách từ `list()` để tái dùng logic dịch nhãn/suy kênh có sẵn, không viết trùng). `ProductVariantController` thêm `GET /api/product-variants/{code}/detail`. Frontend: chuyển `ProductVariantPage.tsx` vào `pages/variant/`; `ProductVariantDetailPage.tsx` (mới, giàu nhất từ trước đến nay) gồm: banner + chip family/status, khối "Nguồn gốc sản phẩm" (4 ô Pattern→Template→Config→Variant nối mũi tên, click 3 ô đầu điều hướng sang trang chi tiết tương ứng đã có sẵn, ô Variant hiện tại highlight viền xanh), khối "Thông tin sản phẩm" (hạn mức/lãi suất/nội dung marketing nếu có), khối "Quy trình phát hành" (CTA điều hướng `/release/{code}` — TÁI DÙNG luôn màn Giai đoạn 32, không lặp logic 8 bước), 2 cột "Niêm yết Catalog" (card theo từng catalog, badge trạng thái, ngày niêm yết hoặc "Chưa có ngày niêm yết") và "Hoạt động gần đây" (timeline chấm tròn, actor/hành động/kênh/thời gian). `fadeUp` stagger toàn bộ card con. Route mới `/variant/:code`. Verify: curl `VAR-101` (published, đủ 3 catalog, 1 activity) và `VAR-107` (draft, 2 catalog chưa niêm yết ngày, 1 activity "Tạo") + 404 case; Playwright cả 2 trạng thái xác nhận giao diện đẹp, mượt, đầy đủ, không bịa gì. Trước đó **Giai đoạn 35 — thêm detail cho Block & Answer Slot**: user hỏi tiếp sau Giai đoạn 34, chỉ ra màn Block & Answer Slot cũng chưa có detail. Điểm khác biệt: backend **đã có sẵn từ Giai đoạn 6** — `BlockService.detail(id)`/`BlockController GET /api/blocks/{id}/detail` được xây trước (dự phòng cho việc gỡ fix cứng builder Pattern) đã trả đúng `{block, slots:[{code,name,type,required,def,rule,attrCode,attrName}]}` join answer_slot+attribute+data_type — **không cần sửa backend gì cả**, chỉ thiếu frontend. Frontend: chuyển `BlockPage.tsx` vào subfolder riêng (`pages/block/`) theo convention Giai đoạn 31; thêm `BlockDetailPage.tsx` (banner icon "block" + chip nhóm nghiệp vụ + StatusChip, stat card số Answer Slot + "chi phối bởi", danh sách Answer Slot dạng card có badge type/required + mặc định/ràng buộc, `fadeUp` stagger + hover elevate — cùng style với `LifecycleDetailPage`/`DomainDetailPage` Giai đoạn 34). Route mới `/block/:id`. Verify: curl `GET /api/blocks/BLK_ELIGIBILITY/detail` (3 slot: age/min_income/occupation đúng type Range/Money/Enum) + Playwright xác nhận UI đẹp, đúng dữ liệu. Trước đó **Giai đoạn 34 — thêm detail cho Lifecycle & State và Domain (UI mới ngoài prototype, user yêu cầu "giao diện phải mượt mà và đẹp mắt")**: sau Giai đoạn 29 đã xác nhận prototype gốc KHÔNG có detail cho 2 màn này (chỉ list), nhưng user hỏi ý tưởng rồi chốt làm cả 2. Backend: `LifecycleStateRepository.findByLifecycleCodeOrderBySortOrder` + `LifecycleService.detail(code)` (entity + states đúng thứ tự `sort_order`) + `LifecycleController` thêm `GET /api/lifecycles/{code}/detail`; `AttributeGroupRepository.findByDomainCodeOrderByName` + `DomainService` mới (join Attribute Group theo `domain_code` + đếm attribute từng group qua `AttributeRepository.countByGroupCode` có sẵn) + `DomainController` thêm `GET /api/domains/{code}/detail` (giữ nguyên list/byId read-only thuần cũ). Frontend: chuyển `LifecyclePage.tsx`/`DomainPage.tsx` vào subfolder riêng (`pages/lifecycle/`, `pages/domain/`) theo đúng convention Giai đoạn 31; thêm `LifecycleDetailPage.tsx` (chuỗi state machine dạng vòng tròn nối mũi tên, tô gradient xanh nhạt→đậm theo thứ tự — chỉ để phân biệt trực quan, KHÔNG mang nghĩa done/undone vì đây là định nghĩa state machine chứ không phải tiến độ 1 thực thể) và `DomainDetailPage.tsx` (stat card + danh sách Attribute Group dạng progress-bar theo số attribute); cả 2 dùng chung style banner gradient/back-button/StatusChip đã có ở `ArchetypeDetailPage`/`ReleasePage`, thêm hiệu ứng `fadeUp` stagger theo index + hover scale/elevate cho mượt. Route mới `/lifecycle/:code`, `/domain/:code` đặt trước `/:view`. Verify: curl `GET /api/lifecycles/{code}/detail` (7 state Term Loan: Draft→...→Closed) và `GET /api/domains/{code}/detail` (Pricing: 2 group/5 attribute) đúng thật + 404 khi mã không tồn tại; Playwright xác nhận UI mượt, đúng dữ liệu. Trước đó **Giai đoạn 33 — bổ sung seed `activity_log` cho đủ dày**: user hỏi màn Nhật ký hoạt động có đang fix cứng không. Audit kỹ (frontend/backend/DB) xác nhận **không có gì fix cứng** — mọi field đều lấy từ DB thật (channel suy ra bằng regex trên cột `detail` thật, nhãn hành động dịch từ code thật có fallback, COUNT thật không còn số bịa "1.284" từ Giai đoạn 18). Vấn đề thật sự chỉ là **quy mô**: seed gốc chỉ có 8 dòng. User chọn bổ sung thêm — đã thêm 20 dòng `activity_log` mới (tổng 28), mỗi dòng là sự kiện tạo/gửi duyệt/phê duyệt/xuất bản/thu hồi khớp ĐÚNG trạng thái thật hiện có của entity đó (business_intent/product_intent/product_pattern/product_template/product_config/product_variant) — không bịa entity hay trạng thái mới, không đổi code (chỉ sửa `V2__seed.sql`). Cần `docker compose down -v && up --build` để Flyway chạy lại seed mới. Verify: `GET /api/activity-logs` trả `totalElements:28`, Playwright xác nhận list hiển thị đa dạng actor/hành động/đối tượng/kênh, đều là dữ liệu thật. Trước đó **Giai đoạn 32 — liên kết Catalog ↔ Quy trình phát hành theo trạng thái sản phẩm**: user yêu cầu bấm 1 sản phẩm trong Catalog phải chuyển sang màn Quy trình phát hành và hiển thị đúng tiến độ 8 bước khớp trạng thái sản phẩm đó (draft/review/approved/published); mặc định vào từ sidebar phải hiển thị done 8 bước. Phát hiện bug thật: DB chỉ có 1 dòng `maker_checker_process` (cho VAR-101) với `done_count=4` — lệch với trạng thái thật của VAR-101 (`published`, đáng lẽ done 8/8). Quyết định (đã hỏi user): **không** seed thêm dòng `maker_checker_process` cho từng variant (tránh lặp lại đúng lỗi lệch dữ liệu) — thay vào đó dùng đúng 1 dòng hiện có làm **process template** (8 bước title/role/input/output/checklist chuẩn, không đổi theo sản phẩm) và **tính runtime** done/current/upcoming từ `product_variant.status` thật (draft→5/8, review→6/8, approved→7/8, published/retired→8/8; bước 7="Phê duyệt", bước 8="Đóng gói & Phát hành"). Backend: `ReleaseProcessService.detailByVariant(variantCode)` thay `detail(id)`, `MakerCheckerProcessRepository.findFirstByOrderById()`, controller đổi `/{id}/detail`→`/{variantCode}/detail`, xóa `list()` không dùng nữa. Frontend: `ProductCatalogPage` card thêm `onClick` điều hướng `/release/{variantCode}`; route mới `/release/:variantCode`; `ReleasePage` bỏ bước lấy danh sách rồi lấy dòng đầu, gọi thẳng theo variantCode (mặc định `VAR-101` khi vào từ sidebar). Verify bằng curl 4 trạng thái + Playwright click từ Catalog. Trước đó **Giai đoạn 30 — popup xem nhanh cho tab Attribute Group/Data Type**: sau Giai đoạn 29, user hỏi tiếp về 2 tab còn lại trong màn Attribute. Đối chiếu prototype gốc phát hiện cả 2 tab ĐỀU có `onClick` (`openCreate('attribute')`) — nhưng đó là mở modal CUD "Tạo Attribute mới" dùng chung với nút góc phải (không truyền id riêng theo dòng), và modal CUD này chưa từng được cài đặt cho bất kỳ màn nào trong dự án (đúng luật "Nút CUD: no-op"). Hỏi lại user: muốn dựng modal tạo/sửa thật hay chỉ xem thông tin — user chọn **chỉ xem, không có tác động gì**. Kết quả: thêm `InfoModal` (component popup dùng chung, không phải CUD) trong `AttributePage.tsx` — bấm dòng Attribute Group hoặc Data Type mở popup hiển thị thông tin nhóm/kiểu dữ liệu + danh sách attribute liên quan (lọc từ chính danh sách Attribute đã fetch sẵn, KHÔNG gọi thêm API mới, không bịa). Đây là UI mới không có trong prototype gốc (bản gốc chỉ có modal CUD) nhưng đúng yêu cầu user, hoàn toàn dữ liệu thật. Trước đó **Giai đoạn 29 — hoàn thiện click-through list→detail + màn Attribute Usage**: user yêu cầu rà mọi màn list xem bấm vào có sang detail không, so với prototype gốc để biết thiếu gì. Khảo sát (đọc state `view`/`isList`/các setter mang id như `openArchDetail`/`openAttrUsage` trong `docs/Product Factory 5.1.html`) xác nhận: prototype gốc CHỈ có đúng 2 khu vực có detail theo bản ghi — `archetype→archetypeDetail` (đã xong từ trước) và `attribute→attrUsage` (lineage, còn thiếu — chính là nợ 5.4 cũ). `pattern`/`template` có "builder" và `config` có "configForm" nhưng dùng chung 1 view cho mọi dòng (không truyền id qua state) nên KHÔNG tính là detail thật; 13 nav còn lại (variant/catalog/obligation/block/matrix/lifecycle/domain/activity/dashboard/ontology/sysmap/release/simulation) không có khái niệm detail trong bản gốc — giữ nguyên list-only, không tự vẽ thêm (đúng tinh thần pixel-perfect, không bịa). User chọn scope: chỉ làm Attribute Usage. Đã bổ sung backend (`AttributeEnumValue` entity mới, `TemplateFrameRepository`/`FragmentRepository` thêm method `findByBlockIdAndSlotCode`, `ProductVariantRepository.findByFromConfigCodeIn`, `AttributeUsageService`+`AttributeUsageController` mới — `GET /api/attributes/{code}/usage`) và frontend (`AttributeUsageDetailPage.tsx` trích đúng markup `attrUsageModel()`/`ATTR_USAGE()` từ prototype — rail chọn attribute, 5-stage lineage, constraint, Selector Scope values, where-used; route `/attribute/:code`; `AttributePage.tsx` tab Attribute thêm `onRowClick`). Verify bằng curl thật (`base_rate`: 2 constraint, 6 template, 7 config đa scope có 1 case warning, 7 variant; `occupation`: enum 4 giá trị, 0 fragment/variant — trả mảng rỗng gọn, không lỗi) + Playwright screenshot so khớp bố cục prototype. Trước đó (Giai đoạn 22) đã audit toàn diện sample data theo FK thật phát hiện 5 lỗ hổng dữ liệu; trước nữa (Giai đoạn 21) viết lại Product Config theo đúng markup builder gốc sau khi user đối chiếu phát hiện bản rút gọn sai — bài học: **luôn trích đúng markup gốc, không tự rút gọn, và khi so sánh list-vs-detail với prototype phải đọc đúng state/setter mang id, đừng suy diễn qua tên view.** Chỉ còn: **5.3 loading/error states nhất quán + Docker hoàn thiện** (nợ polish cuối, chưa có yêu cầu mới nào khác).

## NỢ — LÀM CUỐI, KHÔNG XEN GIỮA (← ĐANG Ở ĐÂY)
5.3: loading/error states nhất quán; Docker hoàn thiện.

## ĐÃ XONG
dashboard, businessintent(list + detail/KPI, route `/businessintent/:id`), intent(list+detail), pattern(builder, đã WIRE về DB thật + **"Xem trước" thật, Giai đoạn 39** — overlay toàn màn hình `PatternPreviewModal.tsx`, không API mới; **Giai đoạn 55: nhãn "Obligation Type" giữ dạng đầy đủ "Obligation Type Family" (không rút gọn OTF như các màn khác)**), block(list + **detail route `/block/:id`, Giai đoạn 35** — Answer Slot thuộc block, backend `BlockService.detail` có sẵn từ Giai đoạn 6), matrix(**Giai đoạn 58: chỉ còn đúng 2 tab (FOA × Obligation Element, Obligation Element × Block), cả 2 đều derived — bỏ hẳn OET×OET và Pattern×Block-độ-phủ, không còn lưu `constraint_matrix`/`matrix_cell` nào; Giai đoạn 59: OE×Block chỉ hiện 5 Block thật sự có OE chi phối (bỏ 7 Block luôn-rỗng), foa_element bổ sung 2 OE thiếu + sửa 9 ô lệch theo tài liệu**), attribute(list 3-tab + backend `Domain`/`AttributeGroup`/`AttributeConstraint` + **detail "Attribute Usage" route `/attribute/:code`, Giai đoạn 29** — lineage Answer Slot→Template→Config→Variant, backend `AttributeUsageService`/`AttributeEnumValue` + **popup xem nhanh tab Group/Data Type, Giai đoạn 30** — `InfoModal` client-side, không API mới), obligation(list **4-tab, Giai đoạn 51** — thêm tab "Obligation Type (lõi)", join làm giàu ontology có sẵn; **Giai đoạn 51b: catalog Obligation Element sửa lại đúng 36 mã đóng theo Data Dictionary, đổi tên 13 mã + thêm 10 mã thiếu**; **Giai đoạn 52: tab "Element Type" gỡ 2 dòng thừa OET_NATURE/OET_LIFECYCLE, còn đúng 6 chiều chuẩn**; **Giai đoạn 52b: đổi nhãn "Element Type"→"OET" khắp nơi**; **+ detail route `/obligation-type/:code` cho tab OTF, Giai đoạn 53** — backend `ObligationTypeService.detail()` join Pattern dùng OTF + cấu trúc OT lõi/leg/OET; **Giai đoạn 53b: mỗi OE hiện kèm Block liên quan (nếu có) + layout dạng bảng 3 cột**; **Giai đoạn 54: nhãn "Obligation Type" rút gọn thành "OTF" khắp nơi, trừ màn Pattern**), archetype(card grid + detail, route `/archetype/:code`, **+ khối "Quy tắc kích hoạt OT lõi phụ trợ", Giai đoạn 51**), domain(list + **detail route `/domain/:code`, Giai đoạn 34** — Attribute Group thuộc domain kèm progress bar, backend `DomainService`), lifecycle(list, join stateCount + **detail route `/lifecycle/:code`, Giai đoạn 34** — chuỗi state machine theo `sort_order`, backend `LifecycleService.detail`), ontology(ER-chain + decomposition + vocab, không backend mới — tổng hợp client-side từ API đã có; **Giai đoạn 51: group theo Archetype thay Family, decomposition nhóm theo OT lõi**), sysmap(pipeline + foundations + bảng quan hệ, không backend mới), template(list + detail `/template/:code`, backend `pipeline.ProductTemplate`/`CustomerSegment`/`TemplateSegment`/`TemplateFrame`), config(builder Fragment+Resolution pixel-perfect `/config/:code`, backend `pipeline.ProductConfig`/`SelectorScope`/`Fragment` — VIẾT LẠI Giai đoạn 21), variant(list + **detail route `/variant/:code`, Giai đoạn 36** — lineage Pattern→Template→Config→Variant, niêm yết Catalog, hoạt động gần đây, CTA sang Quy trình phát hành, backend `pipeline.ProductVariant`/`ProductCatalog`/`CatalogListing` + `ProductVariantService.detail`), catalog(card grid, backend `pipeline.ProductCatalogController`), release(stepper 8 bước + swimlane, route `/release/:variantCode` — Giai đoạn 32, backend `release.MakerCheckerProcess`/`ProcessStep`/`ProcessStepChecklist` dùng làm template, tiến độ tính runtime từ `product_variant.status`), activity(list + **detail route `/activity/:id`, Giai đoạn 45** — banner theo hành động, liên kết sang entity nguồn, khối hoạt động liên quan tái dùng `ApprovalHistory`, backend `activity.ActivityLog` + `ActivityLogService.detail`), simulation(form + annuity engine thật, backend `simulation.SimulationEngine`, `POST /api/simulation/run` — nút DUY NHẤT gọi tính toán thật, không no-op). **`ListScreen` (dùng chung mọi màn list) nay có search + filter dropdown THẬT chạy client-side (`components/ListScreen.tsx`, không cần sửa page nào khác).** **Thanh tìm kiếm topbar (`components/GlobalSearch.tsx`, Giai đoạn 38) nay THẬT — quét 12 loại thực thể qua `GET /api/search`, không còn là input trang trí.** **Menu sidebar nay lọc theo role người dùng thật (Giai đoạn 42)** — bảng `app_user` + `UserContext.tsx` + dropdown "đổi vai trò" (demo, không đăng nhập thật) thay khối user hardcode cũ; **khối "Lịch sử duyệt" (`components/ApprovalHistory.tsx`)** ở Business Intent/Product Intent/Config/Pattern/Template detail (Giai đoạn 42-44) hiển thị đúng ai tạo/gửi duyệt/phê duyệt/xuất bản từ `activity_log` thật qua `GET /api/activity-logs/entity`, tái dùng lại (đổi nhãn qua prop `title`) làm khối "Hoạt động liên quan" ở detail Nhật ký hoạt động (Giai đoạn 45). TẤT CẢ 18 MÀN + Simulation Engine + Attribute Usage ĐÃ HOÀN TẤT. **Giai đoạn 56: nhãn "OTF" viết tắt trần đã spell-out thành "Obligation Type Family (OTF)" khắp Dashboard/Archetype/Matrix/Sysmap/Obligation Library/GlobalSearch/tables.ts (trừ cột bảng dữ liệu + câu văn xuôi).** **Giai đoạn 57: checklist Bước 2 Quy trình phát hành hết nhắc "Obligation Nature"/"Family" đã xoá, thay bằng đúng flow OT lõi Cốt lõi/Phụ trợ→OTF.** **Giai đoạn 58: màn Ma trận chỉ còn 2 ma trận (FOA×OE, OE×Block), banner độ phủ Pattern builder đổi sang đọc OE→Block thật thay vì ma trận OTF×Block fabricated cũ.** **Giai đoạn 59: sửa badge sidebar hiện sai 0, ma trận OE×Block bỏ 7 Block luôn-rỗng, foa_element bổ sung theo tài liệu (đã xác nhận không hardcode).** Chỉ còn 5.3 (loading/error, Docker).
