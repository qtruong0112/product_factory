# NEXT_WORK.md — Kế hoạch bước tiếp theo (Product Factory 5.1)

> **Bổ trợ `PROJECT_STATUS.md`.** Ghi QUYẾT ĐỊNH về thứ tự ưu tiên + đặc tả màn kế. Đọc cùng `PROJECT_STATUS.md` (nguồn sự thật về convention, kiến trúc, ghi chú kỹ thuật).
> **Quy tắc:** Code là chuẩn. MD lệch code → tin code. Giải nén zip mới nhất, quét code trước khi làm.

---

## 0. QUYẾT ĐỊNH ĐÃ CHỐT (ưu tiên) — ĐÃ ĐỔI THỨ TỰ

**Quyết định mới (user chốt):** làm **THƯ VIỆN NỀN TẢNG trước, PIPELINE SẢN PHẨM sau.**

Lý do: các màn Pipeline (pattern/template/config/variant/catalog) *đọc dữ liệu từ* thư viện nền tảng (`block`, `answer_slot`, `attribute`, `data_type`, `constraint_matrix`, obligation, archetype…). Dựng nền tảng trước → khi tới Pipeline, mọi thứ **join API thật, KHÔNG fix cứng dòng nào**. Builder Pattern hiện tại giữ nguyên (hình đã đúng); khi backend nền tảng xong thì wire nó chỉ còn **đổi nguồn data** — không phí công đã làm.

1. **VIỆC KẾ TIẾP = Thư viện nền tảng.** Bắt đầu **Block & Answer Slot (+ data_type)** — vừa là màn thư viện, vừa cấp nguồn để gỡ fix cứng builder Pattern. Thứ tự + đặc tả ở **mục 2**.
2. **Khi backend nền tảng đủ → WIRE builder Pattern về API thật** (chỉ đổi nguồn data, mục 2.7) → rồi **Pipeline** (template → config → variant → catalog), đều DB-driven (mục 3).
3. **NỢ (Business Intent detail+KPI, ListScreen interactive) → vẫn để đợt polish cuối, KHÔNG xen giữa.** Mục 5.

---

## 1. TIẾN ĐỘ HIỆN TẠI (snapshot — đã quét code)

Đã xong pixel-perfect + verify:
- **dashboard** — Layout + Dashboard.
- **businessintent** — **chỉ màn LIST**. *Chưa* detail/KPI (không có `BusinessIntentKpi`, không có `BusinessIntentDetailPage`, không có endpoint detail). → nợ (5.1).
- **intent (Product Intent)** — **list + detail + backend** đầy đủ.
- **pattern (Product Pattern)** — **backend + BUILDER pixel-perfect** ("Trình dựng Product Pattern" 3 cột), **đã WIRE về DB thật (Giai đoạn 13, mục 2.7)** — `patternBuilderData.ts` đã xóa, không còn fix cứng.
- **block (Block & Answer Slot)** — ✅ **XONG (Giai đoạn 6).** Backend package `structure`: `Block`/`AnswerSlot`(+`AnswerSlotId`)/`DataType` + repos; `BlockController` `/api/blocks` (list làm giàu `slotCount`/`gov` + `/{id}/detail` join answer_slot+attribute+data_type) + `DataTypeController` `/api/data-types`. Frontend `BlockPage` list 6 cột pixel-perfect (bỏ footer giả, hiện đủ 12 block thật). Build + render verify OK. `/{id}/detail` đã sẵn nguồn cho việc wire builder Pattern (2.7).
- **matrix (Ma trận ràng buộc)** — ✅ **XONG (Giai đoạn 7).** Backend package `governance`: `ConstraintMatrix`/`MatrixCell`(+`MatrixCellId`) + repos; `ConstraintMatrixController` `/api/constraint-matrices` (list + `/{id}/detail` grid join nhãn theo kind + `/pattern-coverage` phái sinh từ pattern OT × ma trận 3). Frontend `MatrixPage` 4-tab pixel-perfect (grid verdict tô màu, stats, legend). Verdict khớp seed 100%; nhãn cột/hàng dùng name thật DB (khác nhẹ nhãn rút gọn prototype); tab 4 phái sinh hiện đủ 6 pattern. Logic coverage tính ở BE — dùng lại cho 2.7.
- **attribute (list 3-tab)** — ✅ **XONG (Giai đoạn 8).** Backend package `attribute`: `Domain`/`AttributeGroup`/`AttributeConstraint` (entity mới); `AttributeController`/`AttributeGroupController` (mới) + `structure/DataTypeController` chuyển sang tự viết Page<Map> join làm giàu (dataTypeName, usedInSlots từ answer_slot, constraintSummary từ attribute_constraint, domainName, attributeCount). Frontend `AttributePage` 3-tab (Attribute/Attribute Group/Data Type) dùng chung `ListScreen` + tab bar trích style `MatrixPage`. Cột "Mô tả"/"Ví dụ giá trị" bị bỏ ở tab Group/Data Type (bảng DB không có cột nguồn). Build + render verify OK (mock data từ seed thật: 31 attribute/12 group/9 data type). **Detail "Attribute Usage" (lineage) — ✅ XONG (Giai đoạn 29)**, xem mục 5.4.

Backend đã có sẵn (đừng làm trùng):
- **Lớp I Ontology (9 bảng): backend XONG** (Giai đoạn 1) — obligation_element_type, obligation_element, financial_obligation_archetype, foa_element, obligation_family, obligation_type, obligation_type_composition, lifecycle, lifecycle_state. **Chỉ thiếu frontend pixel-perfect** (đang dùng DataTable tạm).
- **`attribute`**: entity/repo/controller đã có (`/api/attributes`, đã join làm giàu ở Giai đoạn 8). `domain`/`attribute_group`/`attribute_constraint` cũng đã có (Giai đoạn 8).

`attribute_enum_value` nay đã có entity (Giai đoạn 29, mục 5.4); `selector_scope`/`fragment` đã có backend từ Giai đoạn 15/21.

Zip mới nhất đã giao trước đó: `product-factory-phase7-matrix.zip` (từ giờ dùng git commit thay zip — xem CLAUDE.md).

nav key nhóm nền tảng (từ `nav.ts`): `obligation`, `ontology`, `sysmap`, `archetype`, `attribute`, `block`, `matrix`, `lifecycle`, `domain`.

---

## 2. VIỆC KẾ TIẾP: THƯ VIỆN NỀN TẢNG (thứ tự thực thi)

> Làm từng màn trọn vẹn (backend + frontend + build + render verify + zip). Xếp để đồng thời gỡ fix cứng builder Pattern sớm nhất.

### 2.1 Block & Answer Slot (+ data_type) — nav key `block` — ✅ XONG (Giai đoạn 6)
> Đã hoàn thành đúng đặc tả dưới đây. `Block`/`AnswerSlot`(+Id)/`DataType` package `structure`; `/api/blocks` (+`/{id}/detail`), `/api/data-types`; `BlockPage` list pixel-perfect. Backend `/{id}/detail` trả `{block, slots:[{code,name,type,required,def,rule,attrName,attrCode}]}` — nguồn thật để gỡ fix cứng builder ở 2.7. **Không có block detail page** (prototype click row = create no-op, không có màn chi tiết → không bịa). **Kế tiếp = 2.2 Ma trận ràng buộc.**


Backend **package `structure`** (Lớp II):
- **`Block`** (PK `id` varchar(40)): `code` varchar(60) UNIQUE, `name` varchar(160), `biz_group` `biz_group_enum` (Khởi tạo/Giá trị/Kích hoạt/Vận hành/Thu hồi), `governed_by_element_code` varchar(60) NULL, `governed_by_aspect` varchar(80) NULL, `status`. + `BlockRepository` + `BlockController` `/api/blocks` (+ `/{id}/detail` join answer slots).
- **`AnswerSlot`** (+`AnswerSlotId` composite `[block_id, code]`): `name` varchar(160), `attribute_code` varchar(60), `is_required` bool, `default_value` varchar(255) NULL, `rule_text` varchar(255) NULL. + `AnswerSlotRepository.findByBlockId(String)` (giữ thứ tự seed; nếu cần thứ tự ổn định thì `OrderByCode`).
- **`DataType`** (PK `code` varchar(20)): `name` varchar(60). + `DataTypeRepository`. (Map: DT_MONEY→Money, DT_PERCENT→Percent, DT_INT→Integer, DT_ENUM→Enum, DT_RANGE→Range, DT_BOOL→Boolean, DT_FORMULA→Formula, DT_REF→Reference, DT_TEXT→Text.)

Frontend: **trích markup màn `block` từ prototype** trước khi code (list các block + panel answer slot — có thể giống builder right-panel). `pages/BlockPage.tsx` (+ detail nếu prototype có). Hiển thị: block (name/biz_group/gov/status) + answer slots (name/type=data_type.name/attribute/required/default/rule). nav key `block`.

Seed thật đã có: 12 block (BLK_ELIGIBILITY…BLK_BILLING) + answer_slot đầy đủ (vd `('BLK_ELIGIBILITY','age','Độ tuổi','age',true,'18 – 60','MIN 18')`). Dùng làm mock render verify.

### 2.2 Ma trận ràng buộc — nav key `matrix` — ✅ XONG (Giai đoạn 7)
> Đã hoàn thành. `ConstraintMatrix`/`MatrixCell`(+Id) package `governance`; `/api/constraint-matrices` (list + `/{id}/detail` grid + `/pattern-coverage`); `MatrixPage` 4-tab pixel-perfect. Nhãn hàng/cột join theo kind (element/archetype/OET/obligation_type/block name). Tab 4 "Pattern × Block" phái sinh từ DB (OT của pattern × ma trận 3, rank na<pos<req) — **logic coverage này dùng lại cho 2.7**. Verdict khớp seed 100%. **Kế tiếp = 2.3 Attribute.**

<details><summary>Đặc tả gốc (đã thực hiện)</summary>


Backend **package `governance`** (Lớp IV):
- **`ConstraintMatrix`** (PK `id`): `kind` `matrix_kind_enum`, `title`, `description`. + repo `findFirstByKind(String)` / `findAll`.
- **`MatrixCell`** (+`MatrixCellId` composite `[matrix_id, row_code, col_code]`): `verdict` `matrix_verdict_enum` (req|pos|na…), `is_override` bool. + repo `findByMatrixId(Long)`.
- Controller `/api/constraint-matrices` (+ `/{id}/cells` hoặc `/{id}/detail` trả matrix + cells dạng grid).

Seed: 3 ma trận — id=1 ARCHETYPE_X_ELEMENT, id=2 ELEMENTTYPE_X_ELEMENTTYPE, id=3 **OBLIGATIONTYPE_X_BLOCK** (row=OT code, col=block_id, verdict req/pos/na). Đây là nguồn ma trận cho builder Pattern (2.7).

Frontend: trích markup màn `matrix`, dựng lưới ma trận (row × col, ô verdict tô màu). nav key `matrix`.
</details>

### 2.3 Attribute — nav key `attribute` — ✅ XONG (Giai đoạn 8, CHỈ MÀN LIST)
Đã hoàn thành màn list 3-tab (Attribute/Attribute Group/Data Type), backend `Domain`/`AttributeGroup`/`AttributeConstraint` mới + `AttributeController`/`AttributeGroupController`/`structure.DataTypeController` join làm giàu. **Đã hoãn** màn "Attribute Usage" (lineage Attribute→Answer Slot→Template→Config→Variant + Selector Scope + where-used) và modal tạo/sửa — cần dữ liệu từ Pipeline (`product_template`/`product_config`/`product_variant`) + `fragment`/`selector_scope` (Lớp II) chưa có backend. Ghi nợ ở mục 5.4. **Kế tiếp = 2.4 Obligation Library + Archetype.**

### 2.4 Obligation Library + Financial Obligation Archetype — nav key `obligation`, `archetype`
- **`obligation`** — ✅ **XONG (Giai đoạn 9).** List 3-tab (Obligation Type/Obligation Element/Element Type), backend 3 controller (`ObligationTypeController`/`ObligationElementController`/`ObligationElementTypeController`) chuyển từ `ReadOnlyController` sang tự viết join làm giàu (familyName/archetypeName/elementCount/elementTypeName/isIdentify). Không có tab riêng cho `obligation_family` (chỉ 1 cột) hay `obligation_type_composition` (chỉ dùng đếm). Không có detail — giống prototype (row click mở modal generic no-op).
- **`archetype`** — ✅ **XONG (Giai đoạn 10).** Card grid (3 card Term Loan/Revolving/Conditional Obligation) + trang detail riêng thật sự (route `/archetype/:code`). Backend `FinancialObligationArchetypeController` join làm giàu: card có typeCount/elementCount/productCount (productCount = số pattern khác nhau qua `pattern_obligation_type`, join 2 chặng archetype→obligation_type→pattern_obligation_type). Detail có elementRows (foa_element+obligation_element+obligation_element_type, giữ đủ 3 trạng thái requirement req/pos/na) + typeRows (obligation_type theo archetype, kèm productCount riêng từng type). Bảng `financial_obligation_archetype` không có cột status — đã bỏ khỏi UI (không bịa).

### 2.5 Domain + Lifecycle & State — nav key `domain`, `lifecycle` — ✅ XONG (Giai đoạn 11)
Cả 2 đều là list đơn giản (không tab, không detail — prototype xác nhận `onClick` row chỉ mở modal generic). `lifecycle`: backend `LifecycleController` chuyển sang join làm giàu `stateCount` (đếm `lifecycle_state`). `domain`: `DomainController` mới, read-only thuần (entity `Domain` đã có từ Giai đoạn 8, cột `entity_count` là cột thật không cần đếm). Frontend `DomainPage`/`LifecyclePage` pixel-perfect. **Kế tiếp = 2.6 Ontology + Sysmap.**

### 2.6 Sơ đồ Ontology + Sơ đồ quan hệ tổng thể — nav key `ontology`, `sysmap` — ✅ XONG (Giai đoạn 12, HOÀN TẤT NHÓM NỀN TẢNG)
Khảo sát prototype xác nhận: KHÔNG nằm trong `isList` (cờ riêng `isOntology`/`isSysMap`) nhưng cũng KHÔNG phải SVG/canvas — cả hai là layout card/flex/grid tĩnh mô phỏng sơ đồ bằng box + icon mũi tên. Không cần backend mới, tổng hợp client-side từ API Lớp I đã có (obligation-types/families/compositions/elements/element-types cho `ontology`; cấu trúc/điều hướng tĩnh cho `sysmap`). Frontend `OntologyPage.tsx` (chuỗi ER 4 khái niệm + family/type selector + decomposition + vocab accordion) và `SysmapPage.tsx` (7 pipeline + 7 foundation + bảng 15 quan hệ FK thật). **Kế tiếp = 2.7 Wire builder Pattern về DB.**

### 2.7 WIRE builder Pattern về DB thật — ✅ XONG (Giai đoạn 13)
`ProductPatternController#GET /{code}/detail` mở rộng đúng shape đã đặc tả (assignedOTs kèm archetype, blocks join block+answer_slot+attribute+data_type, coverage tính ở BE tái dùng logic `ConstraintMatrixController#patternCoverage` thu hẹp về 1 pattern). Frontend `ProductPatternDetailPage` đọc thẳng field mới; **đã xóa hẳn `patternBuilderData.ts`** (không giữ lại phần nào — toàn bộ đều có nguồn DB thật). Coverage chỉ tính 1 nơi (BE); FE chỉ map verdict/inCanvas → nhãn/màu. Render verify bằng Playwright (mock đúng shape mới từ seed thật PT-001, cố ý thiếu 2 block để test nhánh "Tùy chọn"/"THIẾU") khớp bản cũ. **Kế tiếp = mục 3, Pipeline sản phẩm (Product Template).**

---

## 3. SAU NỀN TẢNG: PIPELINE SẢN PHẨM (DB-driven)

Thứ tự: **template → config → variant → catalog.** Mỗi màn: backend `pipeline/` + frontend pixel-perfect, **join hết vào thư viện nền tảng (API thật), không fix cứng.**

### 3.1 Product Template — nav key `template` — ✅ XONG (Giai đoạn 14)
Khảo sát prototype xác nhận **KHÔNG phải builder**: nav gọi `this.go('template')` (route list thường), khác `pattern` gọi `this.openBuilder('pattern')`. Click 1 dòng gọi `openTplWizard` nhưng hàm này luôn reset `tplStep:0` và không truyền id dòng nào — đây là wizard **TẠO MỚI chung** (state demo tĩnh `TPL_BLOCKS`/`tp.locked`/`tp.frames`), không phải xem chi tiết dòng đã click → cùng bản chất "modal generic no-op" như Obligation/Domain/Lifecycle. Quyết định: **chỉ dựng LIST**, không dựng wizard.

Backend package `pipeline`: `ProductTemplate` (PK `code`, `from_pattern_code`→product_pattern, `status`) + `CustomerSegment` (PK `code`, `name`, `audience`, `tier` nullable, `legal_requirement` nullable) + `TemplateSegment` (+`TemplateSegmentId` composite `[template_code, segment_code]`). `ProductTemplateController` (`/api/product-templates`, tự viết vì cần join) trả `{code,name,fromPatternCode,patternName,segmentCode,segmentName,status}`. Cột "CẬP NHẬT" của prototype **bỏ** (không có cột nguồn). Chưa tạo `TemplateFrame` entity (bảng `template_frame`) — không cần cho list, để dành nếu sau này cần.

Frontend `ProductTemplatePage.tsx` (list đơn giản, mẫu `DomainPage`): 5 cột Mã/Tên Template/Pattern nguồn/Đối tượng KH/Trạng thái, `filters=['Pattern nguồn','Đối tượng']`, không `onRowClick`.

Seed: TPL-001..006 (TPL-001/002←PT-001, TPL-003←PT-002, TPL-004←PT-006, TPL-005←PT-003, TPL-006←PT-005). Build + render verify OK (mock 6 dòng seed thật).

**Bổ sung — Product Template Detail (route `/template/:code`):** user chỉ ra prototype có wizard "Tạo Product Template" 3 bước (đối tượng KH → khóa Block → giá trị khung Answer Slot) và hỏi đã dựng chưa. Đọc lại bundler JS xác nhận `TPL_BLOCKS`/`state.tpl` là dữ liệu TĨNH 100% (không đổi theo dòng click) → quyết định (AskUserQuestion, chọn "Suy ra từ template_frame"): KHÔNG dựng wizard tạo-mới, mà dựng `/{code}/detail` = XEM 1 template thật, tái dùng layout 3 bước:
- Bước 1 (đối tượng KH): thật 100% (`template_segment`→`customer_segment`).
- Bước 2 (Block áp dụng): không có cột DB "khóa" theo template (đã kiểm cả `pattern_block.usage` — đó là field khác, ở cấp Pattern, seed toàn `'active'`) → suy ra active = block có ≥1 dòng `template_frame` cho template này.
- Bước 3 (giá trị khung): thật 100% từ `template_frame`, chỉ hiện block active; slot chưa có giá trị hiện "— chưa đặt —" (không dùng default tĩnh của prototype).

Backend: `TemplateFrame`(+`TemplateFrameId`) entity mới + mở rộng `ProductTemplateController#GET /{code}/detail` join `pattern_block`+`structure.Block`/`AnswerSlot`+`TemplateFrame`. Frontend `ProductTemplateDetailPage.tsx` (route đăng ký trước `/:view`), `ProductTemplatePage` thêm `onRowClick`. Verified Playwright TPL-003 (6/9 block active, giá trị khung khớp seed thật).

### 3.2 Product Config — nav key `config` — ✅ XONG (Giai đoạn 15, list + detail) → **VIẾT LẠI (Giai đoạn 21)**
Bản Giai đoạn 15 ban đầu dựng list+detail đơn giản (trái: slot có fragment; phải: card theo bối cảnh) — nhưng đây là RÚT GỌN sai so với prototype thật: `isConfigForm` (bundler dòng 686-837) là builder 3 cột đầy đủ ("Cấu hình Product Config") gồm sidebar cây Block/Answer Slot theo tỉ lệ bắt buộc, 2 banner (Answer Slot bắt buộc thiếu + cảnh báo ràng buộc Attribute), card chi tiết slot, form "Thêm Config Fragment", bảng fragment, và panel **"Xem trước Resolution"** (chọn People/Place/Time → tính fragment thắng theo ưu tiên). User đối chiếu trực tiếp file prototype gốc và yêu cầu sửa giống 100% + backend map đúng — xem Giai đoạn 21 (mục 4 PROJECT_STATUS) để biết đầy đủ: sửa lỗi seed `from_template_code` CFG-0042 (TPL-001→TPL-003), bổ sung 3 fragment mặc định thiếu (interest_calc/capacity_range/asset_valuation, dùng đúng `default_value` có sẵn), viết lại toàn bộ `ProductConfigController#detail` + `ProductConfigDetailPage.tsx`. **Kế tiếp = mục 3.3, Product Variant** (không đổi, đã xong từ trước).

### 3.3 Product Variant — nav key `variant` — ✅ XONG (Giai đoạn 16, list only)
Khảo sát bundler JS xác nhận `variant` nằm trong `isList`, click dòng chỉ gọi `this.openCreate('variant')` chung (drawer tạo-mới tĩnh, không mang id) → chỉ dựng LIST, không có wizard/detail thật (giống Domain/Lifecycle/Obligation). Cột "Kênh" (App/Web/PGD) của prototype **không cần bỏ** — suy ra thật từ `catalog_listing.variant_code → product_catalog.channel` (distinct, join " · "; variant chưa niêm yết catalog nào hiện "—"). Backend package `pipeline`: `ProductVariant`(PK code, name, fromConfigCode, family/limitRange/displayRate/marketingContent nullable, status) + `ProductCatalog`(PK id auto, name, channel) + `CatalogListing`(+`CatalogListingId` composite `[catalog_id,variant_code]`, publishedDate nullable, status) — 2 entity sau tái dùng cho mục 3.4. `ProductVariantController` list `{code,name,fromConfigCode,configName,limitRange,displayRate,channels,status}`. Frontend `ProductVariantPage` (list đơn giản, mẫu `DomainPage`), không `onRowClick`. Verified Playwright (7 dòng variant thật, cột Kênh khớp tay tính từ seed `catalog_listing`). **Kế tiếp = mục 3.4, Product Catalog** (entity `ProductCatalog`/`CatalogListing` đã có sẵn).

### 3.4 Product Catalog — nav key `catalog` — ✅ XONG (Giai đoạn 17, card grid, HOÀN TẤT PIPELINE SẢN PHẨM)
Khảo sát bundler JS xác nhận `catalog` là **CARD GRID riêng** (`isCatalog`, KHÔNG nằm trong `isList`), không có wizard/detail nào (0 kết quả grep `catalogForm`/`openBuilder('catalog')`). Hàm `catalog()` tĩnh của prototype dựng 6 card từ field {name,variant,family,limit,rate,statusLabel,channels[]} — thực chất là join `product_variant` (family/limitRange/displayRate/status) với `catalog_listing→product_catalog.channel`, không phải cột riêng của `product_catalog`/`catalog_listing`. Không có "3 kệ App/Web/PGD" lồng nhau — chỉ 1 lưới phẳng, mỗi card = 1 variant đã niêm yết.

Không cần entity mới (`ProductCatalog`/`CatalogListing` đã dựng ở Giai đoạn 16 khi làm Variant). `ProductCatalogController` (`/api/product-catalogs`) lặp mọi `ProductVariant`, bỏ qua variant chưa niêm yết catalog nào (khớp đúng 6 card của prototype — VAR-106 không niêm yết nên không xuất hiện, không phải thiếu sót); mỗi card `{variantCode,name,family,limitRange,displayRate,channels,status}`.

Frontend `ProductCatalogPage.tsx` (card grid, mẫu `ArchetypePage`): header gradient cố định (chrome), 2 cột Hạn mức/Lãi suất, chip Family, dòng cuối Kênh + `StatusChip`. Không `onClick` (không có detail thật). Verified Playwright khớp verbatim 6 card thật từ seed.

**NHÓM PIPELINE SẢN PHẨM (mục 3) CHÍNH THỨC HOÀN TẤT.**

---

## 3B. CÔNG CỤ/HỆ THỐNG: Release + Activity Log — nav key `release`, `activity` — ✅ XONG (Giai đoạn 18)

Khảo sát bundler JS xác nhận:
- **`release`** (`isRelease`) — KHÔNG phải list, là **stepper 8 bước** (`releaseSteps()`/`releaseData()`) + view phụ **Sơ đồ Swimlane**. Dữ liệu 8 bước trong bundler là hardcode nhưng khớp gần như y hệt seed thật `maker_checker_process`(1 dòng, done_count=4)/`process_step`(8 dòng: title/role/step_status/input_desc/output_desc)/`process_step_checklist`(24 dòng, is_done thật). `desc`/`tip`/`icon` không có cột DB — giữ làm hằng số UI `STEP_META` ở backend vì là copy mô tả tĩnh của quy trình chuẩn công ty (không đổi theo instance), không phải dữ liệu nghiệp vụ.
- **`activity`** (`isList`) — list đơn giản, 8 dòng hardcode khớp y hệt seed thật `activity_log` (8 dòng). Cột "KÊNH" không có cột DB riêng nhưng **suy ra được thật** từ hậu tố `"· kênh X"` luôn có trong `detail` (regex, khớp đúng 8/8 dòng seed) — không bịa. Footer "trên 1.284 hoạt động" của prototype là số bịa → bỏ, dùng COUNT thật (8). Cột "HÀNH ĐỘNG" của prototype là câu diễn giải ghép động từ+đối tượng — map lại `action` code (`create/update/approve/submit_review/publish/retire/assign/sync`) sang động từ tiếng Việt qua `ACTION_LABEL`. Click dòng nào cũng mở modal export tĩnh giống nhau (`openCreate('activity')`) — không phải detail thật → không làm `onRowClick`.

Backend:
- Package `release` (Lớp IV — Governance): `MakerCheckerProcess`(PK id, variantCode nullable, productName, doneCount) + `ProcessStep`(+`ProcessStepId` composite `[process_id, step_no]`: title, role, stepStatus, inputDesc/outputDesc nullable) + `ProcessStepChecklist`(+`ProcessStepChecklistId` composite `[process_id, step_no, sort_order]`: item, done). `ReleaseProcessController` (`/api/release-processes`, `/{id}/detail`) trả process + 8 step (kèm STEP_META tĩnh desc/tip/icon/nav) + checklist thật.
- Package `activity`: `ActivityLog`(PK id, occurredAt, actor, action, entityType, entityCode nullable, detail nullable). `ActivityLogController` (`/api/activity-logs`) parse `channel` bằng regex `"kênh\s+(\S+)"` trên `detail`, map `actionLabel`, format `occurredAtLabel` (`dd/MM HH:mm`).

Frontend:
- `ReleasePage.tsx`: banner gradient (product từ `maker_checker_process`), progress bar thật (`doneCount/totalSteps`), 2 tab (Hướng dẫn từng bước / Sơ đồ Swimlane). Stepper: timeline trái (8 bước, status thật từ `step_status`) + panel chi tiết phải (desc/input/output/checklist thật/tip, nút "Mở màn liên quan →" điều hướng React Router tới nav key thật nếu có — vd `/config`,`/catalog`; nút "Hoàn thành bước"/"Mở lại bước" giữ giao diện, no-op, tooltip "read-only"). Swimlane: CSS Grid (lane=vai trò × cột=bước, không dùng absolute-position canvas như prototype nhưng giữ đúng ý đồ hình — không rút gọn thành list thường), click node quay lại stepper.
- `ActivityPage.tsx`: dùng `ListScreen` chung, 5 cột Thời gian/Actor/Hành động/Đối tượng/Kênh, filters `['Actor','Loại','Kênh']`, actionLabel "Xuất nhật ký". Không `onRowClick`.
- `main.tsx`: thêm `release`/`activity` vào `CUSTOM` map (không cần Route mới — không có `/:id` trong URL).

Verified Playwright (mock đúng shape từ seed thật): stepper hiện đúng 4/8 done + bước 5 current + checklist đúng is_done seed; swimlane đúng lane/cột/màu trạng thái; activity list đúng 8/8 dòng, kênh Web/API khớp seed. **Kế tiếp = mục 3C, Simulation Engine.**

---

## 3C. Simulation Engine — nav key `simulation` — ✅ XONG (Giai đoạn 19, phần 10% CÓ TÍNH TOÁN)

Khảo sát bundler JS xác nhận `isSimulation` là **form tham số (trái) + panel kết quả realtime (phải)**, KHÔNG phải list. Bundler tính bằng JS thuần `simData()`/`annuity()` (không gọi API) nhưng công thức khớp chính xác seed thật (`simulation_scenario` id=1 CFG-0042/VAR-101: 30tr/18 tháng/1.5%/tháng → PMT 1.914.173đ, khớp seed `monthly_payment`).

**Quyết định kiến trúc:** đây là phần 10% DUY NHẤT có tính toán thật (CLAUDE.md mục 2, PROJECT_STATUS mục 2.2) — nút "Chạy mô phỏng" gọi THẬT `POST /api/simulation/run` (khác mọi nút CUD khác trong dự án luôn no-op). Endpoint này **không ghi DB** — chỉ nhận tham số, tính, trả kết quả.

Backend package mới `simulation` (Lớp IV):
- `SimulationScenario`/`SimulationScheduleRow`(+Id) — entity read-only chỉ dùng để nạp state ban đầu từ kịch bản mẫu thật (`GET /api/simulation/default` — 1 dòng scenario + 18 dòng schedule seed).
- `SimulationRequest` (DTO): amount/months/baseRatePct/assetValue/segmentCode/startDate/appraisalFee/periodicFeePct/graceMonths + 3 bộ toggle tình huống (penalty/prepay/early — không có cột lưu trong `simulation_scenario`, chỉ ảnh hưởng runtime, đúng bản chất "mô phỏng").
- `SimulationEngine` (thuần Java, không phụ thuộc Spring/DB): cổng lại công thức annuity của bundler — PMT dư nợ giảm dần, ân hạn (grace: kỳ chỉ trả lãi+phí, PMT tính lại trên số kỳ còn lại), trả bớt gốc (tái tính PMT phần dư), tất toán sớm (trả hết dư nợ + % phạt, dừng lịch), phạt trễ hạn (`PMT×(số ngày trễ/30)×lãi×1.5`). Điều chỉnh lãi theo tier (`standard`=0/`loyalty`=−0.5/`vip`=−0.3, sàn 0.3%/tháng) — khớp đúng tên hiển thị thật `customer_segment.name` ("Thân thiết (−0,5%/tháng)"), không bịa. **Tổng phải trả = lịch trả nợ + phí thẩm định 1 lần** — khớp đúng seed `total_payment` (35.400.634 = 34.900.634 + 500.000 phí thẩm định, phát hiện qua verify số không khớp lúc đầu, đã sửa).
- `SimulationController` (`/api/simulation`): `GET /default` (thật, đọc seed) + `POST /run` (tính, không ghi DB). Tra `tier` qua `pipeline.CustomerSegmentRepository` (tái dùng, không tạo entity trùng).

Frontend `pages/SimulationPage.tsx`: cột trái form (slider số tiền/kỳ hạn/lãi/tài sản/phí, chọn phân khúc, ngày giải ngân, 3 toggle tình huống có sub-input) + nút "Chạy mô phỏng" (gọi POST thật) + "Ghim phương án" (lưu snapshot local, tối đa 3, hiện bảng so sánh A/B/C/D); cột phải: 8 KPI card, khối "Kiểm tra ràng buộc" (verdict Hợp lệ/Không hợp lệ + 4 check), bảng lịch trả nợ chi tiết từng kỳ. Nút "Xuất CSV"/"Xuất PDF" giữ giao diện, no-op tooltip "read-only" (ngoài phạm vi tính toán cốt lõi).

Build 0 lỗi TS. Verified Playwright: mock JS annuity y hệt seed → khớp chính xác PMT 1.914.173đ/tổng lãi 4.455.122đ/tổng phải trả 35.400.634đ/LTV 66,67%/18 dòng lịch trả nợ; thử tăng số tiền vay lên 40tr → LTV nhảy lên 88,89% và verdict chuyển đúng "Không hợp lệ" (check LTV≤80% chuyển ✗). **Kế tiếp = đợt polish cuối (mục 5).**

---

## 4. QUY TRÌNH BẮT BUỘC (nhắc lại — mục 7 PROJECT_STATUS)

code → `cd frontend && npm run build` (0 lỗi TS) → render Playwright (mock API = seed thật, SPA fallback, dist tuyệt đối) → view PNG so khớp prototype → đóng **zip trọn vẹn** → cập nhật PROJECT_STATUS + NEXT_WORK. Backend không compile được ở sandbox → kiểm cú pháp/ngoặc/import thủ công. Dùng `ListScreen`, `StatusChip`, `Icon`, bảng màu chuẩn. **Màn builder/biểu đồ không rút gọn thành detail thường.**

---

## 5. HẠNG MỤC NỢ — LÀM SAU CÙNG (đợt polish, KHÔNG xen giữa)

### 5.1 Business Intent — detail + KPI — ✅ XONG (Giai đoạn 20)
Backend `BusinessIntentKpi`(+`BusinessIntentKpiId` composite `[business_intent_id, sort_order]`; cột `metric`,`target`,`unit`) + repo `findByBusinessIntentIdOrderBySortOrder` + `BusinessIntentController#/{id}/detail` trả `{intent, kpis}` (không extends thêm, chỉ thêm 1 `@GetMapping` cạnh `ReadOnlyController` sẵn có). Frontend `BusinessIntentDetailPage.tsx` (mẫu `ProductIntentDetailPage`, header gradient + info card + KPI card) + route `/businessintent/:id` (đăng ký trước `/:view`) + `BusinessIntentPage` thêm `onRowClick`. Không có markup prototype cho detail này (row click gốc chỉ mở modal "Tạo Business Intent" chung) — dựng mới theo đúng quyết định nợ đã chốt, dữ liệu 100% thật. Verified Playwright: BI-01 hiện đúng 3 KPI thật (Dư nợ giải ngân mới/Số hợp đồng/Tỷ lệ NPL, khớp seed).

### 5.2 ListScreen interactive — ✅ XONG (Giai đoạn 20)
`components/ListScreen.tsx` giờ tự lọc search + filter dropdown THẬT, hoàn toàn client-side, **không cần đổi signature `rows: ReactNode[][]` hay sửa bất kỳ page nào đang gọi nó** (khác phương án "đổi rows → {cells,raw,onClick}" từng cân nhắc — tránh phải sửa lại ~20 page). Cơ chế: `extractText(node)` đệ quy trích text từ cell React đã dựng (ưu tiên `props.children`; nếu là component tùy biến không có children — như `StatusChip`/chip label riêng — fallback đọc `props.status` (dịch qua `STATUS_LABELS` export từ `StatusChip.tsx`) hoặc `props.label`/`props.text`). Search: substring không phân biệt hoa/thường + bỏ dấu (NFD strip). Filter: `guessColumnIndex(filterLabel, columns)` đoán cột tương ứng bằng so khớp tên (label filter chứa/được chứa trong label cột) — không cần page khai báo thêm; dropdown liệt kê giá trị PHÂN BIỆT thật từ cột đó, chọn nhiều, có "Xóa lọc". Verified Playwright trên `BlockPage`: search "lãi" → còn đúng 1/4 dòng; filter "Trạng thái" chọn "Nháp" → còn đúng 1/4 dòng khớp.

### 5.3 Khác
Loading/error states nhất quán; đóng gói Docker cuối; (tùy chọn) kéo-thả THẬT trong builder — hiện read-only nên không cần.

### 5.4 Attribute Usage screen — ✅ XONG (Giai đoạn 29)
Màn chi tiết "Attribute Usage" (lineage Attribute→Answer Slot→Template→Config→Variant, Selector Scope values theo People/Place/Time, bảng where-used) — route `/attribute/:code`. Backend `AttributeEnumValue` entity mới + `TemplateFrameRepository`/`FragmentRepository.findByBlockIdAndSlotCode` + `ProductVariantRepository.findByFromConfigCodeIn` + `AttributeUsageService`/`AttributeUsageController` (`GET /api/attributes/{code}/usage`). Modal tạo/sửa Attribute KHÔNG làm — nút CUD toàn dự án là no-op theo quy ước chung (mục 7 PROJECT_STATUS), không có lý do làm ngoại lệ riêng cho Attribute. Trigger: user yêu cầu rà toàn bộ list→detail so với prototype gốc (Giai đoạn 29) — xác nhận đây là 1 trong 2 detail thật duy nhất còn thiếu (cùng với archetype đã xong từ trước).

---

## 6. LỘ TRÌNH 18 MÀN (thứ tự thực thi MỚI)

Đã xong: dashboard, businessintent(list), intent(list+detail), **pattern(builder, đã WIRE về DB thật — Giai đoạn 13)**, **block(list + backend structure)**, **matrix(4-tab grid + backend governance)**, **attribute(list 3-tab + backend Domain/AttributeGroup/AttributeConstraint)**, **obligation(list 3-tab, join làm giàu ontology có sẵn)**, **archetype(card grid + detail)**, **domain(list)**, **lifecycle(list, join stateCount)**, **ontology(ER-chain+decomposition+vocab)**, **sysmap(pipeline+foundations+relations)**, **template(list + detail /template/:code, backend pipeline.ProductTemplate/CustomerSegment/TemplateSegment/TemplateFrame)**, **config(builder Fragment+Resolution pixel-perfect /config/:code, backend pipeline.ProductConfig/SelectorScope/Fragment — VIẾT LẠI Giai đoạn 21)**, **variant(list, backend pipeline.ProductVariant/ProductCatalog/CatalogListing)**, **catalog(card grid, backend pipeline.ProductCatalogController)**, **release(stepper 8 bước + swimlane, backend release.MakerCheckerProcess/ProcessStep/ProcessStepChecklist)**, **activity(list, backend activity.ActivityLog)**, **simulation(form + annuity engine thật, backend simulation.SimulationEngine, POST /api/simulation/run)**. **TẤT CẢ 18 MÀN + SIMULATION ENGINE ĐÃ HOÀN TẤT — chỉ còn ĐỢT POLISH CUỐI (mục 5).**

**NỀN TẢNG trước:**
1. ✅ **block** (Block & Answer Slot + data_type) — XONG (Giai đoạn 6)
2. ✅ **matrix** (constraint_matrix + matrix_cell) — XONG (Giai đoạn 7)
3. ✅ **attribute** (list 3-tab; backend Domain/AttributeGroup/AttributeConstraint) — XONG (Giai đoạn 8). Detail "Attribute Usage" (lineage) — XONG (Giai đoạn 29, mục 5.4)
4. ✅ **obligation** (list 3-tab) — XONG (Giai đoạn 9). ✅ **archetype** (card grid + detail riêng) — XONG (Giai đoạn 10)
5. ✅ **domain** + **lifecycle** (list đơn giản) — XONG (Giai đoạn 11)
6. ✅ **ontology** + **sysmap** (biểu đồ, không backend mới) — XONG (Giai đoạn 12)
7. ✅ **WIRE builder Pattern về DB** (mục 2.7) — XONG (Giai đoạn 13). `patternBuilderData.ts` đã xóa.

**PIPELINE sau:**
8. ✅ **template** (list + detail, backend `pipeline.ProductTemplate`/`CustomerSegment`/`TemplateSegment`/`TemplateFrame`) — XONG (Giai đoạn 14). → 9. ✅ **config** (list + detail, backend `pipeline.ProductConfig`/`SelectorScope`/`Fragment`) — XONG (Giai đoạn 15). → 10. ✅ **variant** (list, backend `pipeline.ProductVariant`/`ProductCatalog`/`CatalogListing`) — XONG (Giai đoạn 16). → 11. ✅ **catalog** (card grid, backend `pipeline.ProductCatalogController`) — XONG (Giai đoạn 17). **PIPELINE SẢN PHẨM ĐÃ HOÀN TẤT.**

**CÔNG CỤ / HỆ THỐNG / CUỐI:**
12. ✅ **release** (stepper 8 bước + swimlane, backend `release.MakerCheckerProcess`/`ProcessStep`/`ProcessStepChecklist`) — XONG (Giai đoạn 18). ✅ **activity** (list, backend `activity.ActivityLog`) — XONG (Giai đoạn 18).
13. ✅ **simulation** (form tham số + annuity engine thật, backend `simulation.SimulationScenario`/`SimulationScheduleRow`/`SimulationEngine`, `POST /api/simulation/run`) — XONG (Giai đoạn 19). **TOÀN BỘ 18 MÀN + Simulation Engine ĐÃ HOÀN TẤT.**
14. **ĐỢT POLISH CUỐI:** ✅ **5.1 BI detail+KPI** — XONG (Giai đoạn 20). ✅ **5.2 ListScreen interactive** — XONG (Giai đoạn 20). ✅ **5.4 Attribute Usage** — XONG (Giai đoạn 29). ✅ **5.5 Detail cho Lifecycle & Domain** — XONG (Giai đoạn 34). ✅ **5.6 Detail cho Block & Answer Slot** — XONG (Giai đoạn 35). ✅ **5.7 Detail đầy đủ cho Product Variant** — XONG (Giai đoạn 36). ✅ **5.8 Chia tab con cho Variant detail** — XONG (Giai đoạn 37). ✅ **5.9 Thanh tìm kiếm topbar thật** — XONG (Giai đoạn 38). ✅ **5.10 Nút "Xem trước" Pattern thật** — XONG (Giai đoạn 39). ✅ **5.11 Audit/governance + sửa lệch lifecycle Config↔Variant** — XONG (Giai đoạn 40). ✅ **5.12 Sample data "Vay xe máy mùa tựu trường"** — XONG (Giai đoạn 41). ✅ **5.13 Nhật ký hoạt động theo audit + Menu theo Role** — XONG (Giai đoạn 42). ✅ **5.14 Vá "Lịch sử duyệt" trống trên toàn Pipeline** — XONG (Giai đoạn 43). ✅ **5.15 Lịch sử duyệt cho BI/PI + filter Hành động** — XONG (Giai đoạn 44). ✅ **5.16 Detail cho Nhật ký hoạt động** — XONG (Giai đoạn 45). ✅ **5.17 Tab Giá trị cấu hình ở Variant hiện đủ mọi Answer Slot** — XONG (Giai đoạn 46). ✅ **5.18 Simulation Engine bỏ hardcode, tính từ dữ liệu thật sản phẩm** — XONG (Giai đoạn 47). ✅ **5.19 Bổ sung Block "Hạn mức" vào Pattern PT-001** — XONG (Giai đoạn 48). ✅ **5.20 Sửa lệch ưu đãi VIP < Thân thiết trong Simulation** — XONG (Giai đoạn 49). ✅ **5.21 Sửa bug EMI trôi dần khi có phí quản lý + tính theo ngày thật** — XONG (Giai đoạn 50). ✅ **5.22 Tái cấu trúc Lõi Nghĩa Vụ Tài Chính theo tài liệu FOA/OET/OE/OT/OTF** — XONG (Giai đoạn 51). ✅ **5.23 Sửa catalog Obligation Element cho đúng Data Dictionary 36 mã đóng** — XONG (Giai đoạn 51b). ✅ **5.24 Bỏ "Element Type" thừa (gỡ OET_NATURE, OET_LIFECYCLE)** — XONG (Giai đoạn 52). ✅ **5.25 Đổi nhãn "Element Type" → "OET"** — XONG (Giai đoạn 52b). ✅ **5.26 Detail cho Obligation Type Family (OTF)** — XONG (Giai đoạn 53). ✅ **5.27 Liên kết OE→Block + layout cột cho detail OTF** — XONG (Giai đoạn 53b). ✅ **5.28 Nhất quán hoá nhãn "Obligation Type"→"OTF" (trừ Pattern)** — XONG (Giai đoạn 54). ✅ **5.29 Màn Pattern: đổi nhãn "Obligation Type"→"Obligation Type Family"** — XONG (Giai đoạn 55). ✅ **5.30 Spell-out "OTF"→"Obligation Type Family (OTF)" ở mọi nhãn/tiêu đề còn viết tắt trần** — XONG (Giai đoạn 56). ✅ **5.31 Cập nhật checklist Bước 2 Quy trình phát hành theo mô hình OTF mới** — XONG (Giai đoạn 57). ✅ **5.32 Màn Ma trận chỉ còn 2 ma trận: FOA × Obligation Element, Obligation Element × Block** — XONG (Giai đoạn 58). ✅ **5.33 Sửa badge sidebar/ma trận rỗng + bổ sung foa_element theo tài liệu** — XONG (Giai đoạn 59). ✅ **5.34 Làm đầy Block/Attribute còn thiếu trong 4 Pattern đã duyệt/xuất bản (BLK_VALUEBASE, BLK_DISBURSEMENT)** — XONG (Giai đoạn 60). ✅ **5.35 Làm giàu sample data cho Block/Attribute/Answer Slot + lan xuống Pattern/Template/Config** — XONG (Giai đoạn 61). ✅ **5.36 Vá 2 Config published (CFG-0041/CFG-0042) thiếu Fragment cho 6 slot bắt buộc** — XONG (Giai đoạn 62). ✅ **5.37 Mọi Attribute có "giá trị kèm theo" ngay từ khởi tạo, đối soát Template/Fragment khớp catalog** — XONG (Giai đoạn 63). ✅ **5.38 Làm thật nút "Xuất PDF" ở màn Simulation Engine** — XONG (Giai đoạn 64). Còn lại: **5.3 loading/error states + Docker (← ĐANG TỚI)**.

> Vì làm nền tảng trước: khi tới màn thư viện Block/Ma trận/Attribute là dựng luôn cả backend + frontend; tới Pipeline chỉ còn join API thật. Không còn fix cứng.

---

*Cập nhật: ✅ **Làm thật nút "Xuất PDF" ở màn Simulation Engine** (Giai đoạn 64) — user yêu cầu hoàn thiện nốt nút "Xuất PDF" (trước đây no-op, cạnh nút "Xuất CSV" đã làm thật). Quyết định không thêm thư viện PDF mới — dùng cơ chế in gốc trình duyệt: dựng trang HTML in-được (summary kịch bản + bảng lịch trả nợ đầy đủ, cùng dữ liệu client-side với CSV export) rồi gọi `window.print()`, trình duyệt tự cho lưu "Save as PDF". Sửa `SimulationPage.tsx`: thêm `escapeHtml()` + `handleExportPdf()`, nút đổi từ tĩnh sang có `onClick`/`disabled` như nút CSV. Verified: `npm run build` 0 lỗi TS, `docker compose up -d --build frontend`, smoke test curl OK — CHƯA verify bằng trình duyệt thật (môi trường phiên này không có browser automation), khuyến nghị user tự bấm thử. Xem chi tiết PROJECT_STATUS.md Giai đoạn 64.*

*Cập nhật: ✅ **Mọi Attribute có "giá trị kèm theo" ngay từ khởi tạo, đối soát Template/Fragment khớp catalog** (Giai đoạn 63) — user muốn mọi Attribute có sẵn 1 danh mục giá trị (`attribute_enum_value`) để điền Template sau này chỉ cần chọn, không cần gõ tay. Kiểm tra: cơ chế này vốn chỉ áp dụng cho DT_ENUM, 3 Attribute DT_BOOL (Giai đoạn 61) chưa có — thêm Bật/Tắt cho cả 3. Đối chiếu sâu hơn: quét TOÀN BỘ template_frame/fragment/answer_slot.default_value với catalog, phát hiện 8 chỗ lệch thật (asset_type dạng ngắn "Xe máy" không khớp catalog dạng dài "Xe máy (TwoWheels)" ở 6 chỗ; occupation dùng "Không giới hạn" và fragment CFG-0021 dùng "Thiết bị điện tử (Laptop)" — cả 2 giá trị hợp lý nhưng ngoài catalog). Sửa: bổ sung catalog cho khớp giá trị thật đang dùng (không đổi ý nghĩa dữ liệu) + chuẩn hoá 6 chỗ rõ ràng viết tắt. Verified: seed đổi nên `down -v && up --build`; 3 câu SQL đối soát đều 0 dòng lệch (từ 7+1); `attributes/asset_type/usage` đúng 4 enumValues; completeness mọi Config không đổi; regression composition 246, list API 200 OK; `npm run build` 0 lỗi TS. Xem chi tiết PROJECT_STATUS.md Giai đoạn 63.*

*Cập nhật: ✅ **Vá 2 Config published thiếu Fragment cho 6 slot bắt buộc** (Giai đoạn 62) — user yêu cầu rà mọi Config trạng thái published/approved/retired xem còn slot chưa có giá trị, gợi ý "điền Template rồi tự fill". Audit xác nhận CFG-0041/CFG-0042 (cùng nguồn TPL-003/PT-002, cả 2 published) thiếu Fragment cho 6 slot bắt buộc (age/min_income/compliance/legal_form/rate_type/schedule) dù Template đã có đủ khung — kiểm chứng giả thuyết user: SAI MỘT PHẦN, vì `ProductConfigService` chỉ tính "đã điền" khi có Fragment thật, template_frame chỉ hiển thị tham khảo (đúng hành vi từ Giai đoạn 48/60). Thêm 12 dòng fragment khớp đúng giá trị template_frame có sẵn. Verified: seed đổi nên `down -v && up --build`; CFG-0041/CFG-0042 lên 100% (18/18), 4 Config còn lại (CFG-0021/0039/0040/0043) không đổi; regression composition 246, list API 200 OK; `npm run build` 0 lỗi TS. Xem chi tiết PROJECT_STATUS.md Giai đoạn 62.*

*Cập nhật: ✅ **Làm giàu sample data cho Block/Attribute/Answer Slot + lan xuống Pattern/Template/Config** (Giai đoạn 61) — user muốn thêm sample data cho Block/Attribute/Answer Slot và lan xuống Pattern/Config/Template. Hỏi qua AskUserQuestion 2 quyết định phạm vi, cả 2 chọn phương án khuyến nghị: (1) không tạo Block/Attribute Group mới, chỉ làm giàu 12 Block đã có (đang chỉ 2-3 Answer Slot/Block); (2) làm giàu chiều sâu 6 Pattern/6 Template/8 Config hiện tại, không tạo sản phẩm mẫu mới. Thêm đúng 1 Answer Slot mới/Block (12 tổng, tái dùng group_code có sẵn, is_required=false để không phá completeness) — vd BLK_INTEREST→`reference_index` (kèm 1 attribute_constraint dependency đúng nguyên văn ví dụ tài liệu "Lớp vỏ thương mại": "Rate_type='Thả nổi' → bắt buộc có Reference Index"), BLK_COLLATERAL→`insurance_required`, BLK_REPAYMENT→`repay_channel`... Điền 52 dòng `template_frame` cho 6 Template (không thêm fragment — đúng hành vi đã thiết lập cho slot optional từ trước), giá trị chọn hợp bối cảnh từng Template (TPL-002 doanh nghiệp → dispute_resolution="Trọng tài thương mại"; TPL-002/004 ô tô → insurance_required="Bật"; TPL-006 tín chấp lương → co_borrower_allowed="Bật"+repay_channel="Trích nợ tự động"...). Verified: seed đổi nên `down -v && up --build`; mỗi Block +1 slot (31→43 Answer Slot/Attribute); completeness mọi Config không đổi so với Giai đoạn 60 (slot mới toàn optional); `attributes/reference_index/usage` đúng 1 constraint dependency; regression composition 246, coverage 4 Pattern vẫn đủ, 2 ma trận không đổi, list API 200 OK; `npm run build` 0 lỗi TS. Xem chi tiết PROJECT_STATUS.md Giai đoạn 61.*

*Cập nhật: ✅ **Làm đầy Block/Attribute còn thiếu trong 4 Pattern đã duyệt/xuất bản** (Giai đoạn 60) — user yêu cầu rà các Pattern `approved`/`published` (PT-001/003/005/006), kiểm tra Block nào thiếu so với ma trận OE×Block derived (Giai đoạn 58/59) rồi bổ sung, kéo theo Answer Slot/Attribute và điền giá trị. Phát hiện thiếu BLK_VALUEBASE ở cả 4 Pattern + BLK_DISBURSEMENT ở 3/4 (PT-003 đã có sẵn) — Attribute/Answer Slot của 2 Block này đã tồn tại sẵn trong schema, chỉ chưa Pattern nào dùng tới. Thêm 7 dòng `pattern_block`; `template_frame` cho 5 Template liên quan (TPL-001/002/004/005/006); phát hiện qua verify chỉ `template_frame` KHÔNG đủ để completeness lên 100% (`ProductConfigService` chỉ đếm slot có Fragment thật là "đã điền", đúng hành vi từ Giai đoạn 48) nên thêm tiếp `fragment` scope=default cho 6 Config liên quan (CFG-0021/0037/0038/0039/0040/0043). Tiện thể vá 1 lỗ hổng sót từ Giai đoạn 48: CFG-0037/TPL-002 thiếu hẳn fragment BLK_LIMIT dù Block đã có từ lâu. Verified: seed đổi nên `down -v && up --build`; cả 4 Pattern coverage 100% `inCanvas`; cả 6 Config `completeness.pct=100`; regression composition vẫn 246, list các entity vẫn 200 OK, 2 ma trận không đổi; `npm run build` 0 lỗi TS — hoàn toàn seed data, không đổi code Java/TSX. Xem chi tiết PROJECT_STATUS.md Giai đoạn 60.*

*Cập nhật: ✅ **Sửa badge sidebar/ma trận rỗng + bổ sung dữ liệu foa_element theo tài liệu** (Giai đoạn 59) — sau Giai đoạn 58, user hard-refresh phát hiện 2 lỗi: badge "Ma trận ràng buộc" hiện "0" (do `Layout.tsx` vẫn gọi endpoint `list()` đã xoá, lỗi 500 fallback về 0 — xoá fetch chết, đổi `nav.ts` count sang cố định "2"); ma trận OE×Block trông rỗng (rows đổi từ 12 Block xuống chỉ 5 Block có governed_by_element_code thật — 7 Block còn lại luôn "na" mọi cột nên gây loãng, không thêm thông tin). Sau đó user hỏi "matrix có hardcode không, sao OE nhiều mà lên matrix ít" — xác nhận qua `psql` trực tiếp KHÔNG hardcode, nhưng tìm ra 2 nguyên nhân: FOA×OE có 2 OE thiếu thật + 9 ô lệch giá trị so với tài liệu "Ví dụ xuyên suốt..." (tồn đọng từ audit trước, user xác nhận "có" sửa ngay); OE×Block chỉ 5/12 quan hệ vì tài liệu "Lớp vỏ thương mại" thuần là văn bản khái niệm, không có bảng ánh xạ đầy đủ nào để đối chiếu — không sửa thêm, tránh bịa dữ liệu. Sửa `foa_element`: thêm 9 ô thiếu cho 5 OE có sẵn + 2 OE hoàn toàn mới (`OE_VAL_PRINCIPAL_SINGLE_DECREASE`, `OE_REC_UNSECURED`) — 15→29 dòng, phủ 13→15 OE. Tiện thể xoá 2 card "Obligation Nature"/"Value Structure" ở Archetype detail theo yêu cầu riêng. Verified: seed đổi nên `down -v && up --build`; `foa-oe-matrix` giờ 15 dòng/45 ô (pos tăng 4→17, khớp sát tài liệu); `oe-block-matrix` đúng 5×5; regression composition vẫn 246, pattern list 200 OK, PT-001 coverage không đổi (nguồn khác); `npm run build` 0 lỗi TS. Xem chi tiết PROJECT_STATUS.md Giai đoạn 59.*

*Cập nhật: ✅ **Màn Ma trận chỉ còn đúng 2 ma trận: FOA × Obligation Element, Obligation Element × Block** (Giai đoạn 58) — user yêu cầu thẳng "ở màn ma trận giờ sẽ chỉ có OE x block và FOA x OE" — rút màn Ma trận (trước 4 tab: FOA×OE/OET×OET/OTF×Block/Pattern×Block-độ-phủ) xuống còn 2. Hỏi qua AskUserQuestion 2 quyết định: (1) bỏ hẳn "OET × OET" và "Pattern × Block (độ phủ)" khỏi màn Ma trận (chấp nhận banner độ phủ Pattern builder phải đổi nguồn); (2) ma trận OE×Block mới derive từ `block.governed_by_element_code` có sẵn, không tạo bảng/dữ liệu mới. Sửa: `ConstraintMatrixService` viết lại gọn còn 2 method derived (`foaOeMatrix()`, `oeBlockMatrix()` mới — rows=12 Block, cols=5 OE thực sự chi phối ít nhất 1 Block, cell req/na nhị phân) — bỏ hẳn `list()`/`detail(id)`/`patternCoverage()` (dead code); `ConstraintMatrixController` bỏ 3 endpoint chết, thêm `GET /oe-block-matrix`; `ProductPatternService.detail()` field `coverage` viết lại hoàn toàn — gom OE dùng trong composition của các OTF đã gán pattern, lọc Block nào được OE đó chi phối (độ dài coverage giờ thay đổi theo từng pattern thay vì cố định 6 Block cũ); `V2__seed.sql` xoá sạch seed `constraint_matrix`/`matrix_cell` (giữ bảng trong schema, không DROP); tiện thể sửa tiếp `ReleaseProcessService.TIP[2]` (vừa sửa ở Giai đoạn 57 thành "OTF × Block") sang "Obligation Element × Block" cho khớp ma trận mới. `MatrixPage.tsx` viết lại — 2 tab, gọi thẳng 2 endpoint cố định thay vì cơ chế list()+loop id cũ. **Hệ quả đáng chú ý:** độ phủ Pattern builder đổi hẳn ý nghĩa — PT-001 giờ hiện "Thiếu 2 Block" (BLK_VALUEBASE/BLK_DISBURSEMENT) thay vì "✓ Đủ" cũ, vì ma trận OTF×Block cũ là dữ liệu curated tay chưa từng kiểm tra đúng 2 Block này. Verified: seed đổi nên `docker compose down -v && up --build`; curl `oe-block-matrix` đúng 12×5, đúng 5 cặp req khớp Giai đoạn 53b; `product-patterns/PT-001|PT-002/detail` đúng coverage mới; regression `obligation-type-compositions` vẫn 246, `product-patterns` list vẫn 200; `npm run build` 0 lỗi TS. Xem chi tiết PROJECT_STATUS.md Giai đoạn 58.*

*Cập nhật: ✅ **Cập nhật checklist Bước 2 "Tạo Product Intent" ở Quy trình phát hành theo mô hình OTF mới** (Giai đoạn 57) — user rà màn Quy trình phát hành, phát hiện checklist Bước 2 vẫn hiện văn bản cũ từ trước Giai đoạn 51 ("Gán Obligation Nature định danh", "Khai báo Obligation Element nền", "Đối chiếu Ma trận FOA × Element") — nhắc khái niệm "Obligation Nature" đã bị xoá từ Giai đoạn 52. User mô tả bằng văn nói 3 việc thật của quy trình OTF hiện tại, yêu cầu viết lại cho chuẩn. Sửa `V2__seed.sql` (process_step_checklist, process_id=1/step_no=2) thành "Điền OE cho 3 OT lõi Cốt lõi (Giải ngân / Hoàn trả gốc / Trả lãi)", "Bật/tắt OT lõi Phụ trợ theo bảng kích hoạt", "Điền OE cho OT lõi Phụ trợ đã bật để hoàn thành OTF". Rà thêm phát hiện 2 chỗ cùng lỗi thời trong `ReleaseProcessService.java` (hằng số DESC/TIP tĩnh): DESC[1] nhắc "Obligation Nature", TIP[1] nhắc "Family" đã xoá — sửa lại theo đúng flow FOA→OE→OTF; tiện thể sửa TIP[2] (Bước 3) từ "Ma trận Obligation Type × Block" sang "Ma trận Obligation Type Family (OTF) × Block" khớp tên đã đổi ở Giai đoạn 54/56. Verified: seed đổi nên `docker compose down -v && up --build`; curl `GET /api/release-processes/VAR-101/detail` xác nhận checklist/desc/tip đúng văn bản mới; regression `obligation-type-compositions` vẫn 246 dòng. Xem chi tiết PROJECT_STATUS.md Giai đoạn 57.*

*Cập nhật: ✅ **Spell-out "OTF" → "Obligation Type Family (OTF)" ở mọi nhãn/tiêu đề còn viết tắt trần** (Giai đoạn 56) — sau Giai đoạn 55, user rà UI qua nhiều vòng ảnh chụp: 2 chỗ chữ hoa cứng bị sót ở màn Pattern (grep phân biệt hoa/thường không bắt được), tab "Nghĩa vụ tài chính" ở Variant detail (tái dùng data Pattern), stat card Sơ đồ Ontology, tab "OTF" ở Obligation Library. Hỏi qua AskUserQuestion phạm vi rộng hơn — user chọn "Sửa hết mọi chỗ". Sửa: `tables.ts`, `DashboardPage.tsx`, `ArchetypePage.tsx`/`ArchetypeDetailPage.tsx`, `SysmapPage.tsx`, `ObligationPage.tsx`, `ObligationTypeDetailPage.tsx`, `MatrixPage.tsx`, `GlobalSearch.tsx` (đồng bộ với backend vì `type` là chuỗi API trả thẳng), `ProductPatternDetailPage.tsx`, `ProductVariantDetailPage.tsx`. Backend: `ConstraintMatrixService.rowHeadOf()`, `GlobalSearchService`. Giữ ngắn: cột bảng dữ liệu + câu văn xuôi dùng "OTF" như từ viết tắt thông thường. Bài học: `frontend` chạy Docker qua nginx serve `dist/` sẵn — `npm run build` cục bộ không đủ, phải `docker compose up -d --build` mới thấy thay đổi. Verified: build backend+frontend 0 lỗi; curl xác nhận `rowHead`/`type` đổi đúng. Xem chi tiết PROJECT_STATUS.md Giai đoạn 56.*

*Cập nhật: ✅ **Màn Pattern: đổi nhãn "Obligation Type" → "Obligation Type Family"** (Giai đoạn 55) — ngay sau Giai đoạn 54 (đổi hết nhãn "Obligation Type"→"OTF" NHƯNG cố tình chừa riêng màn Pattern giữ nguyên "Obligation Type"/"OT"), user yêu cầu tiếp "ở màn Product Pattern có Obligation Type thì đổi thành Obligation Type Family" — màn Pattern dùng dạng đầy đủ "Obligation Type Family", không viết tắt "OTF" như phần còn lại của app. Sửa 5 chỗ: `ProductPatternPage.tsx` (cột list + filter dropdown cùng tên, sửa đồng thời tránh bug filter chết như Giai đoạn 44), `ProductPatternDetailPage.tsx` (banner cảnh báo độ phủ + tab palette), `PatternPreviewModal.tsx` (stat card + tiêu đề độ phủ ma trận). Chỉ đổi label hiển thị, không đổi biến/hằng số code. Verified: `npm run build` 0 lỗi TS. Xem chi tiết PROJECT_STATUS.md Giai đoạn 55.*

*Cập nhật: ✅ **Nhất quán hoá nhãn "Obligation Type" → "OTF" (trừ Pattern)** (Giai đoạn 54) — user rà lại UI 2 lượt: (1) "đổi tên thành OTF đi ở pattern vẫn để là ot" — muốn đổi hết nhãn "Obligation Type" còn sót sang OTF nhưng riêng màn Pattern giữ nguyên "OT"; (2) "Obligation Type đổi tên thành OTF đi Obligation Type Family" — rút gọn luôn dạng đầy đủ "Obligation Type Family (OTF)" thành "OTF". Hỏi qua AskUserQuestion xác nhận phạm vi (chọn "Đổi hết, trừ Pattern"). Sửa: Archetype (list+detail — StatCard, "OTF thuộc Archetype này"; riêng "Quy tắc kích hoạt OT lõi phụ trợ" KHÔNG đổi thành OTF vì đó là khái niệm OT lõi/`ot_activation_rule`, không phải OTF — sửa đúng thay vì áp máy móc), Matrix (tab "OTF × Block", và cả `constraint_matrix.title/description` seed cho ma trận 2/3 — sót từ Giai đoạn 52b), Dashboard (KPI), GlobalSearch (nhãn nhóm kết quả + path đổi sang `/obligation-type/{code}` — route detail mới từ Giai đoạn 53, không cần vòng qua Archetype cha nữa), `tables.ts`, `ObligationPage.tsx`/`OntologyPage.tsx`/`SysmapPage.tsx`. KHÔNG đụng `pages/pattern/*`/`PatternPreviewModal.tsx` theo đúng yêu cầu. Verified: `constraint-matrices` trả đúng title mới, `constraint-matrices/3/detail` → `rowHead:"OTF"`, `GET /api/search?q=vay` trả `type:"OTF"` + path đúng; grep xác nhận Pattern không bị đụng. Xem chi tiết PROJECT_STATUS.md Giai đoạn 54.*

---

*Cập nhật: ✅ **Liên kết OE→Block + layout dạng cột cho detail OTF** (Giai đoạn 53b) — ngay sau Giai đoạn 53, user hỏi "đọc trong các tài liệu thì OE sẽ có block ở trong đó đúng k". Giải mã thêm `docs/Lớp+vỏ+thương+mại.doc` xác nhận: KHÔNG phải "OE chứa Block" — Block là tầng kế tiếp (Lớp Tham số Định lượng, nhóm Attribute định lượng hóa OE), được OE "chi phối" qua cột có sẵn `block.governed_by_element_code`. Thêm `BlockRepository.findByGovernedByElementCode()`, `ObligationTypeService.detail()` trả kèm `blockId`/`blockName` cho mỗi OE (null nếu không có — chỉ 5/37 OE thật sự governs 1 Block). Frontend hiện chip Block bấm được dưới mỗi OE có Block. User phản hồi layout "hơi rối" — đổi khối "Cấu trúc OTF" từ lưới thẻ dồn dòng sang bảng 3 cột (OET/Obligation Element/Block) rõ ràng hơn. Verified: curl xác nhận đúng 5 OE có blockName (Value Base/Disbursement/Repayment/Collateral), còn lại null. Xem chi tiết PROJECT_STATUS.md Giai đoạn 53b.*

---

*Cập nhật: ✅ **Detail cho Obligation Type Family (OTF)** (Giai đoạn 53) — user yêu cầu thêm chức năng click detail cho tab OTF trong Obligation Library (trước chỉ list). Backend: `ObligationTypeCompositionRepository.findByObligationTypeCode()` mới + `ObligationTypeService.detail(code)` trả `{otf, archetypeName, patterns:[{patternCode,patternName,role}], otCores:[{otCoreCode,otCoreName,groupKind,leg,elements:[...]}]}` — `patterns` tái dùng `PatternObligationTypeRepository.findByObligationTypeCode()` có sẵn (dùng cho Archetype detail), `otCores` nhóm composition theo `(ot_core_code, leg)` — cùng logic OntologyPage đang tính client-side nhưng trả gọn cho 1 OTF thay vì kéo cả 246 dòng về. `GET /api/obligation-types/{code}/detail` mới. Frontend: chuyển `ObligationPage.tsx` vào `pages/obligation/` (đúng convention khi có detail sibling), tab OTF thêm `onRowClick`; `ObligationTypeDetailPage.tsx` mới (banner tím, 3 stat card, khối "Cấu trúc OTF" theo nhóm OT lõi/leg, khối "Product Pattern dùng OTF này" click sang `/pattern/:code`). Route `/obligation-type/:code`. Verified: curl `OT_PLEDGE_INSTALLMENT/detail` đúng 5 nhóm (Giải ngân/Hoàn trả gốc/Trả lãi/Bàn giao TS×2 leg) đủ 6 element mỗi nhóm, 2 pattern đúng role; `OT_UNSECURED/detail` đúng chỉ 3 nhóm (không Bàn giao TS); 404 khi mã không tồn tại. Xem chi tiết PROJECT_STATUS.md Giai đoạn 53.*

---

*Cập nhật: ✅ **Đổi nhãn "Element Type" → "OET"** (Giai đoạn 52b) — ngay sau Giai đoạn 52, user hỏi lại "sao vẫn còn tab tên là element type" — hỏi rõ qua AskUserQuestion thì hoá ra không phải còn dữ liệu thừa (đã đúng 6 dòng) mà là **tên nhãn**: tài liệu chỉ dùng "Obligation Element Type"/"OET", không có nơi nào dùng "Element Type" chung chung. Đổi: backend `ConstraintMatrixService.rowHeadOf()` (Matrix 2) trả "OET"; frontend tab Obligation Library, cột trong `ObligationPage.tsx`/`tables.ts`, 3 câu mô tả `OntologyPage.tsx`, nhãn tab `MatrixPage.tsx` — tất cả đổi "Element Type"→"OET"/"(OET)". Chỉ đổi nhãn hiển thị, không đổi dữ liệu/logic. Verified: curl `constraint-matrices/2/detail` trả đúng `rowHead:"OET"`. Xem chi tiết PROJECT_STATUS.md Giai đoạn 52b.*

---

*Cập nhật: ✅ **Bỏ "Element Type" thừa (gỡ OET_NATURE, OET_LIFECYCLE)** (Giai đoạn 52) — user gửi thêm tài liệu BA v1.0 mới (`docs/Tu_duy_Loi_Nghia_vu_Tai_chinh_v1.0.docx`, .docx thật), đối chiếu với Obligation Library và chỉ ra "cái element type đang bị thừa thãi". Xác nhận đúng: tab "Element Type" có 8 dòng thay vì đúng 6 chuẩn. `OET_NATURE` không phải 1 trong 6 OET — tài liệu mới (OI-4) nói thẳng đây là va chạm thuật ngữ, đề xuất gỡ, chuyển Nature về thuộc tính FOA; kiểm chứng DB xác nhận trùng lặp dữ liệu thật: `product_intent` có cả `archetype_code` VÀ `nature_element_code` cùng mã hoá 1 fact (2 cột/card UI cạnh nhau); `foa_element` có 3 dòng tautology làm nhiễu ma trận FOA×OE (16 hàng thay vì đúng 13); `product_intent_element` dư 5 dòng; 3 Block (`BLK_LIMIT`/`BLK_INTEREST`/`BLK_FEE`) governed_by mã nature giả (2 block cuối trùng y hệt 1 mã — gán qua loa, xác nhận field này chỉ dùng hiển thị). `OET_LIFECYCLE` còn thừa hơn — 0 dòng `obligation_element` nào thuộc chiều này, comment thừa nhận chỉ thêm cho tab có dòng. Hỏi user qua AskUserQuestion cách xử lý 3 Block governed-by — chọn chuyển sang `governed_by_aspect` = mã FOA thật (`FOA_REVOLVING`/`FOA_TERM_LOAN`), không bịa liên kết OE mới. Sửa `V2__seed.sql`/`V1__schema.sql`: xóa OET_NATURE/OET_LIFECYCLE + 3 element + 3 dòng foa_element tautology + cột `nature_element_code` + 5 dòng product_intent_element + co Matrix 2 (36→25 ô); backend `ProductIntent.java` bỏ field; frontend `ProductIntentPage.tsx`/`ProductIntentDetailPage.tsx` bỏ cột/card "Obligation nature" (Archetype đã hiển thị đúng fact). Ngoài phạm vi (để sau): OI-4 còn đề xuất đổi `OT_*`→`OTF_*`, chưa làm đợt này. Verified: `obligation-element-types` đúng 6 dòng, `obligation-elements` đúng 37 dòng, `foa-oe-matrix` đúng 13 hàng, `product-intents/{1..6}/detail` sạch natureElementCode, `blocks/BLK_LIMIT|INTEREST|FEE` đúng gov mới, regression `obligation-type-compositions` vẫn 246 dòng, `product-patterns/PT-001`/`archetypes/FOA_TERM_LOAN` vẫn 200 OK. Xem chi tiết PROJECT_STATUS.md Giai đoạn 52.*

---

*Cập nhật: ✅ **Sửa catalog Obligation Element cho đúng Data Dictionary 36 mã đóng** (Giai đoạn 51b) — ngay sau Giai đoạn 51, user báo "Obligation Library vẫn sai", yêu cầu đọc lại thật kỹ Data Dictionary và đối chiếu. Đã giải mã lại trực tiếp file MHTML gốc (không dựa trí nhớ) lấy chính xác Mục 4 (36 mã OE). Phát hiện: ở Giai đoạn 51 chỉ OET_PARTY được thêm đúng canonical, còn 13 mã ở 5 OET khác vẫn giữ tên cũ (`PRINCIPAL_NO_INCREASE_MULTI_DECREASE`...) thay vì đổi đúng tên tài liệu, thiếu 10 mã catalog, và `PAYMENT_BULLET` trùng nghĩa với `OE_FUL_LUMP_SUM` đã thêm. Hỏi user qua AskUserQuestion về 1 mã tự thêm ngoài catalog (`OE_ACT_SETTLEMENT_TRIGGER`, không mã nào trong 7 mã đóng OET_ACTIVATION khớp đúng "trigger tất toán") — user xác nhận Obligation Library chỉ gồm đúng 4 khái niệm OET/OT/OE/OTF (không đổi cấu trúc tab), giữ mã này làm phần mở rộng có ghi chú rõ. Đã grep xác nhận không Java/TSX nào hardcode các mã sắp đổi — sửa thuần `V2__seed.sql`: đổi tên 13 mã, gộp `PAYMENT_BULLET`, thêm 10 mã thiếu, áp dụng nhất quán ở `obligation_element`/`foa_element`/`obligation_type_composition`(246 dòng)/`block.governed_by_element_code`/`product_intent_element`/`ot_activation_rule`. Verified: `obligation-elements` đúng 40 dòng (36 canonical + 3 NATURE lịch sử + 1 mở rộng có ghi chú), `obligation-type-compositions` vẫn đúng 246 dòng không còn mã cũ, regression block/product-intent/archetype/pattern/matrix đều đúng. Xem chi tiết PROJECT_STATUS.md Giai đoạn 51b.*

---

*Cập nhật: ✅ **Tái cấu trúc Lõi Nghĩa Vụ Tài Chính theo tài liệu FOA/OET/OE/OT/OTF** (Giai đoạn 51) — user bổ sung 4 file thiết kế mới vào `docs/` (thực chất MHTML/Confluence export, đã tự viết script Node giải mã), yêu cầu đọc rồi giải thích quan hệ FOA/OET/OE/OT/OTF, sau đó hỏi tác động nếu sửa theo tư duy mới. Đối chiếu kỹ với code/schema thật qua nhiều vòng grep — phát hiện khung sườn đã có sẵn ~70% dưới tên gọi khác (`financial_obligation_archetype`≈FOA, `foa_element`≈ma trận FOA×OE, `obligation_element`/`_type`≈OE/OET đã có sẵn, `obligation_type`≈OTF chứ không phải OT thật), chỉ có 1 lỗ hổng cấu trúc thật: `obligation_type_composition` coi 1 OTF chỉ có 1 dòng OET phẳng thay vì tổ hợp NHIỀU OT lõi (Giải ngân/Hoàn trả gốc/Trả lãi/Bàn giao TS...) + 2 chỗ trùng lặp dữ liệu (`obligation_family`≡FOA, `foa_element`≡`constraint_matrix` kind=ARCHETYPE_X_ELEMENT). Xác nhận rõ: Pattern↔Block và toàn bộ Block/Attribute/Answer Slot/Selector Scope/Fragment/Template/Config/Variant/Catalog/Release KHÔNG bị ảnh hưởng (2 join độc lập, mã `obligation_type.code` không đổi tên). EnterPlanMode, hỏi user 3 quyết định qua AskUserQuestion — cả 3 chọn khuyến nghị: (1) gộp bỏ `obligation_family`, dùng thẳng archetype_code; (2) làm ĐỦ theo tài liệu (tách OT lõi, thêm OET_PARTY); (3) gộp `foa_element`+`constraint_matrix` về 1 nguồn. Backend: schema thêm bảng `obligation_type_core` (7 OT lõi cố định) + `ot_activation_rule` (OE→OT phụ trợ, độc lập FOA); `obligation_type_composition` thêm cột `ot_core_code`+`leg`; xóa `obligation_family`+`family_code`. Backfill lại TOÀN BỘ 246 dòng composition cho 9 OTF hiện có (Đúng đặc điểm tài liệu: FOA_CONDITIONAL không có OT_INTEREST). Entity/Repo/Controller mới `ObligationTypeCore`/`OtActivationRule`; `FinancialObligationArchetypeService.detail()` thêm `activationRules`; `ConstraintMatrixService` thêm `foaOeMatrix()` derived từ `foa_element` (cùng cơ chế `patternCoverage()` có sẵn) + endpoint mới. Frontend: `ObligationPage.tsx` 3→4 tab (+"Obligation Type (lõi)"), `OntologyPage.tsx` group theo Archetype thay Family + decomposition nhóm theo OT lõi, `ArchetypeDetailPage.tsx` thêm khối "Quy tắc kích hoạt OT phụ trợ", `MatrixPage.tsx` tab 1 đổi nguồn sang derived endpoint, `SysmapPage.tsx`/`DashboardPage.tsx`/`tables.ts` cập nhật hết mọi chỗ còn nhắc Family. Verified: `docker compose down -v && up --build` Maven+Flyway+tsc 0 lỗi; curl xác nhận 7 OT lõi, 1 activation rule, 246 dòng composition khớp tính tay, `foa-oe-matrix` khớp y hệt dữ liệu cũ, regression Pattern/Product Intent/Dashboard không đổi. **Chưa verify bằng Playwright** (môi trường phiên này không có công cụ browser automation) — chỉ xác nhận qua API + tsc build. Xem chi tiết PROJECT_STATUS.md Giai đoạn 51.*

---

*Cập nhật: ✅ **Sửa bug EMI trôi dần khi có phí quản lý + tính lãi/phí theo ngày thật** (Giai đoạn 50) — user gửi file Excel tham chiếu (`docs/Simulation Engine Tính Lịch.xlsx`) hỏi lịch trả nợ có đang tính sai không. Xác nhận bug thật: khi có `periodicFeePct>0`, payment mỗi kỳ trôi dần (3.001.299→2.924.802đ) thay vì cố định, do PMT chỉ tính trên lãi rồi cộng phí riêng. File Excel làm đúng: gộp Lãi+Phí thành "CPV" theo rate tổng, EMI cố định trên CPV, tách lại theo tỷ lệ, tính theo số ngày thật/365, kỳ cuối có "plug" bù sai số. EnterPlanMode (đổi lõi công thức), hỏi user 2 quyết định qua AskUserQuestion — cả 2 chọn "Có": sửa PMT dùng rate gộp + đổi hẳn sang ngày thật/365. Sửa `SimulationEngine.java`: `rTotal=r+feeMonth`, `dailyRateTotal=rTotal×12/365`, `cpv=opening×dailyRateTotal×daysInPeriod` (ngày thật qua `ChronoUnit.DAYS.between`), tách `interest`/`fee` theo tỷ lệ cấu thành, plug kỳ cuối. Verified: dựng lại input Excel (30tr/12th/1,6%/0,4%) ra EMI cố định 2.836.788đ (gần khớp Excel 2.832.000đ, lệch ~0,17% do khác giả định làm tròn — đã báo trước); VAR-108 giờ payment cố định 2.964.511đ (hết bug trôi); regression penalty/graceDays (G47) và VIP/Thân thiết (G49) vẫn đúng; Playwright xác nhận UI "Kỳ trả" cố định. Xem chi tiết PROJECT_STATUS.md Giai đoạn 50.*

---

*Trước đó: ✅ **Sửa lệch ưu đãi VIP < Thân thiết trong Simulation Engine** (Giai đoạn 49) — user so sánh 2 phân khúc phát hiện tổng phải trả VIP lại LỚN HƠN Thân thiết, hỏi lỗi logic hay gì. Test cô lập (giữ nguyên mọi tham số, chỉ đổi segmentCode) xác nhận logic tính ĐÚNG — nguyên nhân là dữ liệu `customer_segment` seed gốc lỡ đặt Thân thiết ưu đãi −0,5%/tháng CAO HƠN VIP −0,3%/tháng (ngược trực giác thông thường). Hỏi user qua AskUserQuestion, chọn đổi lại đúng thứ tự (VIP ≥ Thân thiết). Sửa: `customer_segment.name` swap (Loyalty→−0,3%, VIP→−0,5%), `SimulationEngine.segmentAdjustment()` swap case values, `SimulationPage.tsx` swap nhãn `SEGMENTS`. Verified: VAR-108 VIP giờ đúng 0,8%/th < Thân thiết 1,0%/th, tổng phải trả VIP (23.249.242đ) < Thân thiết (23.451.715đ); Playwright xác nhận nhãn UI đúng. Xem chi tiết PROJECT_STATUS.md Giai đoạn 49.*

---

*Trước đó: ✅ **Bổ sung Block "Hạn mức" (BLK_LIMIT) vào Pattern PT-001** (Giai đoạn 48) — user hỏi tại sao Template TPL-001 có "hạn mức" mà Config/Variant đóng gói từ nó ("Vay xe máy mùa tựu trường" CFG-0043/VAR-108) lại thiếu. Audit: Pattern PT-001 (nguồn TPL-001 → CFG-0021/0040/0043 → VAR-106/103/108) chưa từng gán Block `BLK_LIMIT` vào `pattern_block` — thiếu sót từ seed gốc, không phải bug code. Phát hiện thêm: `limit_range` hiển thị ở Variant đọc từ 1 cột riêng `product_variant.limit_range` (độc lập khỏi chuỗi Pattern→Block→Answer Slot) nên Variant vẫn "có vẻ" đủ hạn mức dù Answer Slot trống — gây cảm giác lệch dữ liệu. Sửa thuần `V2__seed.sql` (không đổi code): thêm `pattern_block` PT-001+BLK_LIMIT; `template_frame` default cho TPL-001/TPL-002; fragment `limit_amount`+`capacity_range` cho cả 3 Config (CFG-0021/0040/0043) khớp đúng `limit_range` thật đã có sẵn ở từng Variant (không bịa số mới). Verified: CFG-0043 `/detail` (builder) và `/resolved` (Variant tab) đều lên 17/17=100% (trước 15/15, +2 do BLK_LIMIT có 2 slot bắt buộc); PT-001 detail trả đủ 8 block; Playwright xác nhận cả builder Config lẫn tab Variant hiện đúng "Hạn mức cấp: 3.000.000đ – 40.000.000đ". Xem chi tiết PROJECT_STATUS.md Giai đoạn 48.*

---

*Trước đó: ✅ **Simulation Engine bỏ hardcode, tính từ dữ liệu thật sản phẩm** (Giai đoạn 47) — sau Giai đoạn 46, user hỏi Simulation Engine có đang hardcode không. Audit `deriveFromVariant()` chia 3 nhóm: A) đã thật (amount/months/rate/LTV, không sửa); B) DB có thật nhưng code hardcode bỏ qua — sửa `appraisalFee` (slot `fee_amount`, khác nhau thật theo Template: TPL-001=300k/TPL-002=800k/TPL-004=400k, trước đó hardcode 500k mọi sản phẩm), `segmentCode` (bảng `template_segment`, trước đó hardcode `SEG_STANDARD` luôn kể cả sản phẩm doanh nghiệp TPL-002), `penaltyFactor` (trước đó lấy trần quy định `attribute_constraint` làm giá trị áp dụng, giờ đọc đúng slot `penalty_rate`), và `grace` (slot `BLK_PENALTY.grace` — số ngày trễ được miễn phạt, trước đó hoàn toàn chưa đọc; đã hỏi user qua AskUserQuestion xác nhận KHÁC `graceMonths`/`graceOn` có sẵn — user chọn đưa vào công thức phạt: `billableDays = max(0, penaltyDays - graceDays)`); C) genuinely không có nguồn DB (`periodicFeePct`, `startDate`, toggle kịch bản) — theo yêu cầu user, KHÔNG đụng nhóm C. Backend: `SimulationService` thêm `resolveSlotValue()` (3 tầng fallback giống Giai đoạn 46), parser `parseVndAmount`/`parseFirstInt`; `SimulationRequest` thêm field `graceDays`; `SimulationEngine.run()` áp dụng trừ ân hạn vào công thức phạt, trả thêm `graceDaysApplied`. Frontend: `SimulationPage.tsx` card "TỔNG PHẠT TRỄ HẠN" hiện thêm dòng "miễn N ngày đầu". Verified: VAR-101 (không có fee/penalty riêng ở cả 3 tầng) đúng fallback 500k (không phải bug); VAR-103 (có fee_amount thật) trả đúng 300k + SEG_INDIVIDUAL; test phạt 10 ngày với graceDays=5 ra đúng phân nửa so với không có configCode (graceDays=0); Playwright xác nhận UI hiện đúng "miễn 5 ngày đầu". Xem chi tiết PROJECT_STATUS.md Giai đoạn 47.*

---

*Trước đó: ✅ **Tab "Giá trị cấu hình" ở Variant hiện đủ mọi Answer Slot** (Giai đoạn 46) — user mô tả đúng bản chất luồng resolve (Template default → Config fragment ghi đè → Variant đóng gói), muốn tab này hiện đủ mọi Answer Slot của Pattern, hỏi DB có phục vụ được không. Audit phát hiện: tab tái dùng thẳng API builder Config (`/detail`), và dòng `activeBlocks = fragBlocks.isEmpty() ? frameBlocks : fragBlocks` (cố ý từ Giai đoạn 21, khớp pixel-perfect 1 ảnh chụp CFG-0042) khiến toàn bộ block chưa từng có fragment bị loại khỏi kết quả — kiểm chứng CFG-0042 (9 block/24 slot) chỉ trả 6 block/17 slot, mất `BLK_BILLING` dù có default Template; completeness% cũng tính thiếu vì cùng lý do. Hỏi user qua AskUserQuestion, chọn **tách endpoint riêng** thay vì sửa chung (tránh regression màn builder). Backend: tách thân vòng lặp `detail()` thành private method dùng chung `buildBody(..., blockFilter)`, thêm method mới `resolved(code)` (`blockFilter=null`) + `GET /api/product-configs/{code}/resolved`; bổ sung field `slotDefaultValue` (cột `answer_slot.default_value` có sẵn nhưng trước đây không dùng) làm tầng fallback thứ 3 cho cả 2 method. Frontend: `ConfigValuesTab` đổi sang gọi `/resolved`, thêm fallback "Mặc định Answer Slot: X". Verified: curl `/detail` CFG-0042 không đổi (6 block/17 slot, không regression); `/resolved` trả đủ 9 block/24 slot, completeness đúng thật 67% (12/18, trước đây báo sai 86%); Playwright `/variant/VAR-101` hiện đủ 9 block kể cả block trước đây ẩn, `/config/CFG-0042` builder không đổi. Xem chi tiết PROJECT_STATUS.md Giai đoạn 46.*

*Cập nhật: ✅ **Detail cho Nhật ký hoạt động** (Giai đoạn 45) — user yêu cầu "làm màn detail cho nhật ký hoạt động". Prototype gốc không có (xác nhận lại từ Giai đoạn 29), nên dựng UI mới đúng ngôn ngữ thiết kế các detail khác. Backend: `ActivityLogService.toRow()` bổ sung field `id` còn thiếu, thêm `detail(id)` + `GET /api/activity-logs/{id}/detail`. Frontend: chuyển `ActivityPage.tsx` vào `pages/activity/` (đúng convention khi có thêm detail), thêm `onRowClick`; `ActivityDetailPage.tsx` mới — banner badge màu theo hành động tái dùng thẳng `STATUS_COLORS` (alias create~draft/submit_review~review/approve~approved/publish~published/retire~retired, không bịa màu), 4 info card, nút "Xem chi tiết" nhảy đúng route entity nguồn nếu có (BI/PI quy đổi code→id vì route dùng `:id`; Pattern/Template/Config/Variant dùng thẳng `:code`; AnswerSlot/ConstraintMatrix không có detail nên ẩn nút). Khối "Hoạt động liên quan của đối tượng này" tái dùng THẲNG `ApprovalHistory` (Giai đoạn 42) qua 1 prop `title` mới — không viết component/API mới. Route `/activity/:id`. Verified: curl detail + 404; Playwright bấm dòng đầu list → detail đúng, bấm "Xem chi tiết" nhảy đúng `/variant/VAR-108`, 2 case không có detail (AnswerSlot/ConstraintMatrix) không crash, không hiện nút chết. Xem chi tiết PROJECT_STATUS.md Giai đoạn 45.*

*Cập nhật: ✅ **Lịch sử duyệt cho Business Intent & Product Intent + filter Hành động** (Giai đoạn 44) — user yêu cầu 2 việc: (1) mở "Lịch sử duyệt" lên 2 tầng đầu Pipeline (BI/PI) chưa có; (2) filter "Loại" ở Nhật ký hoạt động đổi thành filter theo Hành động. Audit 13 entity BI(7)/PI(6) cùng cách Giai đoạn 43: 8/13 thiếu 1-4 bước activity_log so với status thật, thêm 32 dòng (tổng 133), viết lại created_user/updated_user. Thêm `<ApprovalHistory>` (component có sẵn từ Giai đoạn 42, không sửa gì) vào cuối `BusinessIntentDetailPage.tsx`/`ProductIntentDetailPage.tsx`. Phát hiện thêm: filter "Loại" ở `ActivityPage.tsx` **chưa từng hoạt động** — nhãn không khớp cột nào trong `ListScreen.guessColumnIndex()` nên dropdown luôn rỗng; đổi `'Loại'`→`'Hành động'` khớp đúng cột có sẵn, `ListScreen` tự suy 8 option thật (Tạo/Gửi duyệt/Phê duyệt/Xuất bản/Thu hồi/Gán/Đồng bộ/Cập nhật), không cần sửa gì khác. Verified: SQL đối chiếu 13 entity khớp 100%; Playwright BI-01/PI-005 hiện đủ lịch sử, filter Hành động lọc được. Xem chi tiết PROJECT_STATUS.md Giai đoạn 44.*

*Cập nhật: ✅ **Vá "Lịch sử duyệt" trống trên toàn Pipeline** (Giai đoạn 43) — user phát hiện qua UI Template `TPL-001` status "Đã xuất bản" nhưng "Lịch sử duyệt" trống trơn, hỏi lỗi data hay code. Chẩn đoán: lỗi **data** — component/API đúng, chỉ là `activity_log` thật sự thiếu dòng. Audit mở rộng ra TOÀN BỘ 28 entity Pattern(6)/Template(6)/Config(8)/Variant(8), đối chiếu 3 nguồn (status thật/activity_log/version_entry): 22/28 entity thiếu 1-4 bước trong chuỗi create→submit_review→approve→publish/retire. Phát hiện thêm bug cùng loại Giai đoạn 40 (Config↔Variant) nhưng ở cấp Template↔version_entry: `TPL-003.status='review'` trong khi version_entry head thật đã `published` từ trước và cả 2 Config đóng gói từ nó đã published — sửa status khớp lại. Thêm 61 dòng `activity_log` (tổng 101), tái dùng actor/mốc thời gian từ `version_entry` khi có sẵn để 2 nguồn khớp nhau; viết lại toàn bộ UPDATE `created_user`/`updated_user` (Giai đoạn 42) khớp chuỗi mới — hết NULL trong 4 loại entity. Verified: SQL đối chiếu status↔hành động muộn nhất cho cả 28 entity khớp 100%; Playwright TPL-001/TPL-003 hiện đủ 4 bước đúng. Ngoài phạm vi (đã báo, chưa sửa): BI/PI vẫn thiếu vài dòng (không có UI đọc), 5/6 Template chưa có version_entry riêng (nút "Phiên bản" sẽ trống). Xem chi tiết PROJECT_STATUS.md Giai đoạn 43.*

*Cập nhật: ✅ **Nhật ký hoạt động theo audit thật + Menu theo Role người dùng** (Giai đoạn 42) — user hỏi "ai duyệt, ai làm gì cho từng sản phẩm" rồi muốn xử lý theo cột audit (`created_user`/`updated_user`, thêm Giai đoạn 40 nhưng chưa từng populate — toàn NULL), đồng ý thêm bảng user; kế đó bổ sung yêu cầu menu sidebar lọc theo role, có 1 role Admin thấy hết. Đã EnterPlanMode do phạm vi lớn. Quyết định: bộ chọn "đổi vai trò" là demo (dropdown, không mật khẩu/session), lọc menu THUẦN frontend — không phải đăng nhập thật (nói rõ với user). `activity_log.actor`/`version_entry.author` giữ `varchar` (không FK hoá, tránh viết lại ~70 dòng có sẵn). Backend: enum `user_role_enum` (5 giá trị, tách khỏi `release_role_enum`) + bảng `app_user` (6 dòng, 5 tên đã dùng thật + 1 "Quản trị viên" mới) + `AppUserController` (`GET /api/users`). Populate `created_user`/`updated_user` cho 6 loại entity (BI/PI/Pattern/Template/Config/Variant) suy trực tiếp từ `activity_log` thật (create→created_user, hành động muộn nhất→updated_user), chỉ update dòng có ≥1 activity_log thật (kể cả `product_catalog` — audit phát hiện KHÔNG có activity_log nào nhắc catalog nên bỏ qua, không suy đoán). Tổng quát `ActivityLogService.forEntity()` (có sẵn từ Giai đoạn 36) thành `GET /api/activity-logs/entity?type=&code=` dùng chung. Frontend: `UserContext.tsx` (Context + localStorage, mặc định Admin); `nav.ts` thêm `roles?: UserRole[]`; `Layout.tsx` thay khối user hardcode (phát hiện qua audit là literal string vô nghĩa) bằng dropdown thật lọc `NAV` theo role. `ApprovalHistory.tsx` mới (tái dùng style timeline "Hoạt động gần đây" của Variant) thêm vào Config/Pattern/Template detail. Verified: curl `/api/users` + `/api/activity-logs/entity`; Playwright Admin 20 mục, Product Owner 4 mục, Checker/Approver 7 mục, role giữ nguyên sau F5, Lịch sử duyệt CFG-0043 đúng 4 bước thật. Xem chi tiết PROJECT_STATUS.md Giai đoạn 42.*

*Cập nhật: ✅ **Sample data "Vay xe máy mùa tựu trường"** (Giai đoạn 41) — user yêu cầu 1 sản phẩm vay hoàn chỉnh đầy đủ các bước, đã duyệt qua kiểm duyệt. Tái dùng khuôn `PT-001`/`TPL-001` published có sẵn (không tạo trùng BI/PI/Pattern mới — đúng bản chất tái sử dụng Pattern/Template). `CFG-0043` (17 fragment, completeness 100%, 2 fragment ưu đãi thời vụ: Học sinh/sinh viên + Mùa tựu trường) + `VAR-108` (published, niêm yết App+Web) + `version_entry` CFG-0043 đủ 5 bước + 8 dòng `activity_log` (create/submit_review/approve/publish cho cả Config và Variant) — dấu vết duyệt đầy đủ, không set thẳng status. Verified: completeness 15/15, lineage đúng, Release process tự tính done 8/8 (runtime, không cần seed thêm), search ra đúng cả Config/Variant, Playwright UI đẹp. Xem chi tiết PROJECT_STATUS.md Giai đoạn 41.*

*Cập nhật: ✅ **Sửa lệch lifecycle Config↔Variant + thêm cột audit/CDC vào 43 bảng** (Giai đoạn 40) — user phát hiện qua UI: CFG-0042 "chờ duyệt" nhưng VAR-101 đóng gói từ nó lại "đã xuất bản". Verify bằng SQL join thật (theo `from_config_code`) xác nhận là lỗi seed data thật, không phải hiển thị — Variant không thể tiến xa hơn Config nguồn trong lifecycle. Nâng status 3 Config (CFG-0042/0041/0038) khớp/vượt Variant, kèm `version_entry`+`activity_log` đầy đủ cho từng bước approve/publish (không đổi trơ 1 cột). Cùng đợt: theo file DDL mới user cung cấp, thêm 9 cột audit (`created_user`/`updated_user`/`deleted_user`/`is_deleted`/`cdc_version`/`cdc_timestamp`...) cho TẤT CẢ 43 bảng + 4 cột governance (`data_owner`/`data_steward`/`sensitivity`/`retention_policy`) cho 8 bảng nghiệp vụ chính — tự sửa lỗi double-encode UTF-8 trong file gốc trước khi áp dụng (không copy nguyên mojibake). Verified: SQL join xác nhận mọi Variant.status ≤ Config.status nguồn, Playwright UI khớp. Xem chi tiết PROJECT_STATUS.md Giai đoạn 40.*

*Cập nhật: ✅ **Làm thật nút "Xem trước" ở builder Product Pattern** (Giai đoạn 39) — nút này trước đây no-op như mọi nút CUD, nhưng bản chất là hành động XEM nên đúng tiền lệ nút "Phiên bản" đã làm thật. Hỏi user 2 quyết định UI (`AskUserQuestion`): overlay toàn màn hình (không phải drawer) + KHÔNG thêm nút Xuất PDF (tránh no-op gây hiểu lầm). Không cần API mới — data Preview (blocks/slots/assignedOTs/coverage) đã có sẵn trong state builder, `PatternPreviewModal.tsx` mới chỉ render prop. Nội dung: header + 3 stat card + Nghĩa vụ tài chính + Độ phủ ma trận + cấu trúc đầy đủ theo `position`. Verified Playwright PT-002: 9 Block/24 Slot/2 Obligation Type, đúng badge độ phủ, đúng cấu trúc từng block. Xem chi tiết PROJECT_STATUS.md Giai đoạn 39.*

*Cập nhật: ✅ **Hoàn thiện thanh tìm kiếm toàn cục ở topbar** (Giai đoạn 38) — user yêu cầu "trang nào có thanh tìm kiếm thì hoàn thiện nó". Rà soát: search trong `ListScreen` đã thật từ Giai đoạn 20 (không cần sửa); thanh search topbar (`Layout.tsx`) hoàn toàn trang trí — không state, không logic, y nguyên từ prototype, xuất hiện ở MỌI trang. Backend mới `GlobalSearchService` quét 12 repo lõi có sẵn (Business Intent/Product Intent/Pattern/Template/Config/Variant/Obligation Type/Archetype/Block/Attribute/Domain/Lifecycle), `findAll()`+lọc substring trong Java (dữ liệu nhỏ, không cần derived query), tối đa 5 kết quả/loại, `path` trỏ route chi tiết có sẵn (Obligation Type chưa có detail riêng → trỏ qua Archetype cha qua `archetype_code`). `GET /api/search?q=`. Frontend `GlobalSearch.tsx` mới thay input tĩnh: debounce 250ms, dropdown kết quả có icon/StatusChip, `⌘K` focus, `Escape`/click-outside đóng. Phát hiện lúc verify: `curl` qua Git Bash Windows không encode UTF-8 đúng cho tiếng Việt có dấu (không phải bug — percent-encode thủ công xác nhận backend đúng); verify cuối dùng Playwright gõ trực tiếp. Verified: gõ "xe máy" ra 6 kết quả đúng đa loại, bấm điều hướng đúng route. Xem chi tiết PROJECT_STATUS.md Giai đoạn 38.*

*Cập nhật: ✅ **Chia detail Product Variant thành 3 tab con** (Giai đoạn 37) — user phân vân xem "chi tiết toàn bộ FOA/Block/Attribute/giá trị" nên đặt ở Catalog hay Variant. Recommend (user đồng ý): Catalog giữ nguyên trỏ Release (đúng nghĩa của nó); đào sâu ngay ở Variant vì đó là entity gốc nắm `fromConfigCode`. Vì nội dung dài, chia 3 tab thay vì cuộn dài: **Tổng quan** (nội dung Giai đoạn 36), **Giá trị cấu hình** (tái dùng thẳng `GET /api/product-configs/{code}/detail` có sẵn từ Giai đoạn 21 — block/slot/giá trị resolve theo scope, badge cảnh báo), **Nghĩa vụ tài chính** (tái dùng thẳng `GET /api/product-patterns/{code}/detail` có sẵn từ Giai đoạn 13 — Obligation Type + archetype + vai trò). **Không thêm backend nào** — `configCode`/`patternCode` lấy từ chính response Variant detail đã có. Phát hiện lúc verify: đoán nhầm role là `primary`/`secondary`, DB thật lưu `Primary`/`Support` — sửa lại map cho khớp thật. Verified Playwright cả 3 tab với VAR-101. Xem chi tiết PROJECT_STATUS.md Giai đoạn 37.*

*Cập nhật: ✅ **Detail đầy đủ cho Product Variant** (Giai đoạn 36) — user yêu cầu rõ mức đầu tư cao hơn hẳn: "thật chi tiết và đẹp và mượt và đầy đủ thông tin". Khác Block: backend chưa có sẵn, phải viết mới. `ActivityLogService` tách `toRow()` dùng chung + thêm `forEntity(entityType, entityCode)` (mới `ActivityLogRepository.findByEntityTypeAndEntityCodeOrderByOccurredAtDesc`) để lấy nhật ký riêng 1 variant, tái dùng logic dịch nhãn/suy kênh có sẵn từ Giai đoạn 33. `ProductVariantService.detail()` join lineage ngược Pattern←Template←Config←Variant (3 repo mới inject, null-safe từng bước) + niêm yết Catalog (tên/kênh/ngày/trạng thái) + activity riêng. `ProductVariantController` thêm `GET /{code}/detail`. Frontend `ProductVariantDetailPage.tsx` (giàu nhất từ trước đến nay): khối "Nguồn gốc sản phẩm" 4 ô nối mũi tên bấm được (điều hướng sang `/pattern`, `/template`, `/config` đã có sẵn — không xây lại), khối thông tin sản phẩm, card CTA "Quy trình phát hành" điều hướng `/release/{code}` (tái dùng màn Giai đoạn 32, không lặp logic 8 bước), 2 cột niêm yết Catalog + hoạt động gần đây (timeline). Route mới `/variant/:code`. Verified curl VAR-101 (published, đủ lineage/3 catalog/1 activity) + VAR-107 (draft, 2 catalog chưa có ngày, 1 activity khác) + 404 + Playwright cả 2 trạng thái. Xem chi tiết PROJECT_STATUS.md Giai đoạn 36.*

*Cập nhật: ✅ **Detail cho Block & Answer Slot** (Giai đoạn 35) — user hỏi tiếp sau Giai đoạn 34, chỉ ra màn Block & Answer Slot cũng chưa có detail. Khác Giai đoạn 34: backend đã có sẵn từ Giai đoạn 6 (`BlockService.detail(id)`/`GET /api/blocks/{id}/detail`, xây trước dự phòng cho việc gỡ fix cứng builder Pattern) — không cần sửa backend, chỉ thiếu frontend. Chuyển `BlockPage.tsx` vào `pages/block/` (đúng convention Giai đoạn 31), thêm `onRowClick` theo `id`. `BlockDetailPage.tsx` (mới): banner + chip nhóm nghiệp vụ + StatusChip, stat card số Answer Slot/"chi phối bởi", danh sách Answer Slot dạng card có badge type/required + mặc định/ràng buộc, animation `fadeUp` stagger + hover elevate (đồng bộ Giai đoạn 34). Route mới `/block/:id`. Verified curl `BLK_ELIGIBILITY/detail` (3 slot: age Range/Bắt buộc, min_income Money/Bắt buộc, occupation Enum/Tùy chọn) + Playwright list+detail. Xem chi tiết PROJECT_STATUS.md Giai đoạn 35.*

*Cập nhật: ✅ **Detail cho Lifecycle & State và Domain** (Giai đoạn 34) — prototype gốc không có detail cho 2 màn này (Giai đoạn 29 đã xác nhận), nhưng user hỏi ý tưởng rồi chốt làm cả 2, yêu cầu rõ "giao diện phải mượt mà và đẹp mắt". Backend: `LifecycleService.detail()` trả chuỗi state đúng `sort_order` (mới `LifecycleStateRepository.findByLifecycleCodeOrderBySortOrder`); `DomainService` mới join `AttributeGroup` theo `domain_code` + đếm attribute mỗi group (mới `AttributeGroupRepository.findByDomainCodeOrderByName`, tái dùng `countByGroupCode` có sẵn). Cả 2 thêm endpoint `GET /{code}/detail`, giữ nguyên list cũ. Frontend: chuyển `LifecyclePage`/`DomainPage` vào subfolder riêng (đúng convention Giai đoạn 31); `LifecycleDetailPage.tsx` vẽ chuỗi state machine dạng vòng tròn nối mũi tên, gradient xanh theo vị trí (không phải done/undone — đây là định nghĩa chung, không phải tiến độ 1 thực thể); `DomainDetailPage.tsx` vẽ danh sách Attribute Group dạng progress-bar theo số attribute. Cả 2 dùng animation `fadeUp` so le (stagger) + hover scale/elevate cho mượt, tái dùng banner/StatusChip style từ `ArchetypeDetailPage`/`ReleasePage`. Verified curl 2 endpoint mới + 404 case + Playwright cả 4 màn (2 list + 2 detail). Xem chi tiết PROJECT_STATUS.md Giai đoạn 34.*

*Cập nhật: ✅ **Bổ sung seed activity_log cho đủ dày** (Giai đoạn 33) — user hỏi màn Nhật ký hoạt động có fix cứng không. Audit xác nhận KHÔNG fix cứng gì (channel suy từ regex trên detail thật, nhãn hành động dịch từ code thật có fallback, count thật) — chỉ seed gốc ít (8 dòng). Thêm 20 dòng `activity_log` thật (tổng 28) khớp đúng trạng thái hiện có của business_intent/product_intent/product_pattern/product_template/product_config/product_variant, không đổi code, không bịa entity/trạng thái mới. Cần `docker compose down -v && up --build`. Verified `totalElements:28` + Playwright.*

*Cập nhật: ✅ **Liên kết Catalog ↔ Quy trình phát hành theo trạng thái** (Giai đoạn 32) — user muốn bấm 1 sản phẩm trong Catalog chuyển sang màn Quy trình phát hành đúng tiến độ 8 bước khớp trạng thái sản phẩm (draft/review/approved/published); mặc định vào từ sidebar phải done 8 bước. Phát hiện bug thật: DB chỉ có 1 dòng `maker_checker_process` (VAR-101, `done_count=4`) lệch với trạng thái thật (`published`, đáng lẽ 8/8) — bằng chứng lưu tiến độ tay dễ lệch. Quyết định (đã hỏi user): KHÔNG seed thêm dòng cho từng variant; dùng 1 dòng hiện có làm **process template** (8 bước chuẩn), tính runtime done/current/upcoming từ `product_variant.status` (draft→5/8, review→6/8, approved→7/8, published/retired→8/8; bước 7=Phê duyệt, bước 8=Đóng gói&Phát hành). Backend: `ReleaseProcessService.detailByVariant()` thay `detail(id)`, `MakerCheckerProcessRepository.findFirstByOrderById()`, controller đổi path `/{id}/detail`→`/{variantCode}/detail`, xóa `list()` không dùng. Frontend: Catalog card thêm `onClick`→`/release/{variantCode}`, route mới, `ReleasePage` gọi thẳng theo variantCode (mặc định VAR-101). Verified curl 4 trạng thái + 404 + Playwright click từ Catalog khớp đúng tiến độ. Xem chi tiết PROJECT_STATUS.md Giai đoạn 32.*

*Cập nhật: ✅ **Drill-down chi tiết attribute từ popup Group/Data Type** (Giai đoạn 30b) — user cung cấp ảnh chụp modal "Tạo Attribute mới" từ prototype gốc, muốn dùng đúng bố cục đó làm màn xem-chỉ-đọc khi bấm 1 attribute trong popup Group/Data Type. Quyết định: drill-down thay nội dung popup (nút "← Quay lại"), bỏ hẳn trường không có cột DB (số chữ số thập phân, cho phép âm, căn cứ pháp lý, scope ưu tiên override). Backend thêm `unique`/`nullable`/`defaultValue` vào response `/api/attributes/{code}/usage` sẵn có (không tạo endpoint mới). Frontend `AttrDetailBody` mới trong `AttributePage.tsx`: Thông tin cơ bản, dãy nút Data Type THẬT (9 kiểu DB, không dùng list tĩnh prototype), 3 badge ràng buộc chung, danh sách ràng buộc + enum tái dùng cách render của `AttributeUsageDetailPage`. Verified Playwright: "Lãi suất cơ sở" hiện đúng Percent highlight, Bắt buộc=Có/Duy nhất=Không/Nullable=Có, giá trị mặc định, ràng buộc trần thật.*

*Cập nhật: ✅ **Popup xem nhanh Attribute Group/Data Type** (Giai đoạn 30) — user hỏi tiếp về 2 tab còn lại sau Giai đoạn 29. Đối chiếu prototype: cả 2 tab đều `onClick` nhưng mở modal CUD "Tạo Attribute mới" dùng chung (không phải detail riêng theo dòng) — modal này chưa cài cho màn nào (đúng luật no-op). User xác nhận chỉ muốn xem thông tin, không cần modal tạo/sửa. Thêm `InfoModal` trong `AttributePage.tsx`: bấm dòng Group/Data Type mở popup thông tin + danh sách attribute liên quan, lọc client-side từ danh sách Attribute đã fetch sẵn — không thêm API, không bịa. Verified Playwright: "Pricing" → đúng 3 attribute thật; "Money" → đúng 4 attribute thật.*

*Cập nhật: ✅ **Attribute Usage screen** (Giai đoạn 29, mục 5.4) theo yêu cầu user rà toàn bộ 20 nav key xem list→detail đã đủ chưa, so với `docs/Product Factory 5.1.html`. Khảo sát xác nhận bản gốc chỉ có đúng 2 detail thật theo bản ghi (`archetype`, `attribute`) — `attribute→attrUsage` là cái duy nhất còn thiếu (13 list khác không có detail trong bản gốc, giữ nguyên, không tự vẽ thêm). Backend mới: `AttributeEnumValue`(+Id), `TemplateFrameRepository`/`FragmentRepository.findByBlockIdAndSlotCode`, `ProductVariantRepository.findByFromConfigCodeIn`, `AttributeUsageService`/`AttributeUsageController` (`GET /api/attributes/{code}/usage`). Frontend: route `/attribute/:code` → `AttributeUsageDetailPage.tsx` (rail chọn attribute, 5-stage lineage, constraint, Selector Scope, where-used — trích đúng markup `attrUsageModel()` prototype, bỏ `desc` vì bảng `attribute` không có cột description). Verified curl `base_rate` (2 constraint/6 template/7 config đa scope có warning/7 variant) + `occupation` (case rỗng, không lỗi) + 404 case + Playwright 3 màn hình khớp bố cục gốc. Xem chi tiết PROJECT_STATUS.md Giai đoạn 29.*

*Cập nhật: ✅ **Audit toàn diện sample data** (Giai đoạn 22) theo yêu cầu user kiểm tra 7 khu vực (Business Intent, Product Intent, Product Pattern, Product Template, Product Config, Block & Answer Slot, Attribute) xem quan hệ nào thiếu dòng con. Query trực tiếp DB thật đếm theo FK (không chỉ đọc seed file) → phát hiện 5 lỗ hổng thật: `business_intent_kpi` 5/7 BI trống, `product_intent_element` 4/6 PI trống, `template_frame` 5/6 template trống, `fragment` 6/7 config trống (vi phạm bất biến DDL "mỗi config-slot phải có ≥1 fragment default"), `occupation` (DT_ENUM) 0 enum value. Bổ sung toàn bộ, dữ liệu suy diễn từ cấu trúc thật đã có (`obligation_type_composition`, `pattern_block`, `answer_slot.default_value`) — không bịa số. Verified `docker compose down -v && up --build` sạch + query lại DB xác nhận mọi FK có ≥1 dòng con + API `/product-configs/CFG-0021/detail` completeness từ 0%→100%. Xem chi tiết PROJECT_STATUS.md Giai đoạn 22.*

*Ghi chú lịch sử: ✅ **VIẾT LẠI Product Config** (Giai đoạn 21, mục 3.2) theo phản hồi trực tiếp của user (đối chiếu file prototype gốc, chỉ ra bản Giai đoạn 15 rút gọn sai). Trích đúng markup builder `isConfigForm` (3 cột: sidebar Block/Slot, 2 banner, card slot, form thêm fragment, bảng fragment, panel Resolution People/Place/Time). Sửa 1 lỗi seed thật (`product_config.from_template_code` CFG-0042: TPL-001→TPL-003, TPL-001 không có `template_frame` nào) + bổ sung 3 fragment mặc định thiếu (`interest_calc`/`capacity_range`/`asset_valuation`, dùng đúng `default_value` có sẵn — không bịa). Viết lại toàn bộ `ProductConfigController#detail` (block "đang áp dụng" suy từ chính `fragment` thật, không còn phụ thuộc `template_frame` một chiều) + `ProductConfigDetailPage.tsx`. Verified Playwright khớp gần như tuyệt đối ảnh chụp gốc user cung cấp (Resolution tính đúng người thắng theo ưu tiên People>Place>Time>Default). **Lưu ý: cần `docker compose down -v` trước khi `up --build` lại vì đã sửa `V2__seed.sql` sau khi Flyway đã chạy.** **Còn lại đợt polish cuối: 5.3 (loading/error, Docker) — mục 5.4 (Attribute Usage) vẫn hoãn theo quyết định gốc.*

*Ghi chú lịch sử: ✅ Hoàn thành **Business Intent detail+KPI + ListScreen interactive** (Giai đoạn 20, mục 5.1+5.2). BI detail: backend thêm `BusinessIntentKpi`(+Id) + `/business-intents/{id}/detail`, frontend `BusinessIntentDetailPage` (route `/businessintent/:id`, không có markup gốc trong prototype nên dựng mới theo mẫu `ProductIntentDetailPage`, dữ liệu KPI thật 100%). ListScreen interactive: search+filter chạy client-side qua `extractText()` (đệ quy cell React, fallback đọc `status`/`label` cho component tùy biến) — không cần sửa bất kỳ page nào khác, áp dụng ngay cho toàn bộ ~20 màn list hiện có.*

*Ghi chú lịch sử: ✅ Hoàn thành **Simulation Engine** (Giai đoạn 19, mục 3C) — **TOÀN BỘ 18 MÀN + phần 10% tính toán ĐÃ HOÀN TẤT.** Backend package mới `simulation` (`SimulationScenario`/`SimulationScheduleRow` đọc kịch bản mẫu thật + `SimulationEngine` thuần Java tính annuity dư nợ giảm dần, cổng lại công thức bundler: PMT, ân hạn, trả bớt gốc, tất toán sớm, phạt trễ hạn, điều chỉnh lãi theo tier). `POST /api/simulation/run` là nút DUY NHẤT trong dự án gọi tính toán thật (không no-op) — không ghi DB. Verified Playwright: khớp chính xác PMT/tổng lãi/tổng phải trả/LTV/18 dòng lịch trả nợ với seed thật (phát hiện+sửa 1 lỗi lúc verify: tổng phải trả thiếu phí thẩm định 1 lần); thử tăng số tiền vay → verdict "Không hợp lệ" đúng khi LTV vượt 80%. **Kế tiếp = đợt polish cuối (mục 5): Business Intent detail+KPI, ListScreen interactive, loading/error, Docker.***

*Ghi chú lịch sử: ✅ Hoàn thành **Release + Activity Log** (Giai đoạn 18, mục 3B). `release` là stepper 8 bước + swimlane (không phải list) — backend package mới `release` (`MakerCheckerProcess`/`ProcessStep`/`ProcessStepChecklist`) đọc thật `maker_checker_process`/`process_step`/`process_step_checklist` (khớp seed y hệt bundler hardcode: done=4/8, step_status/is_done thật). `activity` là list đơn giản — backend package mới `activity` (`ActivityLog`), cột KÊNH suy ra thật bằng regex trên `detail` (hậu tố "· kênh X", khớp 8/8 dòng), bỏ số bịa "1.284" dùng COUNT thật. Verified Playwright (stepper/swimlane/list đều khớp seed thật).*

*Ghi chú lịch sử: ✅ Hoàn thành **Product Catalog** (Giai đoạn 17, mục 3.4) — **HOÀN TẤT TOÀN BỘ NHÓM PIPELINE SẢN PHẨM.** `catalog` là card grid riêng (`isCatalog`, không phải `isList`/wizard), không cần entity mới (tái dùng `ProductCatalog`/`CatalogListing` từ Variant). `ProductCatalogController` join `product_variant`+`catalog_listing`→`product_catalog.channel`, chỉ hiện variant đã niêm yết ≥1 catalog. Verified Playwright khớp verbatim 6 card thật.*

*Ghi chú lịch sử: ✅ Hoàn thành **Product Variant** (Giai đoạn 16, mục 3.3) — màn LIST (xác nhận `variant` nằm trong `isList`, click dòng chỉ mở drawer tạo-mới chung). Phát hiện: cột "Kênh" của prototype suy ra được THẬT từ `catalog_listing→product_catalog.channel` (không cần bỏ, không fabricate). Backend 4 entity mới package `pipeline` (`ProductVariant`/`ProductCatalog`/`CatalogListing`+Id).*

*Ghi chú lịch sử: ✅ Hoàn thành **Product Config** (Giai đoạn 15, mục 3.2) — cả list VÀ detail (`/config/:code`). Cùng bài học Template: view `configForm` gốc của prototype dùng dữ liệu tĩnh (`configBase()`, không đổi theo dòng click) → dựng `/{code}/detail` là màn XEM fragment thật, gom theo Answer Slot, sắp theo `selector_scope.priority`. Backend 3 entity mới package `pipeline` (`SelectorScope`/`ProductConfig`/`Fragment`).*

*Ghi chú lịch sử: ✅ Hoàn thành **Product Template** (Giai đoạn 14, mục 3.1) — cả list VÀ detail (`/template/:code`). Wizard "Tạo Product Template" gốc của prototype là dữ liệu TĨNH 100% (không đổi theo dòng click) → dựng `/{code}/detail` là màn XEM 1 template thật (tái dùng layout 3 bước), suy ra "Block đang áp dụng" từ có/không có dòng `template_frame` thật. Backend 4 entity mới package `pipeline` (`ProductTemplate`/`CustomerSegment`/`TemplateSegment`/`TemplateFrame`).*

*Ghi chú lịch sử: ✅ Hoàn thành **WIRE builder Pattern về DB thật** (Giai đoạn 13, mục 2.7) — `ProductPatternController#/{code}/detail` mở rộng join thật `block`/`answer_slot`/`attribute`/`data_type`/`constraint_matrix`/`matrix_cell`; **`patternBuilderData.ts` đã xóa hoàn toàn**, không còn field/dòng nào fix cứng trong toàn bộ nhóm thư viện nền tảng + builder Pattern.*

*Ghi chú lịch sử: ✅ Hoàn thành **Ontology + Sysmap** (Giai đoạn 12, mục 2.6) — **HOÀN TẤT TOÀN BỘ NHÓM THƯ VIỆN NỀN TẢNG.** Cả 2 màn là card/flex/grid tĩnh (không SVG/canvas), không cần backend mới — tổng hợp client-side từ API Lớp I đã có.*

*Ghi chú lịch sử: ✅ Hoàn thành **Domain + Lifecycle & State** (Giai đoạn 11, mục 2.5) — backend `DomainController` (read-only thuần) + `LifecycleController` (join stateCount) + frontend `DomainPage`/`LifecyclePage` (list đơn giản, không tab/detail), verified.*

*Ghi chú lịch sử: ✅ Hoàn thành **Financial Obligation Archetype** (Giai đoạn 10, phần 2 mục 2.4 — hoàn tất mục 2.4) — backend `FinancialObligationArchetypeController` join làm giàu (typeCount/elementCount/productCount) + frontend `ArchetypePage` (card grid) + `ArchetypeDetailPage` (route `/archetype/:code`), verified.*

*Ghi chú lịch sử: ✅ Hoàn thành **Obligation Library** (Giai đoạn 9, phần 1 mục 2.4) — backend package `ontology` (3 controller chuyển sang tự viết join làm giàu) + frontend ObligationPage 3-tab pixel-perfect, verified.*

*Ghi chú lịch sử: ✅ Hoàn thành **Attribute** (Giai đoạn 8, CHỈ MÀN LIST) — backend package `attribute` (Domain/AttributeGroup/AttributeConstraint mới + AttributeController/AttributeGroupController/structure.DataTypeController join làm giàu) + frontend AttributePage 3-tab pixel-perfect, verified. Màn "Attribute Usage" (lineage) + modal tạo/sửa hoãn — ghi nợ mục 5.4 (chờ Pipeline + fragment/selector_scope).*

*Ghi chú lịch sử: ✅ Hoàn thành **Ma trận ràng buộc** (Giai đoạn 7) — backend package `governance` + frontend MatrixPage 4-tab pixel-perfect, verified. Zip `product-factory-phase7-matrix.zip`.*
